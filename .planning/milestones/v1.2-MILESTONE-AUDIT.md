---
milestone: v1.2
milestone_name: M4 Predictive Echo + Daily-Driver Readiness
audited: 2026-06-07
status: gaps_found
disposition: accepted — gaps are deliberately deferred (Phase 18 → future milestone); closing per operator decision
scores:
  requirements: 17/19            # SEC-01/SEC-02 deferred (Phase 18)
  core_phases: 8/8 complete      # phases 10-17
  integration: 8/8 seams wired   # 0 broken, 0 orphaned (gsd-integration-checker, opus)
  flows: 5/5 complete            # predictive-echo, speculative overlay, noecho, resume gate, QoL
gaps:
  requirements:
    - id: SEC-01
      status: unsatisfied
      phase: "Phase 18 (Security Design Pass)"
      reason: "Security design document not written. Phase 18 explicitly DEFERRED to a future milestone (operator decision post Phase-17 sign-off). Accepted, not a regression."
    - id: SEC-02
      status: unsatisfied
      phase: "Phase 18 (Security Design Pass)"
      reason: "TOFU fingerprint-confirm prompt not implemented. Deferred with Phase 18. (Note: 999.1 review verified TOFU/known_hosts pinning + host-key-mismatch hard-fail already exist; SEC-02 is the interactive first-contact prompt, still deferred.)"
  integration: []   # none — all M4 cross-phase seams wired and proven by live E2E tests
  flows: []         # none broken
tech_debt:
  - phase: "11 / 15 / 16 / 999.3 (human_needed verifications)"
    items:
      - "Deferred LIVE-terminal visual sign-off items (datagram render feel, speculative-overlay visuals, QoL banner/clipboard, terminal-rendering pack). Engineering verified; on-terminal confirmation deferred to operator testing."
  - phase: "12 (historical gaps_found)"
    items:
      - "Phase 12 VERIFICATION.md status was gaps_found at the time; SYNC-02 is WIRED and green at HEAD per the integration check + full workspace tests. Superseded — non-blocking."
  - phase: "HARDEN-02 sign-off"
    items:
      - "Windows MSVC CI job is wired (build-windows in ci.yml); a green Actions run still needs an operator push to confirm (D-16-04b). Not an integration break."
  - phase: "999.1 review (post-auth)"
    items:
      - "OSC-OOM: vte std unbounded osc_raw can OOM the server terminal model on a giant OSC in PTY output (post-auth). Documented in docs/999.1-SECURITY.md; fix tracked as backlog 999.7."
      - "RUSTSEC-2023-0071 (rsa via ssh-key, parse-only) accepted residual."
  - phase: "backlog carried forward (999.x)"
    items:
      - "999.2 client trust-boundary hardening; 999.5 full-screen TUI / alt-screen rendering; 999.6 burst repaint pacing; 999.7 OSC-OOM. Parking-lot items captured during M4, not M4 scope — carry to the next milestone's backlog."
---

# v1.2 (M4 Predictive Echo + Daily-Driver Readiness) — Milestone Audit

**Disposition: closing with accepted gaps.** The milestone's core — a roaming-tolerant predictive-echo shell that's a viable daily driver — is delivered and verified end-to-end. The only unsatisfied requirements (SEC-01/SEC-02) belong to Phase 18, which was explicitly deferred to a future milestone. Operator is closing v1.2 and will report any issues from later testing against a new milestone.

## Requirements: 17/19 satisfied

| Group | Reqs | Status |
|-------|------|--------|
| HARDEN-01/02/03 | PTY reader race fix, Windows CI gate, WSAEMSGSIZE | ✓ satisfied (HARDEN-02 green-run sign-off is an operator push) |
| SYNC-01/02/03 | sparse wire format, terminal state model, coalesced sender + resume gate | ✓ satisfied |
| PREDICT-01..06 | confirmed render, speculative echo, conservative reset, noecho suppression, adaptive RTT, wide-char | ✓ satisfied |
| PREDICT-07 | live Windows-host predictive-echo validation | ✓ satisfied (operator-signed `docs/windows-echo-test.md`, 2026-06-02; checkbox now reflects this) |
| QOL-01..04 | loss banner, OSC 52 clipboard, terminal title, RTT status | ✓ satisfied |
| **SEC-01/SEC-02** | security design doc, TOFU fingerprint prompt | ✗ **deferred to a future milestone (Phase 18)** |

## Phases: 10–17 complete (8/8 core)

All eight core phases shipped with per-phase verification (10/13/14 passed; 11/15/16 human_needed for deferred live-terminal items; 17 live-validated on Windows; 12's historical gaps_found superseded — SYNC-02 wired + green at HEAD). Phase 18 deferred. Backlog 999.1/999.3/999.4 also completed during the M4 cycle (999.1 server fuzz-hardening verified passed; 999.3 terminal-rendering pack; 999.4 read-s/mash-glitch fixes).

## Integration: all seams wired (gsd-integration-checker, opus)

8/8 cross-phase connections WIRED, 0 broken, 0 orphaned; full workspace green at HEAD. The headline predictive-echo pipeline is proven end-to-end by live tests: PTY → TerminalState → coalesced 16 ms state-diffs over QUIC datagrams (resume-gated) → client confirmed render (single composition path) → speculative overlay with epoch/noecho/CJK handling → epoch-ack. Speculative echo, noecho suppression (adversarial `read -s`), the resume gate, and the QoL features all wire through the single render path.

## What carries forward

- **Phase 18 (SEC-01/SEC-02)** → a future milestone (security design + TOFU prompt).
- **Backlog 999.2 / 999.5 / 999.6 / 999.7** → next milestone's backlog (parking-lot items found during M4).
- **Operator follow-ups:** confirm the green Windows `cargo audit` + `build-windows` CI runs; live-test the deferred human_needed items and the 999.4 fixes on Windows.

**Verdict:** v1.2 delivers its core value and is sound to close. Outstanding items are deliberate deferrals, not defects.
