---
phase: 21-channel-multiplexing-foundation
plan: 03
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - crates/nosh-client/src/client.rs
  - crates/nosh-client/src/channel.rs
  - crates/nosh-client/src/lib.rs
autonomous: true
requirements: [MUX-01, MUX-02, MUX-03, MUX-04, MUX-05]
must_haves:
  truths:
    - "The client sends ChannelOpen on the control stream, awaits ChannelAccept/ChannelReject, then opens a bidi stream and writes the channel-id varint prefix before any payload"
    - "Client-initiated channel ids are allocated from an even counter (parity rule)"
    - "Each client channel runs in its own tokio::spawn task; it reads channel data, sends ChannelCredit back through a control-stream mpsc as it drains, and exits on EOF/close"
    - "ChannelAccept/ChannelReject for an unknown id is a logged no-op, never a panic"
    - "After ReattachOk the client can re-send ChannelOpen to re-establish a channel (channels are never byte-replayed)"
  artifacts:
    - path: "crates/nosh-client/src/channel.rs"
      provides: "client run_channel_task + even-id allocator + ChannelCredit replenishment"
      contains: "run_channel_task"
    - path: "crates/nosh-client/src/client.rs"
      provides: "open_channel, send_channel_open, await_channel_accept, varint prefix write"
      contains: "open_channel"
  key_links:
    - from: "crates/nosh-client/src/client.rs open_channel"
      to: "control stream"
      via: "write_message(ChannelOpen) then await_channel_accept on control recv"
      pattern: "ChannelOpen"
    - from: "crates/nosh-client/src/client.rs open_channel"
      to: "new bidi stream"
      via: "open_bi then write postcard varint channel-id prefix"
      pattern: "open_bi"
---

<objective>
Add the client side of the channel-mux layer: an even-id allocator, `open_channel` (send
`ChannelOpen` on the control stream, await `ChannelAccept`/`ChannelReject`, then `open_bi` and
write the channel-id varint prefix), a per-channel `tokio::spawn` task that drains channel data
and replenishes the server's send credit via `ChannelCredit`, and the cold-reattach re-open path.

Purpose: Deliver the client half of MUX-01/MUX-02/MUX-04, the client side of MUX-03 (credit
granting), and MUX-05 (re-open over the control stream after ReattachOk — never byte-replayed).
Output: `crates/nosh-client/src/channel.rs` (new) and new control-stream helpers in client.rs.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-channel-multiplexing-foundation/21-CONTEXT.md
@.planning/phases/21-channel-multiplexing-foundation/21-RESEARCH.md
@.planning/phases/21-channel-multiplexing-foundation/21-PATTERNS.md
@.planning/phases/21-channel-multiplexing-foundation/21-01-SUMMARY.md

<interfaces>
<!-- New wire types from plan 21-01. -->
Message::ChannelOpen { channel_id: u32, channel_type: ChannelType }   // disc 10
Message::ChannelAccept { channel_id: u32 }                            // disc 11
Message::ChannelReject { channel_id: u32 }                            // disc 12
Message::ChannelCredit { channel_id: u32, bytes: u64 }                // disc 13
Message::ChannelClose { channel_id: u32 }                             // disc 14
enum ChannelType { Echo, Scrollback, PortForward, AgentForward }

<!-- Verified client.rs landmarks (read these, do not re-explore): -->
- open_session (open_bi + write_message SessionOpen): lines 594–607 — the shape open_channel copies.
- await_reattach_reply (read_message → match → variant_name in bail!): lines 536–551 — the shape
  await_channel_accept copies.
- send_reattach (write_message helper): lines 515–522 — the shape send_channel_open copies.
- reattach open_bi sequence (open_bi + send_reattach + await_reattach_reply): lines 232–248 of
  tests/reattach.rs — the re-establishment shape MUX-05 follows after ReattachOk.
- collect_until_close (read_message loop): client.rs ~635–670 — the channel-task drain-loop shape.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Control-stream channel helpers in client.rs (open_channel, send_channel_open, await_channel_accept)</name>
  <files>crates/nosh-client/src/client.rs</files>
  <read_first>
    - crates/nosh-client/src/client.rs lines 515–522 (send_reattach), 536–551 (await_reattach_reply), 594–607 (open_session)
    - 21-PATTERNS.md "crates/nosh-client/src/client.rs" section (open_channel / await_channel_accept / send_channel_open shapes)
  </read_first>
  <action>
    Add `pub async fn send_channel_open(control_send: &mut quinn::SendStream, channel_id: u32,
    channel_type: ChannelType) -> anyhow::Result<()>` modelled on send_reattach: write
    `Message::ChannelOpen { channel_id, channel_type }` on the CONTROL stream (not a new stream).
    Add `pub async fn await_channel_accept(control_recv: &mut quinn::RecvStream, expected_id: u32)
    -> anyhow::Result<ChannelAcceptOutcome>` modelled on await_reattach_reply: match on
    `ChannelAccept { channel_id }` (Ok(Accepted) when channel_id == expected_id),
    `ChannelReject { channel_id }` (Ok(Rejected)), any other frame → bail! with variant_name()
    (never Debug). Define a small `ChannelAcceptOutcome { Accepted, Rejected }` enum.
    Add `pub async fn open_channel(conn: &quinn::Connection, control_send, control_recv,
    channel_id: u32, channel_type: ChannelType) -> anyhow::Result<Option<(quinn::SendStream,
    quinn::RecvStream)>>`: call send_channel_open, then await_channel_accept; on Rejected return
    Ok(None); on Accepted call `conn.open_bi()`, write the channel-id varint prefix
    `postcard::to_allocvec(&channel_id)?` to the new SendStream BEFORE any payload, and return
    Ok(Some(stream pair)). This realises MUX-01 control-first ordering: ACCEPT precedes binding the
    stream, and the varint prefix binds stream→channel independent of the QUIC stream id (migration-safe).
    Import ChannelType from nosh_proto. Australian English comments; never Debug a frame.
  </action>
  <verify>
    <automated>cargo build -p nosh-client 2>&1 | tail -20</automated>
  </verify>
  <done>open_channel/send_channel_open/await_channel_accept compile and are exported; ACCEPT is awaited before open_bi; the channel-id varint prefix is written before payload; Rejected returns Ok(None).</done>
</task>

<task type="auto">
  <name>Task 2: New client channel.rs module — even-id allocator, channel drain task, credit replenishment</name>
  <files>crates/nosh-client/src/channel.rs, crates/nosh-client/src/lib.rs</files>
  <read_first>
    - crates/nosh-client/src/client.rs ~635–670 (collect_until_close drain-loop shape)
    - 21-PATTERNS.md "crates/nosh-client/src/channel.rs" section and Shared Patterns (mpsc, no-panic, send.finish)
    - 21-RESEARCH.md Pitfall 6 (credit deadlock — credit replenishment must not block on channel drain)
  </read_first>
  <action>
    Create `crates/nosh-client/src/channel.rs` and declare `pub mod channel;` in lib.rs.
    Add an even-id allocator: a small struct or function returning the next even channel id
    (start at 2, increment by 2 — id 0 is the reserved control channel; client-initiated channels
    are EVEN per the locked parity rule in 21-CONTEXT.md / ROADMAP SC#2).
    Add `pub async fn run_channel_task(channel_id: u32, mut ch_recv: quinn::RecvStream,
    ch_send: quinn::SendStream, control_tx: mpsc::Sender<Message>)`: a read loop over ch_recv that
    consumes channel bytes; as it drains the application buffer it sends
    `Message::ChannelCredit { channel_id, bytes }` back through control_tx (the SINGLE control-stream
    writer path — NEVER write_message on the control stream from this task, Pitfall 6 / A4) to
    replenish the server's 256 KiB window. On EOF/close, finish ch_send (send.finish + bounded
    send.stopped per the half-close pattern) and send `Message::ChannelClose { channel_id }` through
    control_tx, then return — releasing the stream. Unknown/closed-id conditions log + return, never panic.
    The drain task is independent (tokio::spawn) so credit grants keep flowing regardless of how fast
    the consumer reads — this is what keeps PTY input responsive under channel saturation (MUX-02/MUX-03).
    Australian English comments; use variant_name() for any frame logging.
  </action>
  <verify>
    <automated>cargo build -p nosh-client 2>&1 | tail -20; cargo test -p nosh-client --lib 2>&1 | tail -20</automated>
  </verify>
  <done>channel.rs compiles and is declared in lib.rs; even-id allocator yields 2,4,6...; run_channel_task drains, grants ChannelCredit via control_tx, half-closes on EOF, and never writes the control stream directly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → client control stream | Untrusted ChannelAccept/Reject/Credit/Close frames cross here |
| server → client channel stream | Untrusted channel payload (echoed bytes in the test fixture) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-10 | Crash | ChannelAccept/Reject for unknown id | mitigate | await_channel_accept bails with variant_name (no Debug, no panic); client channel-map handlers log + continue (MUX-04) |
| T-21-11 | Denial of Service | client stalls server via withheld credit | accept | Self-inflicted only — the client withholding its own ChannelCredit stalls only its own channel; the server-side 256 KiB cap (plan 21-02) bounds server memory regardless |
| T-21-12 | Tampering | control-stream framing corruption from two writers | mitigate | Channel task sends control frames via control_tx mpsc; the session loop is the single control-stream writer (Pitfall 6 / A4) |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `cargo build -p nosh-client` clean
- `cargo test -p nosh-client --lib` green
- No `write_message` on the control SendStream appears inside channel.rs (credit/close go via control_tx)
</verification>

<success_criteria>
The client can open a channel control-first (ACCEPT awaited before stream bind), allocates even
ids, writes the channel-id varint prefix, drains channel data in an independent task while granting
ChannelCredit, and exposes the helpers needed for the MUX-05 re-open-after-ReattachOk path (proven
in plan 21-04). MUX-01/02/04 client halves and MUX-03 credit-granting are implemented.
</success_criteria>

<output>
Create `.planning/phases/21-channel-multiplexing-foundation/21-03-SUMMARY.md` when done
</output>
