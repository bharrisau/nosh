---
phase: 19-full-screen-tui-rendering-correctness
plan: 05
type: execute
wave: 4
depends_on: ["19-01", "19-02", "19-03", "19-04"]
files_modified:
  - crates/nosh-server/src/terminal.rs
  - docs/999.7-SECURITY.md
autonomous: false
requirements: [TUI-04, SEC-03]
must_haves:
  truths:
    - "A synthetic VT grid-assertion test suite in CI exercises alt-screen enter/exit atomicity, wide-char + ZWJ handling, and cursor addressing as a deterministic regression net"
    - "docs/999.7-SECURITY.md documents the OSC-OOM mitigation (1 MiB pre-bound + parser resync) before the phase closes"
    - "A documented manual visual pass against a reference terminal confirms vim, htop, and Claude Code render correctly over a Linux clientÃ¢ÂÂserver (investigation-first reproduction recorded)"
  artifacts:
    - path: "docs/999.7-SECURITY.md"
      provides: "999.7 OSC-OOM mitigation security note"
      contains: "OSC"
    - path: "crates/nosh-server/src/terminal.rs"
      provides: "Synthetic VT grid-assertion regression suite covering cursor-addressing sequences"
      contains: "fn "
  key_links:
    - from: "CI test suite"
      to: "alt-screen / wide-char / cursor-addressing behaviours"
      via: "deterministic VT byte sequences"
      pattern: "advance\\("
---

<objective>
Establish the TUI-04 acceptance net (D-19-07/08): a synthetic VT grid-assertion regression suite in CI plus a documented manual visual pass against a reference terminal for the three named live apps, and create `docs/999.7-SECURITY.md` documenting the OSC-OOM mitigation before the phase closes.

Purpose: The roadmap requires full-screen apps to render correctly with no garbling or missing spaces, verified investigation-first against a Linux clientÃ¢ÂÂserver. Automated golden-master capture against live apps is too flaky (D-19-07), so CI gets deterministic grid-assertion tests and a human verifies the live apps. The 999.7 security doc is a hard phase-exit requirement.
Output: a cursor-addressing-focused grid-assertion test module in terminal.rs, `docs/999.7-SECURITY.md`, and a human-verify checkpoint capturing the manual visual pass result.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-full-screen-tui-rendering-correctness/19-CONTEXT.md
@.planning/phases/19-full-screen-tui-rendering-correctness/19-RESEARCH.md
@.planning/phases/19-full-screen-tui-rendering-correctness/19-PATTERNS.md
@docs/999.1-SECURITY.md

<interfaces>
<!-- Source: 19-PATTERNS.md (verified). Do NOT re-explore. -->

Test harness (terminal.rs ~882): `fn ts(cols,rows) -> TerminalState`. Grid-assertion pattern: `state.advance(bytes)` then assert via `state.cell(row,col)` (.ch/.wide/.fg/.bg/.style), `state.cursor()`, `state.echo_state()`. Module test-isolation invariant (doc ~33-35): no quinn/tokio/session/registry/server imports.

CSI cursor addressing already supported in csi_dispatch: CUP (`ESC[<row>;<col>H`), ED/EL erases, SGR. These are the building blocks for the cursor-addressing grid-assertion tests.

docs/999.1-SECURITY.md is the format model for the new 999.7 doc (security note, ÃÂ§-numbered, threat + mitigation + test reference). Australian English spelling in docs (project convention).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Synthetic VT grid-assertion regression suite for cursor addressing</name>
  <files>crates/nosh-server/src/terminal.rs</files>
  <read_first>
    - crates/nosh-server/src/terminal.rs (ts() ~882, grid-assertion test patterns ~914-922, existing alt-screen/wide/OSC tests added in plans 01-03, csi_dispatch CUP/ED/EL/SGR)
    - .planning/phases/19-full-screen-tui-rendering-correctness/19-CONTEXT.md (D-19-07 verification bar)
    - .planning/phases/19-full-screen-tui-rendering-correctness/19-PATTERNS.md (Test helper pattern; test isolation invariant)
  </read_first>
  <action>
    Add a focused grid-assertion test group in terminal.rs (using the `ts()` harness) that exercises the cursor-addressing path representative of real TUI rendering, complementing the per-feature tests from plans 01-03 (do NOT duplicate those Ã¢ÂÂ reference them). Cover: (a) CUP-driven absolute positioning then printing lands glyphs at the addressed cells; (b) an alt-screen enter Ã¢ÂÂ CUP-addressed full-screen paint Ã¢ÂÂ exit round trip leaves the primary grid/cursor exactly as before (atomicity at the integration level, not just the unit toggle); (c) a wide char written via CUP positioning advances correctly and the continuation cell is present; (d) erase-in-display / erase-in-line after positioning clears the expected region. Keep everything deterministic (no live apps, no timing Ã¢ÂÂ D-19-07). Maintain the module test-isolation invariant.
  </action>
  <verify>
    <automated>cargo test -p nosh-server --lib terminal:: 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - New tests assert grid cell contents after CUP-addressed writes, alt-screen paint round trip, wide-char-via-CUP, and ED/EL clears.
    - Tests are deterministic (no sleeps, no network, no live-app invocation).
    - `cargo test -p nosh-server --lib` passes the full terminal:: suite.
  </acceptance_criteria>
  <done>CI has a deterministic synthetic VT grid-assertion regression net covering cursor addressing, alt-screen atomicity, and wide chars (D-19-07 automated half of TUI-04).</done>
</task>

<task type="auto">
  <name>Task 2: Create docs/999.7-SECURITY.md for the OSC-OOM mitigation</name>
  <files>docs/999.7-SECURITY.md</files>
  <read_first>
    - docs/999.1-SECURITY.md (ÃÂ§7 OSC-OOM analysis + doc format)
    - crates/nosh-server/src/terminal.rs (OSC_ACCUMULATION_MAX, the advance() pre-filter from plan 03)
    - .planning/phases/19-full-screen-tui-rendering-correctness/19-CONTEXT.md (D-19-01/02/03; canonical_refs note that 999.7 must exist before phase close)
  </read_first>
  <action>
    Create `docs/999.7-SECURITY.md` documenting the OSC-OOM mitigation shipped in this phase: the threat (vte 0.15 `feature = "std"` makes `osc_raw` an unbounded `Vec<u8>` that accumulates across `advance()` calls; the multi-chunk giant-OSC attack vector exhausts server memory before the `osc_dispatch` storage caps run; post-authentication trusted-but-buggy/hostile app output); the mitigation (1 MiB `OSC_ACCUMULATION_MAX` pre-bound in `TerminalState::advance` BEFORE `parser.advance()`; on overflow truncate-and-discard the oversized OSC and resync the parser to ground state via `vte::Parser::default()`; existing `OSC_52_MAX_BYTES`/`MAX_TITLE_BYTES` storage caps retained); the residual risk / rationale (do not kill the session, do not drop-and-keep-parsing Ã¢ÂÂ D-19-02); and the regression test reference (the RED-before/GREEN-after multi-chunk test + extended fuzz target from plan 03). Mirror the ÃÂ§-numbered format of 999.1-SECURITY.md. Use Australian English. End the doc with a short "verify before relying on this" note (memory-bound figure and resync behaviour should be re-confirmed if vte is upgraded).
  </action>
  <verify>
    <automated>test -f docs/999.7-SECURITY.md && grep -qi 'OSC_ACCUMULATION_MAX\|1 MiB\|osc' docs/999.7-SECURITY.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/999.7-SECURITY.md` exists.
    - It names the threat (unbounded vte osc_raw, multi-chunk giant OSC, post-auth), the mitigation (1 MiB pre-bound + parser resync), and references the plan-03 regression test/fuzz target.
    - Australian English spelling; ÃÂ§-numbered format consistent with 999.1.
  </acceptance_criteria>
  <done>The 999.7 OSC-OOM mitigation is documented before phase close, satisfying the ROADMAP phase-exit requirement.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual visual pass â vim, htop, Claude Code over a Linux clientâserver</name>
  <what-built>Two-grid alt-screen, width-accurate print_char, predictor suppression in alt-screen, and the OSC OOM bound Ã¢ÂÂ all server/client-side. Automated CI now has grid-assertion tests, but TUI-04 requires a live visual pass that cannot be automated (D-19-07/08).</what-built>
  <how-to-verify>
    Investigation-first (D-19-08): if not already done, first reproduce the original garbling/missing-spaces against a Linux clientÃ¢ÂÂserver on the pre-fix code or recall the recorded repro, then verify the fix.
    1. Build release: `cargo build --release`.
    2. Start the nosh server and connect a Linux nosh client to it (per the project's run instructions / existing dev workflow).
    3. Run `vim --noplugin` Ã¢ÂÂ confirm it opens to a blank canvas (no shell text bleeding through), edit a few lines, `:q` Ã¢ÂÂ confirm the shell prompt and prior scrollback are exactly where they were before vim (primary buffer + cursor restored).
    4. Run `htop` Ã¢ÂÂ confirm columns, bars, and CPU meters render aligned against a reference terminal (e.g. the same htop in a local terminal); no garbled output, no missing spaces.
    5. Run `claude` (Claude Code) or another full-screen TUI Ã¢ÂÂ confirm correct rendering and that no speculative-echo overlay flickers while typing inside it.
    6. Type CJK text (e.g. paste "Ã¤Â¸Â­Ã¦ÂÂÃ¦ÂµÂÃ¨Â¯Â") at the shell and confirm no column drift; type an emoji ZWJ sequence and confirm the cursor advances correctly.
    Record the comparison result (pass / specific defects) in the SUMMARY.
  </how-to-verify>
  <resume-signal>Type "approved" if vim/htop/Claude Code render correctly with no garbling, or describe the specific rendering defects observed.</resume-signal>
  <action>Operator-only: this step cannot be automated (live-app visual comparison per D-19-07). Execute the how-to-verify steps and record the outcome in the SUMMARY.</action>
  <verify>
    <human-check>vim opens to a blank canvas and restores the primary buffer/cursor on exit; htop and Claude Code render aligned against a reference terminal with no garbling or missing spaces; no predictor overlay inside alt-screen apps; CJK/emoji cause no column drift.</human-check>
  </verify>
  <done>Operator has confirmed ("approved") that vim, htop, and Claude Code render correctly over a Linux client↔server, or logged specific defects for gap closure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PTY output Ã¢ÂÂ TerminalState | This plan is verification + documentation; the live trust boundary is the OSC-OOM vector documented in 999.7 and mitigated in plan 03 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-06 | Denial of Service | OSC-OOM (documented here, mitigated in plan 03) | mitigate | docs/999.7-SECURITY.md records the 1 MiB pre-bound + resync; regression test in plan 03 is the binding gate; this plan satisfies the ROADMAP "999.7 doc before phase close" exit requirement |
</threat_model>

<verification>
- `cargo test -p nosh-server --lib` passes the full terminal:: suite including the new cursor-addressing grid-assertion tests.
- `docs/999.7-SECURITY.md` exists and documents the mitigation.
- Human-verify checkpoint approved (vim/htop/Claude Code render correctly; CJK no drift; no overlay in alt-screen).
</verification>

<success_criteria>
ROADMAP criterion 4 is met: full-screen TUI apps (vim, htop, Claude Code) render correctly over nosh with no garbling and no missing spaces, verified against a reference terminal on a Linux clientÃ¢ÂÂserver (investigation-first), backed by a deterministic CI grid-assertion suite. The 999.7 OSC-OOM mitigation is documented before phase close.
</success_criteria>

<output>
Create `.planning/phases/19-full-screen-tui-rendering-correctness/19-05-SUMMARY.md` when done.
</output>
