---
context: research
phase: none (milestone v1.1 setup)
task: 6
total_tasks: 8
status: paused
last_updated: 2026-05-29T12:25:20.506Z
---

# Resume: nosh v1.1 (M3 Roaming + Windows Client) — research deferred

<current_state>
Mid `/gsd:new-milestone` for **v1.1 M3 Roaming + Windows Client**. Milestone setup is done and committed; the workflow is paused right before the **research** step (which the user chose to defer and trigger later). Next is research → requirements → roadmap.

No active phase directory (v1.0 phases archived, phase dirs cleared). Handoff lives at the project-research/default path.
</current_state>

<completed_work>

- Task 1: Scope v1.1 via questioning — Done
- Task 2: Granularity coarse → balanced (config.json) — Done (commit a9961f5)
- Task 3: PROJECT.md (Current Milestone v1.1 + Active reqs + Out of Scope reconciled), STATE.md reset via SDK, phase dirs cleared — Done (commit a9961f5)
- Task 4: Archived v1.0 research → `.planning/milestones/v1.0-research/` — Done
- Task 5: Staged full research brief → `.planning/RESEARCH-BRIEF-v1.1.md` — Done (commit f3608f2)
</completed_work>

<remaining_work>

- Task 6 (NEXT): Run v1.1 research — spawn 4 `gsd:gsd-project-researcher` (Stack/Features/Architecture/Pitfalls) in parallel, then `gsd:gsd-research-synthesizer` → `.planning/research/SUMMARY.md`. Full prompts in `.planning/RESEARCH-BRIEF-v1.1.md`.
- Task 7: Define `.planning/REQUIREMENTS.md` (REQ-IDs, scoped per category) from research + locked scope decisions below.
- Task 8: Spawn `gsd:gsd-roadmapper` → `.planning/ROADMAP.md`. **Continue** phase numbering: v1.0 ended at phase 3, so v1.1 starts at **phase 4** (`--reset-phase-numbers` was NOT requested).
</remaining_work>

<decisions_made>

- v1.1 = M3 roaming on Linux + a **bounded Windows-client slice**; native Windows server (ConPTY) stays M6.
- Session persistence = **Mosh-style** (survive until shell exits); configurable idle timeout **default 0 = disabled**; per-identity cap bounds memory.
- Cold reattach is **1-RTT**, authorization **bound to SSH identity**; 0-RTT stays deferred.
- Windows client signs from an **on-disk OpenSSH key file** (agent integration deferred) — temporary Windows-only exception to "never handle the private key"; Linux ssh-agent path untouched.
- Roaming validated **headless via forced path change**; real Wi-Fi→cellular as a human-verified live check.
- Granularity → **balanced** for v1.1.
</decisions_made>

<blockers>
- None.
</blockers>

## Required Reading (in order)
1. `.planning/RESEARCH-BRIEF-v1.1.md` — the exact research prompts to run next, and the one-liner trigger.
2. `.planning/PROJECT.md` — Current Milestone v1.1 section + Key Decisions.
3. `.planning/MILESTONES.md` — what v1.0 shipped + known-by-design M3 seams (Session.identity, reattach, datagram traffic).
4. `INIT.md` — authoritative design brief (§10 milestone path; migration/reattach/topology rationale).

## Critical Anti-Patterns (do NOT repeat these)
- [ANTI-PATTERN]: Calling GSD subagents by bare name (e.g. `gsd-project-researcher`) → after `/reload-plugins` the agents are registered under the **`gsd:` namespace**. Always use `gsd:gsd-project-researcher`, `gsd:gsd-research-synthesizer`, `gsd:gsd-roadmapper`. (Discovered via "agent type not found" failure this session.)

## Infrastructure State
- No running services/processes. Working tree clean after WIP commit. Branch: main.

<context>
The user wants to fire research later with a single instruction rather than now. Everything is staged so a fresh session can run it cleanly. After research+synthesis, resume the standard new-milestone flow at step 9 (requirements) then step 10 (roadmap, continuing phase numbering at 4).
</context>

<next_action>
Start with: "Run the v1.1 research from `.planning/RESEARCH-BRIEF-v1.1.md`" — spawn the 4 `gsd:`-prefixed researchers in parallel, wait, then run the synthesizer; then proceed to requirements → roadmap.
</next_action>
