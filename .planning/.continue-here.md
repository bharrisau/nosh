---
context: autonomous-build
milestone: v1.1 (M3 Roaming + Windows Client)
position: Phases 4-6 complete; Phases 7 & 8 planned, need execute only
status: paused
last_updated: 2026-05-30
---

# Resume: nosh v1.1 (M3) — paused after Phase 6 fix

## Current state
Running `/gsd:autonomous --interactive`. **All 5 phases (4-8) are DISCUSSED** (CONTEXT.md committed for each). Phases 4, 5, 6 are **executed, opus-re-verified, and remediated** (each had a real bug the sonnet inline verify missed; all fixed). Phases 7 & 8 are **PLANNED** (PLAN.md files on disk) and only need **execute**.

- `cargo test --workspace`: **63 passed, 0 failed, 3 ignored** (ignored = live ssh-agent + 60s idle). `cargo clippy --workspace --all-targets -- -D warnings`: clean.
- Progress: 3/5 phases, 60%.

## Completed (committed)
- **Phase 4 Identity Threading** ✓ — `Session.identity` non-optional; SHA256 fingerprint (verified vs `ssh-keygen`).
- **Phase 5 Session Persistence** ✓ — SessionRegistry/SessionSlot/SequencedOutputBuffer, orphan-on-transport-loss (no SIGHUP), LRU cap, idle-timeout (`--idle-timeout-secs`/`NOSH_IDLE_TIMEOUT_SECS`, default 0), event-driven orphan-exit removal.
- **Phase 6 Cold Reattach** ✓ — 5 new Message variants, two-factor reattach (no-oracle), single-use rotated token, continuous-ack replay (next-expected-seq), Reconnecting-state mutual exclusion, client indefinite-retry reconnect supervisor. Fixes: d4fd933, 846e472, 75409e6, 99dca90.

## NEXT (do this on resume)
Run `/gsd:resume-work` then continue the autonomous build. Remaining build order (each in a background agent; **serialize 7 then 8** — both touch `crates/nosh-client/` + Cargo.toml + run cargo):

1. **Execute Phase 7** (Connection Migration Validation). Plans: `.planning/phases/07-connection-migration-validation/07-01-PLAN.md`, `07-02-PLAN.md` (2 plans, 5 tasks, 2 waves). Then opus-re-verify (verify now resolves to opus automatically).
2. **Execute Phase 8** (Windows Client). Plans: `08-01`, `08-02` (2 plans, 10 tasks). Then opus-re-verify.
3. **Milestone lifecycle**: `/gsd:audit-milestone` → `/gsd:complete-milestone v1.1` → `/gsd:cleanup`.

No more discuss/plan needed for 7 or 8 — go straight to execute.

## Model rules (LOCKED this session — verify them via `gsd-sdk query resolve-model <agent>`)
- `model_overrides` in `.planning/config.json` now pins `gsd-verifier` + `gsd-integration-checker` → **opus**. So a verify pass resolves to opus on its own.
- Planner = opus; executor/researcher/checker = sonnet (balanced profile). Resolve per-agent; do NOT blanket-override.
- **Background/Workflow agents can't spawn subagents (one-level nesting).** So when wrapping a GSD skill in an Agent, the skill does the work INLINE on the wrapper's model — set the wrapper to the skill's primary-role tier (plan→opus, execute→sonnet). And run **verify as its OWN agent** (not collapsed inside a wrapped execute-phase).
- Make the verifier **adversarial**: have it write a probe test that fails-before/passes-after and trust the code over the executor's summary. (Three phases' bugs were all weak-test false-confidence: shape-only / bypassed-real-path / hardcoded-ack+fuzzy-substring.)
- GSD subagents need the `gsd:` namespace prefix.

## CARRY-FORWARD — open items to address (not yet fixed)
1. **Reader-zombie race (pre-existing, surfaced in Phase 6 W2 fix):** `tokio::task::spawn_blocking` + `abort()` does NOT interrupt a blocked synchronous `read()` on the PTY, so an old PTY reader task can keep consuming/"stealing" output bytes after teardown/reattach. Out of Phase-6 scope, **NOT fixed**. The byte-exact reattach test passes today (latent under timing), but it could affect reattach correctness — investigate next session (good candidate for an opus look, or a small dedicated phase/insert).
2. **Phase 6 W1 token-lifecycle (commit 846e472):** the fixer flagged the `mint_token_candidate`/`commit_token` rotation timing as wanting a human/verify confirmation under the send-failure path. The opus verifier (now default) should re-check it.

## Pending human checks (NON-blocking — autonomous completes without them)
- **Phase 7:** real Wi-Fi→cellular live check (procedure doc will be at `docs/migration-live-check.md`). Phase 7 is marked `human_needed`.
- **Phase 8:** real Windows interactive session (raw mode, resize, on-disk-key auth → Linux server). Phase 8 marked `human_needed`. Automated gate is `cargo check --target x86_64-pc-windows-gnu` (CI workflow added by the plan; needs `rustup target add x86_64-pc-windows-gnu`, no mingw).

## Key locked decisions (for reference; full detail in each phase's CONTEXT.md)
- Reattach = in-memory (designed-for-disk), single-use rotated token, indefinite auto-retry, continuous acks.
- Migration validated headless via `Endpoint::rebind()`; CID-rotation asserted via `Connection::stats()` FrameStats (quinn 0.11.9 qlog can't expose PATH_CHALLENGE/CIDs — qlog artifact still produced).
- Windows = client only; `--identity-file` FileSigner (cross-platform, opt-in; only auth path on Windows); ssh-agent stays Linux default; encrypted keys → error (no prompt, P2); crossterm 0.28→0.29.
