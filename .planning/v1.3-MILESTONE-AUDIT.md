---
milestone: v1.3
audited: 2026-06-12T00:00:00Z
status: passed
scores:
  requirements: 19/19
  phases: 4/4
  integration: 19/19
  flows: 5/5
gaps:
  requirements: []
  integration: []
  flows: []
gaps_found_and_closed_during_audit:
  - id: "SCROLL-01 (display half)"
    found_by: "phase-22 opus verifier (re-verification)"
    issue: "Client buffered scrollback pages but never rendered them (TODO at main.rs:1802)."
    closed_by: "Gap closure 22-05 — render_scrollback_to_buf + 5 wire-up points + 7 unit tests."
  - id: "SCROLL-01 / SCROLL-02 (deep history) + MUX-03 (scrollback channel)"
    found_by: "milestone integration checker (opus)"
    issue: "Server lumped ScrollbackCredit into the control-stream ignore arm; credit never reached the sender, so paging past INITIAL_CREDIT (256 KiB) stalled and closed the channel."
    closed_by: "Gap closure 22-06 — route ScrollbackCredit→ChannelEvent::Credit in run_session + run_reattach_session; added scrollback_deep_paging_replenishes_credit regression test (RED-without-fix, GREEN-with-fix confirmed)."
tech_debt:
  - phase: 19-full-screen-tui-rendering-correctness
    items:
      - "Phase 19 VERIFICATION status is human_needed: green Windows CI + live Windows re-test remain an operator TODO. Not a Linux blocker (the spike milestone is Linux-focused); carried forward."
  - phase: 22-scrollback-sync
    items:
      - "IN-P-01: ScrollbackLine.width carries post-resize column width, not original live width (S-3 metadata limitation in the Vec<Cell> representation). Pre-existing; left as-is per review."
nyquist:
  overall: not_applicable
  note: "workflow.nyquist_validation disabled for this project."
---

# Milestone v1.3 (M5) — Audit

**Channel Multiplexing, Scrollback Sync & TUI Rendering Correctness**

## Verdict: PASSED

All 19 milestone requirements (TUI-01..05, PACE-01..03, MUX-01..06, SCROLL-01..05) are
satisfied and wired end-to-end across phases 19–22. Two genuine blockers were surfaced by
independent verification during this audit cycle and both were closed and regression-tested
before completion (see `gaps_found_and_closed_during_audit`).

## Phase verifications

| Phase | Status | Score |
|-------|--------|-------|
| 19 Full-Screen TUI Rendering Correctness | human_needed | 6/6 must-haves (Windows re-test pending — operator TODO) |
| 20 Repaint Pacing | passed | 9/9 |
| 21 Channel Multiplexing Foundation | passed | 6/6 |
| 22 Scrollback Sync | passed | 5/5 |

## Requirements coverage (3-source: REQUIREMENTS.md traceability + VERIFICATION + SUMMARY)

All 19 REQ-IDs marked `[x]` / Complete in the traceability table, each backed by a passing
phase VERIFICATION. SCROLL-01/02 and MUX-03 (scrollback channel) were PARTIAL at the
integration checkpoint due to the credit-routing bug; now SATISFIED after gap closure 22-06.

## Cross-phase integration (5 seams)

1. Scrollback channel rides the Phase 21 mux (OPEN/ACCEPT + per-channel credit) — WIRED (credit half fixed in 22-06).
2. Phase 19 `!alt_screen` gate feeds Phase 22 scrollback exclusion (same `TerminalState`) — WIRED.
3. Phase 22 `epoch_at_snapshot` uses Phase 20's `current_epoch` (read under the same `with_terminal_state` lock, TOCTOU-free) — WIRED.
4. Cold-reattach re-opens the scrollback channel via Phase 21's re-establish path (channels re-opened, not byte-replayed) — WIRED.
5. Repaint pacing and scrollback rendering coexist (scrollback render bypasses the predictor; live grid full-repaints on snap-back) — WIRED.

## Tests

`cargo build --workspace` clean; `cargo test --workspace` green (no failures). Note:
`channel_echo_roundtrip` is a known load-sensitive PTY-latency timing flake (passes in
isolation); not a regression.
