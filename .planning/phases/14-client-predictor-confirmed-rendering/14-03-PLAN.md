---
phase: 14-client-predictor-confirmed-rendering
plan: 03
type: execute
wave: 3
depends_on: [14-01, 14-02]
files_modified:
  - crates/nosh-client/tests/render.rs
autonomous: true
requirements: [PREDICT-01]

must_haves:
  truths:
    - "A datagram-rendered ClientScreen confirmed grid matches the server's TerminalState visible characters for the same byte stream (PREDICT-01 success criterion #2)"
    - "An end-to-end test connects a real client to a real server, types input, receives StateDiff datagrams, applies them, and asserts the typed text appears in the confirmed grid"
    - "highest_applied still advances from PtyData over the reliable stream while datagrams drive display (cold-reattach Ack not regressed, D-14-03)"
  artifacts:
    - path: "crates/nosh-client/tests/render.rs"
      provides: "grid-comparison unit test + end-to-end datagram render integration test"
      min_lines: 80
      contains: "ClientScreen"
  key_links:
    - from: "crates/nosh-client/tests/render.rs"
      to: "nosh_server::terminal::TerminalState"
      via: "dev-dependency import; drive both with same bytes, compare grids"
      pattern: "TerminalState::new"
    - from: "crates/nosh-client/tests/render.rs"
      to: "conn.read_datagram()"
      via: "integration loop applying real server datagrams to ClientScreen"
      pattern: "read_datagram"
---

<objective>
Prove PREDICT-01 success criterion #2 end-to-end: the screen rendered from datagrams matches raw PTY output's visible characters. Add `crates/nosh-client/tests/render.rs` with (a) a pure grid-comparison test driving a server `TerminalState` and a `ClientScreen` with the same byte stream and asserting grid equality, and (b) a live integration test connecting a real client/server, typing input, and asserting the typed text lands in the confirmed grid from received datagrams.

Purpose: This is the phase's verification gate — it closes the loop that the datagram display path actually reproduces server terminal state. Implements the locked roadmap item "end-to-end test: datagram-rendered screen matches the raw PTY output's visible characters."
Output: `crates/nosh-client/tests/render.rs`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-client-predictor-confirmed-rendering/14-CONTEXT.md
@.planning/phases/14-client-predictor-confirmed-rendering/14-RESEARCH.md
@.planning/phases/14-client-predictor-confirmed-rendering/14-PATTERNS.md

<interfaces>
<!-- Test harness (crates/nosh-client/tests/common/mod.rs + sync.rs patterns) -->
- `mod common; use common::{spawn_server_with_registry, TestKey, HOST};`
- `TestKey::generate() -> TestKey`; `key.public`; `key.client_identity()`
- `common::client_endpoint(identity, known_hosts_path) -> anyhow::Result<quinn::Endpoint>`
- `common::spawn_server_with_registry(&host_key, &[&pub], AuthLimits, Some(shell), registry) -> TestServer { addr, .. }`

<!-- client.rs public API -->
- `client::connect(&ep, addr, HOST, Duration) -> anyhow::Result<quinn::Connection>` (main.rs:187 / client.rs)
- `client::open_session(&conn, term: String, cols: u16, rows: u16, env: Vec<..>) -> anyhow::Result<(SendStream, RecvStream)>` (client.rs:587)
- `client::send_input(&mut send, bytes: &[u8]) -> anyhow::Result<()>` (client.rs:610)

<!-- nosh-server (dev-dependency only) -->
- `nosh_server::terminal::TerminalState::new(cols, rows)`; `.advance(&[u8])`; `.cell(row, col) -> &Cell`; `.cursor() -> CursorPos`; `.viewport_rows() -> impl Iterator<Item=(u16, &[Cell])>`; `.size() -> (u16,u16)`
- `nosh_server::registry::SessionRegistry::new(cap, idle_timeout)`
- `nosh_server::server::AuthLimits::default()`

<!-- ClientScreen (Plan 01) + datagram decode -->
- `nosh_client::screen::ClientScreen::new/apply/confirmed_cell/last_applied_epoch`
- `nosh_proto::datagram::{decode_datagram, StateDiff, DiffRun, CursorPos, CellStyle}`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pure grid-comparison test — ClientScreen confirmed grid matches server TerminalState</name>
  <files>crates/nosh-client/tests/render.rs</files>
  <read_first>
    - crates/nosh-client/tests/sync.rs lines 12-52 (imports, have_sh, server_with_key, client_endpoint_for helpers to reuse), 62-125 (datagram receive loop pattern)
    - crates/nosh-server/src/terminal.rs lines 59-81 (Cell fields), 206 (advance), 327-389 (cursor/cell/viewport_rows/size read API)
    - crates/nosh-client/src/screen.rs (ClientScreen::new/apply/confirmed_cell from Plan 01)
    - .planning/phases/14-client-predictor-confirmed-rendering/14-PATTERNS.md lines 464-575 (test header, server spawn, pure grid-comparison test building a StateDiff from a TerminalState viewport), 642-677 (idempotent render test)
    - .planning/phases/14-client-predictor-confirmed-rendering/14-RESEARCH.md lines 564-605 (grid-comparison strategy), 688-704 (Open Question 1 — build StateDiff via viewport_rows, avoid SessionSlot)
  </read_first>
  <action>
Create `crates/nosh-client/tests/render.rs`. Module doc: "Phase 14 render tests — confirmed screen matches server TerminalState (PREDICT-01)."

Imports per the interfaces block: `nosh_client::screen::ClientScreen`, `nosh_proto::datagram::{decode_datagram, CellStyle, CursorPos, DiffRun, StateDiff}`, `nosh_server::terminal::TerminalState` (dev-dependency — available in tests), and `mod common;` with the `spawn_server_with_registry, TestKey, HOST` helpers. Copy the `have_sh`, `server_with_key`, and `client_endpoint_for` helpers from sync.rs lines 25-52 (or factor into common if trivially shared — executor discretion; copying is acceptable for a test file).

Add `#[test] fn confirmed_grid_matches_terminal_state_after_diff()`: build `let mut server_ts = TerminalState::new(80, 24); server_ts.advance(b"hello world\r\nline two\r\n");`. Build a full-repaint `StateDiff` (epoch 1, baseline empty) from the server viewport: iterate `server_ts.viewport_rows()`, and for each row coalesce consecutive non-default cells (same style/fg/bg) into `DiffRun { row, start_col, style, fg, bg, chars }` (per PATTERNS lines 528-556 — skip cells equal to the default blank). Assemble `StateDiff { epoch: 1, cols: 80, rows: 24, cursor: CursorPos { row: server_ts.cursor().row, col: server_ts.cursor().col }, runs }`. Round-trip through the wire to exercise the real decode path: `let bytes = nosh_proto::datagram::encode_datagram(&diff /* or per encode signature */); let decoded = decode_datagram(&bytes).unwrap();` (consult encode_datagram signature at datagram.rs:252 — if it takes individual fields rather than a StateDiff, build the args accordingly; the goal is a real encode→decode round-trip before apply). Apply to `let mut screen = ClientScreen::new(80, 24); screen.apply(&decoded);`. Then assert cell-by-cell over all 80×24 that `screen.confirmed_cell(row, col).ch == server_ts.cell(row, col).ch` AND the fg/bg/style match — the full-grid equality assertion is the load-bearing PREDICT-01 check, not just spot-checks of `'h'`/`'e'`.

Add `#[test] fn duplicate_datagram_is_idempotent()` (pure, no QUIC): apply a content diff at epoch 1, render to `buf1: Vec<u8>`, then apply the same diff again (epoch 1 ≤ 1 → discarded), render to `buf2`, assert `buf2.len() < buf1.len()` and that `buf2` contains no cell-content chars (PATTERNS lines 642-677).
  </action>
  <verify>
    <automated>cargo test -p nosh-client --test render confirmed_grid_matches_terminal_state_after_diff duplicate_datagram_is_idempotent 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p nosh-client --test render confirmed_grid_matches_terminal_state_after_diff` exits 0.
    - `cargo test -p nosh-client --test render duplicate_datagram_is_idempotent` exits 0.
    - `grep -n "TerminalState::new" crates/nosh-client/tests/render.rs` matches (server model driven in-test).
    - `grep -n "decode_datagram" crates/nosh-client/tests/render.rs` matches (real wire round-trip exercised).
    - The grid-comparison test asserts over the full 80×24 grid (a `for row in 0..24 { for col in 0..80 {` loop with `assert_eq!` on ch + fg + bg + style), not just a few cells.
  </acceptance_criteria>
  <done>A pure test drives both server TerminalState and ClientScreen with the same bytes via a real encode/decode round-trip and asserts full-grid equality; an idempotency test confirms duplicate datagrams emit no new cell content.</done>
</task>

<task type="auto">
  <name>Task 2: End-to-end integration test — typed text appears in confirmed grid from live datagrams</name>
  <files>crates/nosh-client/tests/render.rs</files>
  <read_first>
    - crates/nosh-client/tests/sync.rs lines 62-125 (full integration test: registry, server_with_key, connect, open_session, discard SessionOpened, send_input, conn.read_datagram() loop with timeout, teardown)
    - crates/nosh-client/src/client.rs lines 187 (connect), 587 (open_session), 610 (send_input) signatures
    - .planning/phases/14-client-predictor-confirmed-rendering/14-PATTERNS.md lines 577-639 (integration datagram test template)
    - .planning/phases/13-server-datagram-sender/13-SUMMARY.md (server emits datagrams ~16ms after PTY output; ResumeComplete gate)
  </read_first>
  <action>
Add `#[tokio::test] async fn render_integration_client_screen_matches_server_output()`. Guard with `if !have_sh() { return; }`.

Follow the sync.rs integration pattern exactly (lines 62-125): create `let registry = SessionRegistry::new(5, Duration::from_secs(30));`, `let client_key = TestKey::generate();`, `let server = server_with_key(registry.clone(), &client_key).await;`, build the client endpoint via `client_endpoint_for(&client_key)`, `let conn = client::connect(&ep, server.addr, HOST, Duration::from_secs(30)).await.expect("connect");`, `let (mut send, mut recv) = client::open_session(&conn, "xterm".into(), 80, 24, vec![]).await.expect("open_session");`. Discard the first `SessionOpened` with `let _ = nosh_proto::read_message(&mut recv).await;`. Type input: `client::send_input(&mut send, b"echo hello\n").await.expect("send_input");`.

Build `let mut screen = ClientScreen::new(80, 24);`. Loop receiving datagrams with a per-iteration timeout (e.g. `tokio::time::timeout(Duration::from_secs(5), conn.read_datagram())`); on each `Ok(Ok(bytes))`, `if let Ok(diff) = decode_datagram(&bytes) { screen.apply(&diff); }`, then scan the confirmed grid for the substring "hello" appearing contiguously in some row (collect each row's `confirmed_cell(r, c).ch` into a String and check `row_str.contains("hello")`); break when found. On `Ok(Err(e))` panic with the connection error; on `Err(_)` (timeout) panic "timed out waiting for 'hello' in confirmed grid". This asserts the live datagram path renders typed text into the confirmed grid — PREDICT-01 end-to-end.

To also guard D-14-03 (highest_applied / reattach Ack not regressed via the new display path): the integration test naturally exercises the reliable stream (open_session uses it); if a lightweight assertion is feasible without restructuring run_pump, note that the reliable stream still delivers PtyData (the existing sync.rs reattach tests already cover highest_applied advancement, so a duplicate assertion here is optional — executor discretion). Tear down: `drop(send); drop(recv); conn.close(0u32.into(), b"done"); ep.close(0u32.into(), b"done");`.
  </action>
  <verify>
    <automated>cargo test -p nosh-client --test render render_integration_client_screen_matches_server_output 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p nosh-client --test render render_integration_client_screen_matches_server_output` exits 0 (or is cleanly skipped via `have_sh()` guard on a host without /bin/sh — on Linux CI it runs).
    - `cargo test -p nosh-client --test render` (whole file) exits 0.
    - `grep -n "read_datagram" crates/nosh-client/tests/render.rs` matches (live datagram loop).
    - `grep -n "send_input" crates/nosh-client/tests/render.rs` matches (real typed input).
    - The test scans for the contiguous substring "hello" in a confirmed-grid row (not just a single 'h' char) — `grep -n "contains(\"hello\")" crates/nosh-client/tests/render.rs` matches.
  </acceptance_criteria>
  <done>A live client↔server integration test types `echo hello`, receives real StateDiff datagrams, applies them to a ClientScreen, and asserts "hello" appears in the confirmed grid — closing the PREDICT-01 end-to-end loop.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness only | This plan adds tests; no production trust boundary is introduced. The tests exercise the server→client datagram boundary defended in Plans 01/02. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-09 | Tampering (verification) | datagram render correctness | mitigate | The grid-comparison test is the adversarial check that the datagram path reproduces server state exactly (full-grid equality, not spot-checks) — catches silent diff/apply corruption that an executor summary might miss. |
| T-14-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies (test reuses existing dev-deps: nosh-server, tempfile, tokio). No install task; no blocking checkpoint required. |
</threat_model>

<verification>
- `cargo test -p nosh-client --test render` exits 0 (all three tests pass on Linux; integration test guarded by have_sh()).
- The grid-comparison test asserts full-grid equality between server TerminalState and ClientScreen.
- The integration test drives the real client/server datagram path and finds typed text in the confirmed grid.
</verification>

<success_criteria>
- PREDICT-01 success criterion #2 is verified by an automated test: datagram-rendered confirmed grid matches the server's terminal state's visible characters.
- Idempotency and live end-to-end behavior both covered.
- `cargo test -p nosh-client` passes overall (no regression in sync.rs or other tests).
</success_criteria>

<output>
Create `.planning/phases/14-client-predictor-confirmed-rendering/14-03-SUMMARY.md` when done.
</output>
