---
phase: 21-channel-multiplexing-foundation
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - crates/nosh-server/src/server.rs
  - crates/nosh-server/src/channel.rs
  - crates/nosh-server/src/lib.rs
autonomous: true
requirements: [MUX-01, MUX-02, MUX-03, MUX-04]
must_haves:
  truths:
    - "A ChannelOpen on the control stream is answered with ChannelAccept or ChannelReject before any data stream is bound"
    - "Echo channels are accepted only under #[cfg(test)]; PortForward/AgentForward are always REJECTed; client-sent odd ids are ignored"
    - "Each accepted channel runs in its own tokio::spawn task; the accept_bi arm reads only the varint prefix and hands the stream pair to that task"
    - "A 256 KiB byte-credit window bounds per-channel sends; ChannelCredit replenishes it; the server never blocks the session pump on a slow channel"
    - "ChannelAccept/ChannelReject/ChannelCredit/ChannelClose for an unknown or already-closed id is a logged no-op, never a panic"
    - "The secondary accept_bi arm exists only inside run_session/run_reattach_session, after the pre-auth permit is dropped"
    - "Under #[cfg(test)] only, the server can allocate an ODD channel id from its own parity space and send a real ChannelOpen over the control stream; this path is compiled out of production builds"
  artifacts:
    - path: "crates/nosh-server/src/channel.rs"
      provides: "run_channel_task + ChannelEvent + read_varint_u32 helper + #[cfg(test)] server-initiated odd-id open helper"
      contains: "run_channel_task"
    - path: "crates/nosh-server/src/server.rs"
      provides: "channel_map, accept_bi arm, ChannelOpen/Credit/Close dispatch in run_session and run_reattach_session, #[cfg(test)] server-open trigger"
      contains: "ChannelOpen"
  key_links:
    - from: "crates/nosh-server/src/server.rs"
      to: "crates/nosh-server/src/channel.rs"
      via: "tokio::spawn(run_channel_task(...)) + mpsc::Sender<ChannelEvent> in channel_map"
      pattern: "run_channel_task"
    - from: "crates/nosh-server/src/server.rs accept_bi arm"
      to: "channel task"
      via: "read_varint_u32 then ChannelEvent::Stream over mpsc"
      pattern: "ChannelEvent::Stream"
    - from: "crates/nosh-server/src/server.rs #[cfg(test)] server-open trigger"
      to: "control stream"
      via: "allocate odd id + write_message(ChannelOpen) through control_tx (single-writer path)"
      pattern: "next_server_channel_id"
---

<objective>
Add the server side of the channel-mux layer: control-stream dispatch for the new mux Message
variants, a secondary `accept_bi` arm that binds incoming data streams to channels by their
varint channel-id prefix, a per-channel `tokio::spawn` task with a 256 KiB byte-credit window,
and clean lifecycle teardown. The accept loop and all channel I/O live strictly after
authentication and never block the session pump. A `#[cfg(test)]`-gated server-initiated open
path (odd-id allocation) exists purely so the integration suite can exercise a real
server-originated `ChannelOpen` for SC#6 — it is compiled out of production builds.

Purpose: Deliver MUX-01 (control-first OPEN/ACCEPT/REJECT, opaque REJECT), MUX-02 (concurrent
per-stream channels with no HOL blocking), MUX-03 (per-channel credit flow control), and the
server half of MUX-04 (clean lifecycle, no-panic on unknown ids, parity enforcement — including
the server's own ODD-id parity space being reachable under test).
Output: `crates/nosh-server/src/channel.rs` (new), extended `run_session`/`run_reattach_session`
select! loops, channel-map lifecycle, and a test-only server-open helper.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-channel-multiplexing-foundation/21-CONTEXT.md
@.planning/phases/21-channel-multiplexing-foundation/21-RESEARCH.md
@.planning/phases/21-channel-multiplexing-foundation/21-PATTERNS.md
@.planning/phases/21-channel-multiplexing-foundation/21-01-SUMMARY.md

<interfaces>
<!-- New wire types from plan 21-01 (now in nosh-proto). -->
Message::ChannelOpen { channel_id: u32, channel_type: ChannelType }   // disc 10
Message::ChannelAccept { channel_id: u32 }                            // disc 11
Message::ChannelReject { channel_id: u32 }                            // disc 12 (opaque)
Message::ChannelCredit { channel_id: u32, bytes: u64 }                // disc 13
Message::ChannelClose { channel_id: u32 }                             // disc 14
enum ChannelType { Echo, Scrollback, PortForward, AgentForward }

<!-- Verified server.rs landmarks (read these line ranges, do not re-explore): -->
- handle_connection / run_accept_loop: the ONE existing accept_bi is at lines 557–560 (control
  stream) and MUST stay where it is — never add a second accept_bi before run_session is entered.
- drop(permit): the pre-auth AuthLimits permit is dropped before run_session is entered; the new
  accept_bi arm belongs AFTER that drop, inside run_session/run_reattach_session only.
- run_session select! loop: lines 743–944; the `msg = nosh_proto::read_message(&mut recv)` arm and
  its match block (existing arms 898–941) is where the new ChannelOpen/Credit/Close arms go.
- mpsc + tokio::spawn pattern to copy: lines 685–710.
- send.finish() + send.stopped() half-close: lines 976–982.
- no-panic-on-unknown precedent: lines 888–890.
- run_reattach_session has the same select! shape and needs the same new arms + channel_map.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: New channel.rs module — ChannelEvent, run_channel_task, read_varint_u32</name>
  <files>crates/nosh-server/src/channel.rs, crates/nosh-server/src/lib.rs</files>
  <read_first>
    - 21-PATTERNS.md "crates/nosh-server/src/channel.rs" section (module skeleton, ChannelEvent enum, run_channel_task signature)
    - crates/nosh-server/src/server.rs lines 685–710 (mpsc spawn pattern), 976–982 (send.finish/stopped)
    - crates/nosh-server/src/pty_io.rs module header (module-doc style to match)
    - crates/nosh-server/src/lib.rs (module declaration list)
  </read_first>
  <action>
    Create `crates/nosh-server/src/channel.rs` and declare `pub mod channel;` in lib.rs.
    Define `pub enum ChannelEvent { Stream(quinn::SendStream, quinn::RecvStream), Credit(u64), Close }`
    modelled on the SessionEnd enum style.
    Define `pub async fn run_channel_task(channel_id: u32, mut events: mpsc::Receiver<ChannelEvent>,
    control_tx: mpsc::Sender<Message>)` — the task owns the channel's stream pair once it arrives via
    ChannelEvent::Stream, and communicates back to the session pump ONLY through `control_tx` (an mpsc
    sender for control-stream frames). The task MUST NOT call write_message on the control SendStream
    directly (Pitfall 6 / A4: a second writer corrupts framing — credit and close go through control_tx).
    Implement the test-only Echo behaviour inside the task under `#[cfg(test)]`: read bytes from the
    channel RecvStream, echo them back on the channel SendStream, decrement a `remaining_credit: u64`
    (initialised to 256 * 1024) by bytes sent, and pause sending when credit hits 0 until a
    ChannelEvent::Credit(n) tops it up (MUX-03, 256 KiB window per 21-CONTEXT.md). On RecvStream EOF or
    ChannelEvent::Close, finish the SendStream (send.finish() then a bounded send.stopped() timeout per
    the half-close pattern) and send `Message::ChannelClose { channel_id }` through control_tx so the
    pump removes the map entry, then return — releasing all resources.
    Add a `pub async fn read_varint_u32(recv: &mut quinn::RecvStream) -> anyhow::Result<u32>` helper that
    reads a postcard/LEB128 u32 varint one byte at a time (continuation bit on the high bit), at most 5
    bytes; return Err on a malformed/truncated varint — never panic (RESEARCH Pattern 3, V5 input
    validation). This is the read side of the channel-id prefix; the opener (plan 21-03) writes
    `postcard::to_allocvec(&channel_id)`.
    Code comments in Australian English, no AI-cliché prose; never Debug a frame — use variant_name().
  </action>
  <verify>
    <automated>cargo build -p nosh-server 2>&1 | tail -20</automated>
  </verify>
  <done>channel.rs compiles; ChannelEvent, run_channel_task, read_varint_u32 exported; Echo logic is #[cfg(test)]-gated; task communicates via control_tx mpsc only (no direct control-stream write).</done>
</task>

<task type="auto">
  <name>Task 2: Wire mux dispatch + accept_bi arm into run_session and run_reattach_session</name>
  <files>crates/nosh-server/src/server.rs</files>
  <read_first>
    - crates/nosh-server/src/server.rs lines 743–944 (run_session select! loop + match msg arms 898–941)
    - the run_reattach_session select! loop (same shape; locate via grep for "run_reattach_session")
    - 21-PATTERNS.md "crates/nosh-server/src/server.rs" section (the exact ChannelOpen/Credit/Close arms and accept_bi arm)
    - 21-RESEARCH.md Pitfall 2 (auth bypass), Pitfall 3 (HOL), Pitfall 5 (unknown id no-panic), Pitfall 6 (credit deadlock)
  </read_first>
  <action>
    In both run_session AND run_reattach_session, after the pre-auth permit has been dropped (these
    functions already run post-auth), introduce a `channel_map: HashMap<u32, mpsc::Sender<ChannelEvent>>`
    and a `control_tx`/`control_rx` mpsc pair for channel tasks to send control frames back to the pump.
    Add a select! arm `Ok((ch_send, ch_recv)) = conn.accept_bi()` that reads ONLY the channel-id varint
    via `channel::read_varint_u32`, then forwards `ChannelEvent::Stream(ch_send, ch_recv)` to the matching
    channel task via the map; if no entry exists (stream arrived before ACCEPT processed, or channel
    closed) close/drop the stream, log, and continue — never read channel payload inline (Pitfall 3 HOL).
    On a malformed varint, drop the stream and continue (never panic). On
    ConnectionError::ApplicationClosed/LocallyClosed, break SessionEnd::TransportLost.
    Add a select! arm draining `control_rx` that writes the channel task's outbound control frames
    (ChannelClose, ChannelCredit) to the control-stream SendStream — this is the SINGLE writer for the
    control stream (Pitfall 6 / A4). On receiving a ChannelClose from a task, also remove that id from
    channel_map.
    Extend the `match msg` block with:
    - `Message::ChannelOpen { channel_id, channel_type }`: reject (write ChannelReject) and continue if
      channel_id is odd (client parity is even — log+ignore per 21-PATTERNS), if it already exists in the
      map, or if the open-channel count is at a cap (enforce MAX_OPEN_CHANNELS, e.g. 64 — DoS bound from
      RESEARCH Security Domain). Reject PortForward and AgentForward unconditionally (FWD-01/FWD-02
      declared-but-rejected). Reject Scrollback this phase (Phase 22 consumer). Accept Echo ONLY under
      `#[cfg(test)]`; reject Echo in non-test builds (test-only fixture, 21-CONTEXT.md). On accept: write
      `ChannelAccept { channel_id }`, create an mpsc pair, insert the tx in channel_map, and
      `tokio::spawn(channel::run_channel_task(channel_id, rx, control_tx.clone()))` — NEVER inline
      (Pitfall 3).
    - `Message::ChannelCredit { channel_id, bytes }`: forward `ChannelEvent::Credit(bytes)` to the task;
      unknown id → log + continue (no panic, Pitfall 5).
    - `Message::ChannelClose { channel_id }`: send ChannelEvent::Close to the task (if present) and remove
      the map entry; unknown id → log + continue.
    - `Message::ChannelAccept { .. } | Message::ChannelReject { .. }`: server→client only; receiving from
      the client is a protocol error → break SessionEnd::ClientClosed (matches existing unexpected-frame
      handling).
    Import HashMap and the channel module. Keep all logging via variant_name(), never Debug a frame.
    Confirm no accept_bi is added to run_accept_loop (auth gate — Pitfall 2).
  </action>
  <verify>
    <automated>cargo build -p nosh-server 2>&1 | tail -20; cargo test -p nosh-server -- --test-threads=1 2>&1 | tail -20; grep -n "accept_bi" crates/nosh-server/src/server.rs</automated>
  </verify>
  <done>run_session and run_reattach_session both dispatch ChannelOpen/Credit/Close and have the accept_bi+control_rx arms; Echo accepted only under cfg(test); PortForward/AgentForward/Scrollback rejected; odd ids ignored; MAX_OPEN_CHANNELS enforced; no accept_bi appears before run_session; existing nosh-server tests still pass.</done>
</task>

<task type="auto">
  <name>Task 3: #[cfg(test)]-gated server-initiated odd-id ChannelOpen path (SC#6 evidence)</name>
  <files>crates/nosh-server/src/server.rs, crates/nosh-server/src/channel.rs</files>
  <read_first>
    - crates/nosh-server/src/server.rs run_session select! loop after Task 2 (the channel_map, control_tx/control_rx, and the new ChannelOpen/Accept/Reject arms just added)
    - 21-PATTERNS.md "crates/nosh-server/src/server.rs" section — the existing single-writer control-stream rule (control_tx is the only writer) and the parity check showing client-even / server-odd
    - 21-CONTEXT.md Deferred Ideas (server-initiated channels reserved-but-not-shipped) and Channel-id allocation decision (client-even / server-odd; id 0 reserved)
    - the echo-channel #[cfg(test)] gate added in Task 1 (model this new path's gating the same way)
  </read_first>
  <action>
    Add a SERVER-INITIATED channel-open path that is gated entirely behind `#[cfg(test)]`, modelled on the
    test-only Echo fixture in channel.rs — this is NOT a production server-initiated-channel feature
    (21-CONTEXT.md defers that to a future remote-port-forward consumer). Its sole purpose is to make the
    server's ODD-id parity space reachable so plan 21-04's `channel_simultaneous_open` test can fire a real
    server-originated ChannelOpen concurrently with a client-originated one (ROADMAP SC#6).

    Concretely:
    - In run_session (the fresh-session loop only — cold-reattach does not need it), under `#[cfg(test)]`,
      add a server-side odd-id allocator `next_server_channel_id: u32` initialised to `1` and a private
      helper `#[cfg(test)] fn alloc_server_channel_id(&mut counter) -> u32` (or an inline closure) that
      returns the current value then advances by 2, so it yields 1, 3, 5, … — always ODD, never colliding
      with the client's even space, and never 0 (control). Assert/`debug_assert!` the returned id is odd.
    - Provide a `#[cfg(test)]`-gated trigger by which a test can ask the running server session to open a
      channel from its own side. Prefer the smallest mechanism that fits the existing harness: add a new
      `#[cfg(test)]` select! arm in run_session that drains a `tokio::sync::mpsc::Receiver<ChannelType>`
      (a `server_open_rx` created next to channel_map). When a value arrives, allocate an odd id via the
      allocator, register an mpsc pair in channel_map exactly as the client-open accept path does, spawn
      `channel::run_channel_task`, and send a real `Message::ChannelOpen { channel_id, channel_type }` over
      the control stream THROUGH `control_tx` (never a second direct writer — Pitfall 6 / A4 single-writer
      invariant). Expose the corresponding `server_open_tx` to the test harness via a `#[cfg(test)]` field
      on whatever per-session handle the integration tests already use to reach a live session (model the
      wiring on how the existing reattach tests reach session state); if no such handle exists, add a
      minimal `#[cfg(test)]` accessor rather than widening any production API.
    - The server then awaits the client's `ChannelAccept`/`ChannelReject` for that odd id on the existing
      control-stream read arm: extend the `Message::ChannelAccept { channel_id } | Message::ChannelReject
      { channel_id }` handling so that — under `#[cfg(test)]` only — an ACCEPT/REJECT whose id is ODD and
      present in channel_map is treated as the valid reply to a server-initiated open (Accept → leave the
      channel live and, if needed, drive the test-only echo/data exchange; Reject → drop the map entry),
      while the production behaviour for client-sent ACCEPT/REJECT (even ids, or unknown ids) remains the
      protocol-error/no-op path from Task 2. Keep unknown/already-closed odd ids a logged no-op (MUX-04,
      Pitfall 5) — never a panic.

    All new symbols (`next_server_channel_id`, `alloc_server_channel_id`, `server_open_rx`/`server_open_tx`,
    and the odd-id ACCEPT/REJECT handling) MUST be `#[cfg(test)]`-gated so they are absent from production
    binaries. Australian English comments; never Debug a frame (use variant_name()).
  </action>
  <verify>
    <automated>cargo build -p nosh-server 2>&1 | tail -20; cargo build --release -p nosh-server 2>&1 | tail -5; grep -n "next_server_channel_id\|server_open_tx\|server_open_rx" crates/nosh-server/src/server.rs; grep -c "cfg(test)" crates/nosh-server/src/server.rs; cargo test -p nosh-server -- --test-threads=1 2>&1 | tail -15</automated>
  </verify>
  <done>A #[cfg(test)] server-initiated path exists: an odd-id allocator (1, 3, 5, …), a test trigger that sends a real Message::ChannelOpen over the control stream via the single control_tx writer, and odd-id ChannelAccept/ChannelReject reply handling. Release build of nosh-server compiles with every new symbol compiled out (no production server-initiated-open surface). Existing nosh-server tests still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → server control stream | Untrusted ChannelOpen/Credit/Close frames cross here after auth |
| client → server secondary bidi stream | Untrusted channel-id varint prefix + channel payload |
| pre-auth → post-auth | The accept_bi arm must live only on the post-auth side of the AuthLimits permit |
| test build → production build | The server-initiated odd-id open path is a test fixture and must not exist in production binaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03 | Elevation of Privilege | secondary accept_bi loop | mitigate | accept_bi arm exists only inside run_session/run_reattach_session, after drop(permit); never in run_accept_loop — verified by grep gate (Pitfall 2) |
| T-21-04 | Denial of Service | unbounded channel opens | mitigate | MAX_OPEN_CHANNELS cap (64); ChannelOpen past the cap is REJECTed |
| T-21-05 | Denial of Service | slow channel consumer | mitigate | 256 KiB per-channel byte-credit window; sender pauses at 0 credit, never blocks the session pump; channel I/O is in a spawned task (Pitfall 3, MUX-03) |
| T-21-06 | Tampering / Crash | malformed channel-id varint | mitigate | read_varint_u32 returns Err on truncated/oversized varint; stream dropped, not panicked (V5) |
| T-21-07 | Crash | ChannelAccept/Reject/Credit/Close for unknown id | mitigate | unknown-id handlers log + continue, never unwrap/panic (Pitfall 5, MUX-04) |
| T-21-08 | Elevation of Privilege | PortForward/AgentForward accepted early | mitigate | both channel types unconditionally REJECTed by v1.3 peers; SSH_AUTH_SOCK never reachable (FWD-02 deferred) |
| T-21-09 | Information disclosure | REJECT reason oracle | mitigate | ChannelReject carries only channel_id (enforced by the wire type in plan 21-01); identical REJECT for unknown-type, duplicate, parity, and at-cap cases |
| T-21-10 | Elevation of Privilege | server-initiated odd-id open reachable in production | mitigate | the odd-id allocator, server_open trigger, and odd-id ACCEPT/REJECT handling are entirely `#[cfg(test)]`-gated and compiled out of production builds — verified by a release build compiling with the symbols absent (server-initiated channels remain deferred per 21-CONTEXT.md); the test path still uses the single control_tx writer, so it cannot corrupt control-stream framing |
</threat_model>

<verification>
- `cargo test -p nosh-server -- --test-threads=1` green
- `grep -n "accept_bi" crates/nosh-server/src/server.rs` shows the control-stream accept (pre-existing) plus the new one(s) inside run_session/run_reattach_session — none in run_accept_loop
- Channel tasks send control frames only via control_tx (no write_message on the control SendStream inside channel.rs)
- `cargo build --release -p nosh-server` compiles with the server-initiated odd-id path absent (all `#[cfg(test)]`-gated)
</verification>

<success_criteria>
MUX-01 (control-first OPEN→ACCEPT/REJECT, opaque REJECT), MUX-02 (per-stream channel tasks, no
inline channel reads in the pump), MUX-03 (256 KiB credit window) and the server half of MUX-04
(no-panic unknown ids, parity, lifecycle teardown) are implemented in both the fresh-session and
cold-reattach session loops. Echo is test-only; forwarding types are rejected. The server's ODD-id
parity space is reachable under `#[cfg(test)]` via a real server-initiated ChannelOpen, providing the
binding evidence for ROADMAP SC#6 (consumed by plan 21-04) without adding any production
server-initiated-channel surface.
</success_criteria>

<output>
Create `.planning/phases/21-channel-multiplexing-foundation/21-02-SUMMARY.md` when done
</output>
</content>
</invoke>
