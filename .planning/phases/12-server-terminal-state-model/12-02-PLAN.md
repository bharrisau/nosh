---
phase: 12-server-terminal-state-model
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - crates/nosh-server/src/registry.rs
  - crates/nosh-server/src/server.rs
autonomous: true
requirements: [SYNC-02]

must_haves:
  truths:
    - "SessionSlot owns a TerminalState behind a Mutex, fed from the same callsite as SequencedOutputBuffer"
    - "push_output_and_parse pushes to SequencedOutputBuffer FIRST (assigns seq, never fails) THEN advances TerminalState"
    - "All three server.rs PTY-output callsites feed both buffers"
    - "SequencedOutputBuffer behavior is byte-identical to before — cold-reattach replay (replay_from/trim_acked) is unaffected"
    - "PTY resize updates TerminalState grid dimensions (no reflow) alongside the existing Session::resize"
  artifacts:
    - path: "crates/nosh-server/src/registry.rs"
      provides: "terminal_state: Mutex<TerminalState> field on SessionSlot; push_output_and_parse; terminal-state resize hook"
      contains: "push_output_and_parse"
    - path: "crates/nosh-server/src/server.rs"
      provides: "3 callsites converted from push_output to push_output_and_parse"
      contains: "push_output_and_parse"
  key_links:
    - from: "SessionSlot::push_output_and_parse"
      to: "SequencedOutputBuffer::push + TerminalState::advance"
      via: "push FIRST (seq), advance SECOND"
      pattern: "output_buf.lock\\(\\).*push.*\\n.*terminal_state.lock\\(\\).*advance"
    - from: "crates/nosh-server/src/server.rs PTY output callsites"
      to: "slot.push_output_and_parse"
      via: "out_rx.recv chunk handling"
      pattern: "push_output_and_parse"
---

<objective>
Wire the `TerminalState` model (from Plan 12-01) into the live session substrate. Add a
`terminal_state: Mutex<TerminalState>` field to `SessionSlot`, add the
`push_output_and_parse` method that feeds BOTH the `SequencedOutputBuffer` (UNCHANGED) and
the `TerminalState`, convert the three `server.rs` PTY-output callsites, and hook the resize
path so grid dimensions track the PTY (no reflow, D-12-03).

Purpose: SYNC-02 success criterion 3 — both buffers fed from one callsite, with cold-reattach
replay provably unaffected. The SequencedOutputBuffer path must remain byte-identical.
Output: modified `registry.rs` and `server.rs`; a regression test proving replay integrity.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-server-terminal-state-model/12-CONTEXT.md
@.planning/phases/12-server-terminal-state-model/12-RESEARCH.md
@.planning/phases/12-server-terminal-state-model/12-01-PLAN.md

<interfaces>
<!-- From Plan 12-01 (crates/nosh-server/src/terminal.rs): -->
<!-- TerminalState::new(cols: u16, rows: u16) -> TerminalState -->
<!-- TerminalState::advance(&mut self, bytes: &[u8])   (no error path; CSI/OSC errors silently ignored) -->
<!-- TerminalState read API: cursor(), cell(row,col), echo_state(), title(), osc52_pending(), size() -->

<!-- Existing SessionSlot (registry.rs), verified read from source: -->
<!-- output_buf: Mutex<SequencedOutputBuffer>  (private field) -->
<!-- pub fn push_output(&self, chunk: &[u8]) -> u64  { self.output_buf.lock().unwrap().push(chunk) }   line ~344 -->
<!-- pub fn resize(&self, cols: u16, rows: u16) -> anyhow::Result<()> { self.session.lock().unwrap().resize(cols, rows) }   line ~409 -->
<!-- SessionSlot::new(session) builds the Arc<SessionSlot> with all field initializers   line ~257 -->
<!-- Lock discipline: std::sync::Mutex, held only for brief field ops, never across .await -->

<!-- server.rs PTY-output callsites (verified line numbers): -->
<!-- ~414  slot.push_output(&data);   (interactive pump) -->
<!-- ~504  slot.push_output(&data);   (ShellExited drain loop) -->
<!-- ~793  slot.push_output(&data);   (reattach pump) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add terminal_state field + push_output_and_parse to SessionSlot; hook resize</name>
  <files>crates/nosh-server/src/registry.rs</files>
  <read_first>
    - .planning/phases/12-server-terminal-state-model/12-RESEARCH.md (Pattern 6 push_output_and_parse — push FIRST then advance; Pitfall 8 ordering / replay-integrity)
    - crates/nosh-server/src/registry.rs lines 41-92 (SequencedOutputBuffer::push), 235-270 (SessionSlot fields + new), 342-346 (push_output), 408-411 (resize)
    - crates/nosh-server/src/terminal.rs (TerminalState::new / advance / size — from Plan 12-01)
  </read_first>
  <behavior>
    - SessionSlot::new initializes terminal_state to TerminalState::new with a sane default size (e.g. 80x24) wrapped in a Mutex.
    - push_output_and_parse(&self, chunk) returns the SAME u64 seq that push_output would have returned for the same chunk sequence (SequencedOutputBuffer unchanged): it calls output_buf.lock().push(chunk) FIRST capturing seq, THEN terminal_state.lock().advance(chunk), THEN returns seq.
    - The existing push_output method continues to exist and behaves byte-identically (other callers / replay path untouched).
    - resize(cols, rows) resizes the TerminalState grid dimensions in addition to the existing Session::resize (no reflow, D-12-03); ordering keeps Session::resize's Result as the return value.
  </behavior>
  <action>
    Add a `use crate::terminal::TerminalState;` import. Add a private field `terminal_state: Mutex<TerminalState>` to the `SessionSlot` struct (alongside `output_buf`). Initialize it in `SessionSlot::new` to `Mutex::new(TerminalState::new(80, 24))` (default size; the resize path corrects it when the client reports dimensions) — document that 80x24 is the conventional default until the first resize.

    Add method `push_output_and_parse(&self, chunk: &[u8]) -> u64` per RESEARCH Pattern 6 and Pitfall 8: acquire `output_buf` lock and call `push(chunk)` FIRST, capturing the returned `seq` (this is the seq-assigning, never-failing replay path — must run first); release that lock; then acquire `terminal_state` lock and call `advance(chunk)`; return `seq`. Document the lock order (output_buf then terminal_state; never held across `.await`) and that the SequencedOutputBuffer call MUST stay first so an issue in advance can never skip sequencing (reattach integrity).

    Leave the existing `push_output` method UNCHANGED (it remains the byte-identical primitive; push_output_and_parse delegates the buffer half to the same `output_buf.lock().push`).

    Extend the existing `resize` method: after (or before) the `self.session.lock().unwrap().resize(cols, rows)` call, also call `self.terminal_state.lock().unwrap()` and resize the grid to (cols, rows) — add a `TerminalState::resize(&mut self, cols, rows)` method in terminal.rs if Plan 12-01 did not expose one (resize grid dimensions only: truncate/extend each row to cols width with default cells, truncate/extend rows; on shrink, top rows scroll into scrollback; NO reflow per D-12-03). Preserve the `anyhow::Result<()>` return from `Session::resize`.
  </action>
  <verify>
    <automated>cargo build -p nosh-server 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p nosh-server` exits 0.
    - registry.rs `SessionSlot` has a `terminal_state: Mutex<TerminalState>` field (verify: `grep -n 'terminal_state' crates/nosh-server/src/registry.rs` returns matches in struct + new + push_output_and_parse + resize).
    - push_output_and_parse exists and calls output_buf push before terminal_state advance (verify: `grep -n 'push_output_and_parse' crates/nosh-server/src/registry.rs` returns a match; manual read confirms push precedes advance).
    - The original `push_output` method body is unchanged (verify: `grep -A1 'pub fn push_output(' crates/nosh-server/src/registry.rs` still shows the single `output_buf.lock().unwrap().push(chunk)` line).
    - resize touches terminal_state (verify: `grep -n 'terminal_state' crates/nosh-server/src/registry.rs` shows a match inside the resize method region).
  </acceptance_criteria>
  <done>SessionSlot owns a TerminalState, push_output_and_parse feeds both buffers (push first), and resize updates grid dimensions — all without altering the SequencedOutputBuffer primitive.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert the 3 server.rs callsites and prove reattach replay is unaffected</name>
  <files>crates/nosh-server/src/server.rs, crates/nosh-server/src/registry.rs</files>
  <read_first>
    - crates/nosh-server/src/server.rs lines 410-422 (interactive pump callsite ~414), 501-511 (drain loop ~504), 788-800 (reattach pump ~793)
    - crates/nosh-server/src/registry.rs lines 115-196 (replay_from / trim_acked — the cold-reattach path that MUST remain byte-identical)
    - .planning/phases/12-server-terminal-state-model/12-RESEARCH.md (Pitfall 8 reattach-integrity)
  </read_first>
  <behavior>
    - All three `slot.push_output(&data)` callsites in server.rs become `slot.push_output_and_parse(&data)`; the surrounding logic (write_message, break conditions, drain loop) is unchanged.
    - A registry unit test proves: pushing a fixed chunk sequence via push_output_and_parse yields the SAME assigned seq numbers, the SAME replay_from(last_acked) output (chunks/seq/truncated tuple), and the SAME trim_acked behavior as pushing the identical sequence via push_output — i.e. SequencedOutputBuffer semantics are byte-identical (success criterion 3, reattach unaffected).
    - The same test confirms terminal_state observably advanced (e.g. after pushing b"hi" the slot's terminal cursor/cell reflects it) so both buffers are genuinely fed from one call.
  </behavior>
  <action>
    In crates/nosh-server/src/server.rs, change the three `slot.push_output(&data)` calls (interactive pump ~414, ShellExited drain ~504, reattach pump ~793) to `slot.push_output_and_parse(&data)`. Do NOT touch the seq usage, write_message calls, break/SessionEnd logic, or drain-loop timeouts — only the method name changes. Confirm via grep that no stray `push_output(` callsite remains in server.rs that should have been converted (the only remaining `push_output` references should be intentional, if any; the three PTY-output feeds must all be the `_and_parse` variant).

    In crates/nosh-server/src/registry.rs `#[cfg(test)]` module, add a regression test that builds a SessionSlot (or constructs a SequencedOutputBuffer directly alongside a TerminalState if SessionSlot construction needs a real Session — prefer the lowest-dependency approach: if SessionSlot::new requires a live PTY, instead assert the equivalence at the SequencedOutputBuffer level by replicating the push-first ordering, AND add a slot-level test guarded behind whatever the existing registry tests use for a fake/real session). The test MUST assert: (a) seq numbers from push_output_and_parse match a control run of push_output for the same chunk sequence; (b) replay_from and trim_acked produce identical results; (c) the terminal_state reflects the fed bytes. Mirror the style of existing registry tests for slot construction.
  </action>
  <verify>
    <automated>cargo test -p nosh-server --lib 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p nosh-server --lib` exits 0 (all registry + terminal tests pass).
    - server.rs has exactly the three PTY-output feeds converted (verify: `grep -c 'push_output_and_parse' crates/nosh-server/src/server.rs` == 3).
    - No un-converted PTY-output feed remains (verify: read the three callsite regions; each shows `push_output_and_parse`).
    - A registry test asserts seq-number and replay_from/trim_acked equivalence between push_output_and_parse and push_output for an identical chunk sequence (reattach byte-identical).
    - A registry/slot test asserts the terminal_state advanced after a push_output_and_parse call (both buffers fed).
  </acceptance_criteria>
  <done>All three PTY-output callsites feed both buffers; a regression test proves SequencedOutputBuffer/replay is byte-identical and the terminal model is genuinely fed from the same call.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PTY master output → SessionSlot::push_output_and_parse | Same untrusted PTY byte stream now also drives TerminalState; must not affect the sequencing/replay path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Tampering | push_output_and_parse ordering | mitigate | SequencedOutputBuffer::push is called FIRST (assigns seq, never fails); TerminalState::advance second (no error path). A regression test asserts seq/replay_from/trim_acked are byte-identical to the pre-existing push_output path — reattach integrity preserved (Pitfall 8). |
| T-12-06 | Denial of Service | terminal_state Mutex under PTY load | accept | terminal_state lock is held only for the brief advance() (no .await across it), matching the existing output_buf lock discipline; advance is bounded (scrollback cap from Plan 12-01). No new unbounded surface. |
| T-12-07 | Repudiation | replay correctness regression | mitigate | Existing replay_from/trim_acked code is UNCHANGED; the new method delegates to the same SequencedOutputBuffer::push. Regression test guards against accidental divergence. |
</threat_model>

<verification>
- `cargo build -p nosh-server` exits 0.
- `cargo test -p nosh-server --lib` exits 0.
- `grep -c 'push_output_and_parse' crates/nosh-server/src/server.rs` == 3.
- Existing SequencedOutputBuffer replay tests (if present) still pass unchanged.
</verification>

<success_criteria>
- push_output_and_parse on SessionSlot feeds both SequencedOutputBuffer (unchanged) and TerminalState; cold-reattach replay unaffected (success criterion 3).
- All three server.rs PTY-output callsites converted.
- Resize updates grid dimensions (no reflow, D-12-03).
- Regression test proves byte-identical replay semantics.
</success_criteria>

<output>
Create `.planning/phases/12-server-terminal-state-model/12-02-SUMMARY.md` when done.
</output>
