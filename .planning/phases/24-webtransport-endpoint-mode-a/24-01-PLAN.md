---
phase: 24-webtransport-endpoint-mode-a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/nosh-server/Cargo.toml
  - crates/nosh-client/Cargo.toml
autonomous: true
requirements: [WT-02]
must_haves:
  truths:
    - "`cargo build --workspace --features nosh-server/webtransport,nosh-client/webtransport` compiles"
    - "`cargo tree -f \"{p} {f}\" | grep rustls` shows only the `ring` provider — no `aws-lc-rs`"
    - "Existing `cargo test --workspace` still passes (default features, no behaviour change)"
  artifacts:
    - path: "Cargo.toml"
      provides: "wtransport 0.7.1 + time pin in [workspace.dependencies]"
      contains: "wtransport"
    - path: "crates/nosh-server/Cargo.toml"
      provides: "webtransport feature + optional wtransport dep"
      contains: "webtransport"
    - path: "crates/nosh-client/Cargo.toml"
      provides: "webtransport feature + optional wtransport dep"
      contains: "webtransport"
  key_links:
    - from: "crates/nosh-server/Cargo.toml"
      to: "Cargo.toml [workspace.dependencies] wtransport"
      via: "wtransport = { workspace = true, optional = true }"
      pattern: "wtransport.*workspace = true.*optional"
---

<objective>
Add the `wtransport` 0.7.1 dependency to the workspace and gate it behind a `webtransport` Cargo feature on both `nosh-server` and `nosh-client`, without activating the `aws-lc-rs` crypto provider (D-01, Pitfall WT-1). Pin `time = "=0.3.47"` at the workspace level (D-02, wtransport issue #311 still open as of 2026-06-13).

Purpose: Establishes the build foundation every other Phase 24 plan depends on. The crypto-provider unification check (SC#4) is the day-one blocker called out in STATE.md Phase 24 concerns — it must be confirmed here before any wtransport code is written.
Output: Workspace + both crate manifests carry the optional wtransport dep and `webtransport` feature; `cargo tree` confirms ring-only.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-webtransport-endpoint-mode-a/24-RESEARCH.md
@.planning/phases/24-webtransport-endpoint-mode-a/24-PATTERNS.md

<interfaces>
<!-- Current workspace [workspace.dependencies] (Cargo.toml) already pins: -->
<!--   quinn = { version = "0.11.9", default-features = false, features = ["runtime-tokio", "rustls-ring"] } -->
<!--   rustls = { version = "0.23", default-features = false, features = ["ring", "std"] } -->
<!--   rcgen = "0.14"; async-trait = "0.1.89" -->
<!-- nosh-server/Cargo.toml [features] already has: test-support = [] -->
<!-- nosh-client/Cargo.toml [dev-dependencies] already has: -->
<!--   nosh-server = { path = "../nosh-server", features = ["test-support"] } -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add wtransport + time pin to workspace dependencies</name>
  <read_first>
    - Cargo.toml (the `[workspace.dependencies]` block, lines ~20-36, and any existing `time` entry)
    - .planning/phases/24-webtransport-endpoint-mode-a/24-RESEARCH.md §"Standard Stack" (feature list `["self-signed", "ring", "quinn"]`, the `quinn` feature rationale, the `time = "=0.3.47"` pin rationale)
    - .planning/phases/24-webtransport-endpoint-mode-a/24-PATTERNS.md §"`Cargo.toml` (workspace, modify)"
  </read_first>
  <action>
    Re-verify the D-02 re-ask trigger first: run `cargo search wtransport` and check whether a 0.7.2+ exists that closes issue #311. If only 0.7.1 is published (expected per research), proceed with the pin. Add to `[workspace.dependencies]` in the root `Cargo.toml`, per D-01: `wtransport = { version = "0.7.1", default-features = false, features = ["self-signed", "ring", "quinn"] }`. The `quinn` feature is REQUIRED (not optional) — it exposes `Connection::quic_connection() -> &quinn::Connection`, which the WT wrapper needs for `datagram_send_buffer_space` (it has no native wtransport method). Add the workspace pin `time = "=0.3.47"` (D-02) so rcgen→time coherence does not break the build via wtransport issue #311. Do NOT remove `default-features = false` and do NOT add `aws-lc-rs` anywhere.
  </action>
  <verify>
    <automated>grep -q 'wtransport = { version = "0.7.1", default-features = false, features = \["self-signed", "ring", "quinn"\] }' Cargo.toml && grep -q 'time = "=0.3.47"' Cargo.toml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `Cargo.toml` `[workspace.dependencies]` contains the exact wtransport entry with `default-features = false` and features `["self-signed", "ring", "quinn"]`.
    - `Cargo.toml` contains `time = "=0.3.47"`.
    - No `aws-lc-rs` string appears anywhere in `Cargo.toml`.
  </acceptance_criteria>
  <done>wtransport 0.7.1 and the time pin are declared once at workspace level with the verified feature set.</done>
</task>

<task type="auto">
  <name>Task 2: Add webtransport feature + optional dep to both crates and verify ring-only crypto provider</name>
  <read_first>
    - crates/nosh-server/Cargo.toml (`[features]` block with `test-support = []`, and `[dependencies]`)
    - crates/nosh-client/Cargo.toml (`[dependencies]`, `[dev-dependencies]`)
    - .planning/phases/24-webtransport-endpoint-mode-a/24-PATTERNS.md §"`crates/nosh-server/Cargo.toml`" and §"nosh-client/Cargo.toml"
    - .planning/phases/24-webtransport-endpoint-mode-a/24-RESEARCH.md §"Common Pitfalls" → Pitfall 2 (crypto-provider unification)
  </read_first>
  <action>
    In `crates/nosh-server/Cargo.toml`: add `webtransport = ["dep:wtransport"]` to `[features]` (keep `test-support = []`); add `wtransport = { workspace = true, optional = true }` to `[dependencies]`. In `crates/nosh-client/Cargo.toml`: add the same `webtransport = ["dep:wtransport"]` feature and the same optional `[dependencies]` entry. Leave the existing `nosh-server` dev-dependency `features = ["test-support"]` unchanged for now — Plan 05 will append `"webtransport"` to it. After editing, build with the feature enabled and run the crypto-provider gate (SC#4): build `cargo build -p nosh-server -p nosh-client --features nosh-server/webtransport,nosh-client/webtransport`, then `cargo tree -f "{p} {f}"` and confirm rustls appears only with `ring` (no `aws-lc-rs`). If `aws-lc-rs` appears, do NOT proceed — the fix is `default-features = false` on the wtransport entry (Pitfall WT-1).
  </action>
  <verify>
    <automated>cargo build -p nosh-server -p nosh-client --features nosh-server/webtransport,nosh-client/webtransport 2>&1 | tail -5; cargo tree -f "{p} {f}" --features nosh-server/webtransport,nosh-client/webtransport 2>/dev/null | grep -i rustls | grep -vi 'aws-lc' | head; test -z "$(cargo tree -f '{p} {f}' --features nosh-server/webtransport,nosh-client/webtransport 2>/dev/null | grep -i aws-lc-rs)" && echo RING_ONLY_OK</automated>
  </verify>
  <acceptance_criteria>
    - Both crate manifests have `webtransport = ["dep:wtransport"]` in `[features]` and `wtransport = { workspace = true, optional = true }` in `[dependencies]`.
    - `cargo build` with `--features nosh-server/webtransport,nosh-client/webtransport` exits 0.
    - `cargo tree -f "{p} {f}"` with the webtransport features enabled shows NO `aws-lc-rs` line; rustls shows the `ring` provider only (prints `RING_ONLY_OK`). This is SC#4.
    - `cargo test --workspace` (default features) still passes — no behavioural change to existing builds.
  </acceptance_criteria>
  <done>Both crates gate wtransport behind an off-by-default `webtransport` feature; enabling it builds with the ring crypto provider only (SC#4 satisfied).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time dependency graph → runtime crypto provider | A second rustls crypto provider (aws-lc-rs) silently activated by feature unification would panic at the first TLS handshake or, worse, change signature behaviour |
| crates.io → workspace | A new third-party crate (wtransport) enters the supply chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01-T | Tampering | rustls crypto provider activation | mitigate | `default-features = false` on the wtransport entry; SC#4 `cargo tree` gate in Task 2 asserts ring-only |
| T-24-01-SC | Tampering | crates.io wtransport install | accept | RESEARCH Package Legitimacy Audit rated wtransport [OK] via slopcheck (established crate, ~2yr, github.com/BiagioFesta/wtransport). No [ASSUMED]/[SUS] packages → no blocking human checkpoint required |
| T-24-01-D | Denial of Service | build break from time crate drift (#311) | mitigate | Pin `time = "=0.3.47"` (D-02); re-ask trigger re-verified at task start |
</threat_model>

<verification>
- `cargo build --workspace` (default) exits 0.
- `cargo build -p nosh-server -p nosh-client --features nosh-server/webtransport,nosh-client/webtransport` exits 0.
- `cargo tree -f "{p} {f}"` with webtransport features shows ring-only, no aws-lc-rs (SC#4).
- `cargo test --workspace` (default features) passes unchanged.
</verification>

<success_criteria>
wtransport 0.7.1 is integrated as an off-by-default feature on both binaries with the verified feature set, the time pin is in place, and the crypto provider is confirmed ring-only (SC#4). No application code references wtransport yet.
</success_criteria>

<output>
Create `.planning/phases/24-webtransport-endpoint-mode-a/24-01-SUMMARY.md` when done.
</output>
