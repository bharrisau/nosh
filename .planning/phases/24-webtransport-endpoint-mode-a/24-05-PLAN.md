---
phase: 24-webtransport-endpoint-mode-a
plan: 05
type: execute
wave: 3
depends_on: ["24-03", "24-04"]
files_modified:
  - crates/nosh-client/tests/webtransport.rs
  - crates/nosh-client/tests/common/mod.rs
  - crates/nosh-client/Cargo.toml
autonomous: true
requirements: [WT-03, WT-05]
must_haves:
  truths:
    - "An in-process `nosh-server --mode webtransport` (test-support) and a `--webtransport` client complete a live PTY shell session: input echoes, command output round-trips, resize takes effect"
    - "Datagram state-sync flows over the WebTransport session (a state diff is received and applied, proving predictive-echo/scrollback transport works identically to native QUIC)"
    - "A raw-QUIC client attempting to connect to the WebTransport-mode server does NOT reach a session (WT-05 downgrade protection)"
  artifacts:
    - path: "crates/nosh-client/tests/webtransport.rs"
      provides: "WebTransport end-to-end shell session test + datagram-over-WT test + downgrade-rejection test"
      min_lines: 80
      contains: "mode webtransport"
  key_links:
    - from: "crates/nosh-client/tests/webtransport.rs"
      to: "nosh_server::wt_transport::run_wt_accept_loop + nosh_client::wt_transport::connect_wt"
      via: "in-process server spawn + client dial"
      pattern: "run_wt_accept_loop|connect_wt"
    - from: "crates/nosh-client/Cargo.toml [dev-dependencies]"
      to: "nosh-server features test-support + webtransport"
      via: "features list"
      pattern: "test-support.*webtransport|webtransport.*test-support"
---

<objective>
Prove the Phase 24 win condition with an integration test: an in-process WebTransport server (test-support auth bypass) and a `--webtransport` client complete a live interactive shell over WebTransport — input/output/resize working, datagram state-sync flowing, and a raw-QUIC client structurally unable to reach a session (WT-05). This is the actual end-to-end success gate, not a unit compile check (SC#1, SC#2, SC#3).

Purpose: Closes WT-03 (interactive shell + datagram/channels over WebTransport) and WT-05 (downgrade rejection) with executable proof. Uses the `test-support`-gated auth stub so the pump is exercised before inner auth exists (Phase 25).
Output: `crates/nosh-client/tests/webtransport.rs` with three tests, plus the dev-dependency feature wiring.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-webtransport-endpoint-mode-a/24-RESEARCH.md
@.planning/phases/24-webtransport-endpoint-mode-a/24-PATTERNS.md

@crates/nosh-client/tests/session.rs
@crates/nosh-client/tests/transport.rs

<interfaces>
<!-- Existing test harness (crates/nosh-client/tests/common/mod.rs): -->
<!--   TestKey::generate() / .public / .client_identity() ; HOST const -->
<!--   spawn_server_with_shell(&host_key, &[&client_pub], AuthLimits::default(), Some("/bin/sh")) -> TestServer { addr, registry, .. } -->
<!--     (uses server::make_endpoint + server::run_accept_loop on a background task — the NATIVE quinn path) -->
<!--   client_endpoint(identity, known_hosts) -> quinn::Endpoint ; have_sh() -->
<!-- New seams available after Plans 02-04: -->
<!--   nosh_server::wt_transport::make_wt_endpoint(addr, cert_path, key_path) -> wtransport::Endpoint<Server> -->
<!--   nosh_server::wt_transport::run_wt_accept_loop(endpoint, Arc<SessionRegistry>, AuthLimits, Option<String>) -->
<!--   nosh_client::wt_transport::build_wt_client_config() (with_native_certs) + a test-only custom-TLS variant for self-signed certs -->
<!--   nosh_client::wt_transport::connect_wt(config, url) -> Box<dyn NoshTransport> -->
<!--   nosh_client::client session helpers (run_session_collect, open_session, etc.) now take &dyn NoshTransport (Plan 02) -->
<!-- The test-support auth bypass is active because the dev-dep enables nosh-server features ["test-support","webtransport"]. -->
<!-- A self-signed outer cert for the test: wtransport's `self-signed` feature provides cert generation; -->
<!--   OR mint via rcgen (already in tree) and write PEM to a tempfile for make_wt_endpoint's cert/key args. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the dev-dependency feature and add a WebTransport server-spawn harness helper</name>
  <read_first>
    - crates/nosh-client/Cargo.toml ([dev-dependencies], the existing `nosh-server = { path = "../nosh-server", features = ["test-support"] }` line ~51)
    - crates/nosh-client/tests/common/mod.rs (spawn_server_with_shell ~93, spawn_server_with_registry ~107, the background-task accept-loop spawn pattern ~135, client_endpoint ~147)
    - .planning/phases/24-webtransport-endpoint-mode-a/24-RESEARCH.md §"Environment Availability Audit" (use an ephemeral high port like 127.0.0.1:0 — NOT 443 — for the test)
  </read_first>
  <action>
    Edit `crates/nosh-client/Cargo.toml` [dev-dependencies]: change the nosh-server entry to `features = ["test-support", "webtransport"]` so the test build pulls in the WT module and the auth-bypass stub. Add a `crates/nosh-client` dev-dependency on the `webtransport` feature of `nosh-client` itself if cargo requires it to compile the test against `nosh_client::wt_transport` (i.e. enable the client's `webtransport` feature for the test target — via a `[features]` test toggle or by adding `nosh-client = { path = ".", features=["webtransport"] }` is not valid for self; instead ensure the test is run with `--features webtransport`, OR gate the test file behind `#![cfg(feature = "webtransport")]` and document running it with `cargo test -p nosh-client --features webtransport`). In `common/mod.rs`, add a `spawn_wt_server(host_cert_pem, host_key_pem, shell) -> WtTestServer { addr }` helper: mint a self-signed outer cert (use rcgen — already in the tree — to produce cert+key PEM, written to tempfiles, since `make_wt_endpoint` takes cert/key file paths), bind `make_wt_endpoint("127.0.0.1:0".parse(), &cert_path, &key_path)`, read back the bound `addr` from the endpoint (`endpoint.local_addr()`), spawn `run_wt_accept_loop(endpoint, registry, AuthLimits::default(), Some("/bin/sh"))` on a background task, and return the address. Skip cleanly (return None / early-return) if `/bin/sh` is unavailable, mirroring the existing `have_sh()` guard.
  </action>
  <verify>
    <automated>grep -E "test-support.*webtransport|webtransport.*test-support" crates/nosh-client/Cargo.toml && cargo build -p nosh-client --features webtransport --tests 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `crates/nosh-client/Cargo.toml` dev-dep `nosh-server` enables both `test-support` and `webtransport`.
    - `common/mod.rs` has a `spawn_wt_server` helper that binds an ephemeral-port (`127.0.0.1:0`) wtransport endpoint with a self-signed outer cert and runs `run_wt_accept_loop` on a background task, returning the bound address.
    - `cargo build -p nosh-client --features webtransport --tests` exits 0.
  </acceptance_criteria>
  <done>The test harness can spawn an in-process WebTransport server with a self-signed outer cert and the test-support auth bypass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WebTransport end-to-end shell, datagram-sync, and downgrade-rejection tests</name>
  <read_first>
    - crates/nosh-client/tests/session.rs (sess01_02_04_real_tty_and_io: the SessionOpen + script + collect + assert IS_TTY/hello-nosh/stty-size pattern; sess05_resize)
    - crates/nosh-client/tests/sync.rs (how a datagram state-diff is observed/applied on the client — mirror its assertion style for the WT datagram test)
    - crates/nosh-client/tests/transport.rs (the connect + session-usability probe pattern)
    - .planning/phases/24-webtransport-endpoint-mode-a/24-RESEARCH.md §"Re-ask 6" (WT-05: a raw-QUIC client never becomes a session against a wtransport endpoint)
  </read_first>
  <behavior>
    - Test 1 (WT-03 / SC#1): client dials the WT server with `connect_wt`, opens a session, runs `test -t 0 && echo IS_TTY; echo hello-nosh; stty size; exit 0` via the generic pump helper (`run_session_collect` against the boxed `NoshTransport`), and the collected output contains `IS_TTY`, `hello-nosh`, and the configured size — proving a real interactive PTY shell over WebTransport.
    - Test 2 (WT-03 / SC#2): over the same WebTransport session, at least one datagram state-diff is received and applied client-side (or a datagram round-trips), proving RFC 9221 datagram transport works over wtransport identically to native QUIC. `max_datagram_size()` on the WT transport returns `Some(_)` (D-03 capsule-adjusted value).
    - Test 3 (WT-05 / SC#3): a raw-QUIC client (the existing native `client::connect` + `client_endpoint`, pointed at the WT server's address) FAILS to establish a usable session within a short timeout — it never reaches `SessionOpened`/a shell. The assertion is that the native connect errors or times out (no session), not that it succeeds.
  </behavior>
  <action>
    Create `crates/nosh-client/tests/webtransport.rs`, gated `#![cfg(feature = "webtransport")]` with a `have_sh()` skip guard. Build a client WT config via the test-only custom-TLS variant (`build_wt_client_config_insecure` / a self-signed-accepting `rustls::ClientConfig`) so it trusts the harness's self-signed outer cert — the OS trust store has no root for it. Dial `connect_wt(config, "https://127.0.0.1:{port}/nosh")` to the `spawn_wt_server` address. Test 1: run the shell script through the generic `client::run_session_collect` (now `&dyn NoshTransport`) and assert IS_TTY / hello-nosh / size. Test 2: assert `transport.max_datagram_size().is_some()` and exercise the datagram path (mirror `sync.rs`'s observation of a state-diff datagram, or a datagram round-trip if a server echo path is available under test-support). Test 3: spawn a fresh `spawn_wt_server`, then attempt a NATIVE `client::connect(&client_endpoint(...), wt_addr, HOST, Duration::from_secs(3))` and assert it returns `Err` (or that no usable session is reached) — the wtransport endpoint never surfaces a raw-QUIC client as a session. Keep all three tests `#[tokio::test]`, ephemeral ports, with bounded `tokio::time::timeout` so a hang fails loudly rather than blocking CI.
  </action>
  <verify>
    <automated>cargo test -p nosh-client --features webtransport --test webtransport 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `crates/nosh-client/tests/webtransport.rs` contains three `#[tokio::test]` functions covering the shell session (SC#1), datagram-over-WT (SC#2), and downgrade rejection (SC#3).
    - `cargo test -p nosh-client --features webtransport --test webtransport` passes (or cleanly skips when `/bin/sh` is unavailable — never hangs; all waits are timeout-bounded).
    - Test 1 asserts `IS_TTY`, `hello-nosh`, and the size string in the collected output.
    - Test 2 asserts `max_datagram_size().is_some()` and exercises a datagram over the WT session.
    - Test 3 asserts the native-QUIC client does NOT reach a session against the WT-mode server (errors or times out).
  </acceptance_criteria>
  <done>An executable end-to-end test proves a live interactive shell + datagram sync over WebTransport and confirms raw-QUIC downgrade rejection — the Phase 24 win condition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test self-signed cert → test client | The test deliberately accepts a self-signed outer cert; this is a test-only config variant and must never be the default client path |
| test-support auth bypass → test session | The session reaches a shell only because the test-support stub skips inner auth (Phase 25 adds it) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-05-S | Spoofing | downgrade to raw QUIC (WT-05) | mitigate | Test 3 is the adversarial probe: a native-QUIC client against the WT-mode server must fail to reach a session. This is the executable verification of the structural WT-05 claim from Plan 03 |
| T-24-05-E | Elevation of Privilege | test-support bypass leaking to release | accept | The bypass is `#[cfg(any(test, feature = "test-support"))]`-gated (Plan 03); this test enables it via the dev-dep feature only. A release build (no test-support) rejects auth-less connections — out of scope for this test but asserted structurally in Plan 03 |
| T-24-05-D | Denial of Service | test hangs blocking CI | mitigate | Every connect/session wait is wrapped in a bounded `tokio::time::timeout`; tests skip cleanly without `/bin/sh` |
</threat_model>

<verification>
- `cargo test -p nosh-client --features webtransport --test webtransport` passes (three tests) or skips cleanly without `/bin/sh`.
- `cargo test --workspace` (default features) still passes — the new test file is feature-gated and excluded from the default build.
- No test hangs (all waits timeout-bounded).
</verification>

<success_criteria>
An executable integration test demonstrates a live interactive shell with datagram state-sync over WebTransport (SC#1, SC#2) and confirms a raw-QUIC client cannot reach a session against a WebTransport-mode server (SC#3 / WT-05). This is the Phase 24 win condition, proven end-to-end.
</success_criteria>

<output>
Create `.planning/phases/24-webtransport-endpoint-mode-a/24-05-SUMMARY.md` when done.
</output>
