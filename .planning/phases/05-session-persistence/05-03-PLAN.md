---
phase: 05-session-persistence
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified: [crates/nosh-server/src/main.rs]
autonomous: true
requirements: [PERSIST-02]
user_setup: []

must_haves:
  truths:
    - "Implements D-08: --idle-timeout-secs default 0 = disabled (Mosh behavior); D-09: NOSH_IDLE_TIMEOUT_SECS env fallback with CLI-flag precedence over env over the default; D-05: --max-sessions-per-identity default 5 threaded into the registry"
    - "The server accepts --idle-timeout-secs (default 0 = disabled, Mosh behavior) and reads NOSH_IDLE_TIMEOUT_SECS as a fallback when the flag is absent"
    - "An explicit --idle-timeout-secs takes precedence over the NOSH_IDLE_TIMEOUT_SECS env var, which takes precedence over the default of 0"
    - "The server accepts --max-sessions-per-identity (default 5)"
    - "main.rs constructs the SessionRegistry from these values and passes it to run_accept_loop"
  artifacts:
    - path: "crates/nosh-server/src/main.rs"
      provides: "--idle-timeout-secs (+ NOSH_IDLE_TIMEOUT_SECS env), --max-sessions-per-identity, registry construction"
      contains: "idle_timeout_secs"
  key_links:
    - from: "crates/nosh-server/src/main.rs"
      to: "nosh_server::server::run_accept_loop"
      via: "constructs Arc<SessionRegistry> from CLI/env and passes it in"
      pattern: "SessionRegistry::new"
---

<objective>
Add the operator-facing configuration surface for orphaned-session lifetime: a
`--idle-timeout-secs` clap flag (default `0` = disabled, Mosh behavior, D-08) with a
`NOSH_IDLE_TIMEOUT_SECS` environment-variable fallback (D-09), plus a
`--max-sessions-per-identity` flag (default 5, D-05). `main.rs` constructs the
`SessionRegistry` from these values and hands it to `run_accept_loop` (whose signature Plan 02
changed to accept the registry).

Purpose: PERSIST-02 — orphaned-session lifetime must be operator-configurable, defaulting to
disabled, with an env fallback that suits systemd/container deploys.
Output: Updated `crates/nosh-server/src/main.rs`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-session-persistence/05-CONTEXT.md
@.planning/phases/05-session-persistence/05-RESEARCH.md
@.planning/phases/05-session-persistence/05-01-SUMMARY.md
@.planning/phases/05-session-persistence/05-02-SUMMARY.md
@crates/nosh-server/src/main.rs

<interfaces>
<!-- From Plan 01 / Plan 02. Use directly. -->
- `nosh_server::server::run_accept_loop(endpoint, registry: Arc<SessionRegistry>, limits: AuthLimits, shell_override: Option<String>) -> anyhow::Result<()>` (signature set by Plan 02)
- `SessionRegistry::new(max_per_identity: usize, idle_timeout: std::time::Duration) -> Arc<SessionRegistry>`
- `SessionRegistry::DEFAULT_MAX_PER_IDENTITY: usize` (== 5)
- Existing clap pattern in main.rs:16-47 uses `#[arg(long, default_value_t = …)]`; the existing `auth_timeout_secs: u64` field is the closest analog.
- clap's `#[arg(long, env = "NAME", default_value_t = …)]` gives CLI > env > default precedence automatically.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --idle-timeout-secs (+ env fallback) and --max-sessions-per-identity flags</name>
  <files>crates/nosh-server/src/main.rs</files>
  <read_first>crates/nosh-server/src/main.rs, crates/nosh-server/src/registry.rs</read_first>
  <action>In the `Args` struct (main.rs:16), add two fields following the existing `auth_timeout_secs` pattern. (1) `#[arg(long, env = "NOSH_IDLE_TIMEOUT_SECS", default_value_t = 0)] idle_timeout_secs: u64,` with a doc comment: "Idle timeout (seconds) for ORPHANED sessions; 0 = disabled (Mosh behavior, D-08). CLI flag overrides NOSH_IDLE_TIMEOUT_SECS env, which overrides the default 0 (D-09)." The clap `env = "…"` attribute provides the exact CLI > env > default precedence required by D-09 — do NOT hand-roll an env lookup. (2) `#[arg(long, default_value_t = 5)] max_sessions_per_identity: usize,` with a doc comment referencing D-05 (per-identity orphan cap). Keep `use nosh_server::server::{self, AuthLimits};` and add whatever import is needed for `SessionRegistry` (e.g. `use nosh_server::registry::SessionRegistry;` — confirm the path matches the `pub mod registry` declared in lib.rs by Plan 01).</action>
  <verify><automated>cargo build -p nosh-server &amp;&amp; ./target/debug/nosh-server --help 2>&amp;1 | grep -- --idle-timeout-secs</automated></verify>
  <acceptance_criteria>
    - main.rs contains `idle_timeout_secs` with `env = "NOSH_IDLE_TIMEOUT_SECS"` and `default_value_t = 0`
    - main.rs contains `max_sessions_per_identity` with `default_value_t = 5`
    - `nosh-server --help` lists `--idle-timeout-secs` and `--max-sessions-per-identity`
  </acceptance_criteria>
  <done>Both flags exist with correct defaults; idle-timeout has the NOSH_IDLE_TIMEOUT_SECS env fallback via clap's env attribute.</done>
</task>

<task type="auto">
  <name>Task 2: Construct the SessionRegistry from config and pass it to run_accept_loop</name>
  <files>crates/nosh-server/src/main.rs</files>
  <read_first>crates/nosh-server/src/main.rs, crates/nosh-server/src/registry.rs, crates/nosh-server/src/server.rs</read_first>
  <action>In `main()` (main.rs:60), after building `limits` and `endpoint` (around main.rs:85-89): construct `let registry = SessionRegistry::new(args.max_sessions_per_identity, std::time::Duration::from_secs(args.idle_timeout_secs));`. Log the effective config: `tracing::info!(idle_timeout_secs = args.idle_timeout_secs, max_sessions_per_identity = args.max_sessions_per_identity, "session persistence config");` so operators can confirm the resolved values (including any env-sourced idle timeout). Change the final call to `server::run_accept_loop(endpoint, registry, limits, args.shell).await` (registry as the new second argument per Plan 02's signature). Ensure `std::time::Duration` is in scope (main.rs already imports `std::time::Duration` at line 4 — reuse it: `Duration::from_secs(args.idle_timeout_secs)`).</action>
  <verify><automated>cargo build -p nosh-server &amp;&amp; cargo clippy -p nosh-server -- -D warnings</automated></verify>
  <acceptance_criteria>
    - main.rs contains "SessionRegistry::new(args.max_sessions_per_identity" and "Duration::from_secs(args.idle_timeout_secs)"
    - main.rs calls run_accept_loop with the registry argument
    - main.rs logs the resolved idle_timeout_secs and max_sessions_per_identity
    - `cargo build` and `cargo clippy -D warnings` are clean
  </acceptance_criteria>
  <done>main.rs builds the registry from the CLI/env config (idle timeout default 0, cap default 5) and passes it into run_accept_loop.</done>
</task>

<task type="auto">
  <name>Task 3: CLI/env precedence test for the idle-timeout configuration</name>
  <files>crates/nosh-server/src/main.rs</files>
  <read_first>crates/nosh-server/src/main.rs</read_first>
  <action>Add a `#[cfg(test)] mod tests` to main.rs that verifies the clap parsing + env precedence for the idle-timeout config (D-09). Use clap's testing API: `Args::parse_from(["nosh-server", "--idle-timeout-secs", "30"])` → assert `idle_timeout_secs == 30`. For the default-is-zero case: `Args::parse_from(["nosh-server"])` with `NOSH_IDLE_TIMEOUT_SECS` UNSET → assert `idle_timeout_secs == 0`. For env fallback + CLI precedence, prefer parsing via `Args::try_parse_from` with a `clap::Command` built so the env is honored — note that `parse_from` does read env attributes, so set the env var with `std::env::set_var("NOSH_IDLE_TIMEOUT_SECS", "45")`, parse with no flag → assert `== 45`, then parse WITH `--idle-timeout-secs 7` → assert `== 7` (CLI wins), then `std::env::remove_var`. Because env tests share process state, run these assertions within a SINGLE `#[test]` (serialized) to avoid cross-test env races, or gate with a comment noting they must not run in parallel. Also assert `max_sessions_per_identity` defaults to 5. If env-attribute parsing through `parse_from` proves unreliable in the test harness, fall back to asserting the documented precedence by reading the `Arg`'s env via clap's introspection — but the flag-precedence and default assertions (no env) MUST be present regardless.</action>
  <verify><automated>cargo test -p nosh-server --bin nosh-server</automated></verify>
  <acceptance_criteria>
    - main.rs test module asserts idle_timeout_secs default == 0 and parsed flag value is honored
    - main.rs test asserts CLI flag takes precedence over NOSH_IDLE_TIMEOUT_SECS when both are set
    - main.rs test asserts max_sessions_per_identity default == 5
    - `cargo test -p nosh-server --bin nosh-server` passes
  </acceptance_criteria>
  <done>Tests prove --idle-timeout-secs defaults to 0, honors the env fallback, and that the CLI flag overrides the env var (D-08/D-09).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator config (CLI/env) → registry behavior | The idle-timeout and cap values come from the operator (trusted); a misconfigured 0 idle-timeout is intentional (Mosh behavior) but is backstopped by the per-identity cap (Plan 01). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Denial of Service | idle_timeout = 0 (default) | mitigate | The default-disabled idle timeout is bounded by the per-identity orphan cap (Plan 01, default 5) so orphans cannot grow unbounded even with idle timeout off (Pitfall #5). |
| T-05-09 | Tampering | env-sourced config | accept | NOSH_IDLE_TIMEOUT_SECS is read from the server process environment (operator-controlled, e.g. systemd unit); it is not attacker-influenced client input. |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No new crates; clap is already a dependency. |
</threat_model>

<verification>
- [ ] `cargo build -p nosh-server` succeeds
- [ ] `cargo clippy -p nosh-server -- -D warnings` clean
- [ ] `cargo test -p nosh-server --bin nosh-server` passes (CLI/env precedence tests)
- [ ] `nosh-server --help` shows `--idle-timeout-secs` and `--max-sessions-per-identity`
</verification>

<success_criteria>
- --idle-timeout-secs defaults to 0 (disabled), with NOSH_IDLE_TIMEOUT_SECS fallback and CLI precedence
- --max-sessions-per-identity defaults to 5
- main.rs builds the registry from config and passes it to run_accept_loop
- precedence tests pass; clippy clean
</success_criteria>

<output>
After completion, create `.planning/phases/05-session-persistence/05-03-SUMMARY.md`
</output>
