---
context: phase
phase: 25-inner-ssh-key-handshake-tofu-prompt
task: 0
total_tasks: 4
status: in_progress
last_updated: 2026-06-13T18:45:53.167Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Discovered through the plan-checker this run. Acknowledge before proceeding.

- [ ] CONSTRAINT: **Adversarial tests must hit the REAL auth gate, not the bypass** — Phase 24 used a compile-time `cfg(any(test, feature="test-support"))` bypass that is always-on in test builds, so 25's security tests would pass *vacuously*. 25-02 replaced it with a runtime `InnerAuthMode{Required,TestBypass}` (Required is the default even in tests). **Structural mitigation:** 25-04 MUST construct its server via the `InnerAuthMode::Required` real-auth harness (`spawn_wt_server_real_auth`) — never the bypass — and `inner_auth_session_open_before_auth` must FAIL if the bypass were active.

**Do not proceed until checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Vacuous security test | Adversarial inner-auth tests passing against the test-support bypass instead of real auth | blocking | 25-04 uses `InnerAuthMode::Required` harness; a pre-auth SessionOpen test must reject |
| Worktree commits stranded | `Agent(isolation=worktree)` does NOT auto-merge changed worktrees — work sits on `worktree-agent-*` branches until the orchestrator merges | advisory | Merge via `git merge <branch> --no-ff`, restore `.planning/STATE.md`+`ROADMAP.md` from main, then `git worktree remove --force` + `git branch -D` |

<current_state>
`/gsd:autonomous` run, paused (user low on tokens). 2.5 of 6 phases done.
- Phase 23 ✓ (opus 4/4; latency UAT → Phase 28), Phase 24 ✓ (opus 5/5).
- `main` HEAD = `c84c0f1` (Phase 25 plan 25-01 merged).
- Phase 25 Wave 2 BOTH plans complete but UNMERGED on worktree branches:
  - 25-02 server inner auth → `worktree-agent-a43f3996f53301241` (commits bd0cb7a/bba1ff2/ccd0cf7)
  - 25-03 client inner auth + TOFU → `worktree-agent-a22723b4a47a86af1` (commits 06f4515/9b3fe97/60dd1fc)
- Both worktrees on disk, committed, safe. `main` clean.
</current_state>

<completed_work>
- 25-01: wire+crypto foundation — variants 18-21 (append-only), `message_discriminant_order_is_stable` + `inner_auth_fail_is_fieldless` tests, transcript constants in nosh-proto, `NoshTransport::export_keying_material` (default-Err), nosh-auth `check_authorized_key`/`verify_ed25519_spki`. MERGED (c84c0f1). Note: `[u8;64]` sig fields became `Vec<u8>` (serde array limit) — verifier must confirm length-validation at use sites.
- 25-02: `run_inner_auth_server` state machine, EKM channel binding via `quic_connection().export_keying_material`, single-use CSPRNG nonce, spawn_blocking sign, `InnerAuthMode` gate, main.rs Required wiring. DONE (unmerged).
- 25-03: `run_inner_auth_client` (10-step EKM-bound), blocking SEC-02 TOFU prompt, `TofuPolicy::Interactive` default, native pre-connect TOFU (block_in_place), no-TTY fail-closed. 114 nosh-client + 25 nosh-auth lib tests pass. DONE (unmerged).
</completed_work>

<remaining_work>
1. Merge both wave-2 worktrees to main (see anti-pattern row for the exact recipe).
2. Post-merge gate: `cargo build --workspace`; `cargo test --workspace`; `cargo test -p nosh-server --features test-support --lib inner_auth`; `cargo test -p nosh-client --features webtransport --lib`. Then `roadmap.update-plan-progress 25 25-02 complete` + `25-03 complete`.
3. Wave 3 — **25-04** adversarial integration tests on main tree (real-auth harness; tampered-EKM→fail, pre-auth SessionOpen→reject, no-TTY→fail-closed, happy-path mutual-auth shell-independent; convert wt01 to real auth).
4. code-review (sonnet) → code-fixer (sonnet) → **opus verifier** → `gsd-sdk query phase.complete 25`, commit completion.
5. Then Phases 26 (migration handover), 27 (security hardening incl. 999.7 re-verify), 28 (interactive UAT — also clears the carried `23-HUMAN-UAT.md` latency item). Then milestone lifecycle: audit → complete → cleanup.
</remaining_work>

<decisions_made>
- Runtime `InnerAuthMode{Required,TestBypass}` (Required default) replaces the compile-time bypass — plan-checker WARNING-1 fix (revision 742be5f).
- Wave-2 server/client run as parallel worktree agents (disjoint crates).
- Native-QUIC TOFU = pre-connect known_hosts check, NOT inside the rustls verifier (D-09).
</decisions_made>

<blockers>
- None. No human action pending until Phase 28 (live UAT).
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — machine-readable state (task list, branches, next_action).
2. `~/.claude/.../memory/m4-pending-work-and-backlog.md` — full narrative resume state + worktree-merge contract.
3. `.planning/phases/25-inner-ssh-key-handshake-tofu-prompt/25-0{2,3}-SUMMARY.md` (in the respective worktrees until merged) — what each wave-2 plan built.
4. `.planning/phases/25-inner-ssh-key-handshake-tofu-prompt/25-04-PLAN.md` — the adversarial test plan to execute next.

## Infrastructure State
- Two git worktrees present: `.claude/worktrees/agent-a43f3996f53301241` (25-02) and `.claude/worktrees/agent-a22723b4a47a86af1` (25-03). No background processes running.
- Models (balanced profile): planner/verifier=opus; researcher/checker/executor/reviewer/fixer=sonnet.

<context>
Drove plan-phase + execute-phase inline (not via Skill wrappers) to save context. Each phase: research(sonnet)→pattern-map(sonnet)→plan(opus)→plan-check(sonnet)+revision→coverage gates→execute(sonnet, worktrees for parallel waves)→post-merge gate→code-review(sonnet)→fixer(sonnet)→opus verify→phase.complete. Decision-coverage gate greps `D-NN:` tokens in plan must_haves (cite them). Phase 25 plans already passed the checker (8/8 decisions, 2 warnings fixed).
</context>

<next_action>
Start with: merge `worktree-agent-a43f3996f53301241` then `worktree-agent-a22723b4a47a86af1` into main (`git merge <branch> --no-ff`, restore `.planning/STATE.md`+`ROADMAP.md` from main after each), `git worktree remove --force` + `git branch -D` both, then run the post-merge build+test gate.
</next_action>
