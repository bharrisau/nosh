---
phase: 25-inner-ssh-key-handshake-tofu-prompt
plan: 04
type: execute
wave: 3
depends_on: ["25-02", "25-03"]
files_modified:
  - crates/nosh-client/tests/common/mod.rs
  - crates/nosh-client/tests/inner_auth.rs
autonomous: true
requirements: [WT-04, SEC-02]
must_haves:
  truths:
    - "Over WebTransport a client with a key in authorized_keys and a server key it already trusts completes MUTUAL inner auth — run_inner_auth_client returns Ok and the server reaches Authenticated (a subsequent SessionOpen is accepted) — and this proof holds independently of whether a shell is present (WT-04 happy path). A live shell over the authenticated stream is an ADDITIONAL bonus assertion guarded by have_sh()"
    - "An inner-auth exchange whose channel-binding EKM is tampered/relayed (different EKM on one side) FAILS auth — proving the RFC 9266 binding defeats a terminating-proxy MITM (WT-3, adversarial)"
    - "A SessionOpen frame sent before inner auth completes is rejected — the server never dispatches a session in the Unauthenticated state (MH-1, adversarial); the test runs against the real run_inner_auth_server gate (InnerAuthMode::Required), so it would FAIL (hang→timeout) if the bypass were active"
    - "A client key absent from authorized_keys, and a server key the client declines at the TOFU prompt / no-TTY, both fail closed with no oracle and no session (WT-04, SEC-02)"
  artifacts:
    - path: "crates/nosh-client/tests/inner_auth.rs"
      provides: "End-to-end inner-auth + TOFU integration tests over a real-auth (InnerAuthMode::Required) WT server"
      contains: "inner_auth_tampered_channel_binding_fails"
    - path: "crates/nosh-client/tests/common/mod.rs"
      provides: "Updated spawn_wt_server signature + a real-inner-auth (InnerAuthMode::Required) WT server harness"
      contains: "InnerAuthMode::Required"
  key_links:
    - from: "crates/nosh-client/tests/inner_auth.rs"
      to: "nosh_client::inner_auth::run_inner_auth_client"
      via: "tests drive the real client inner-auth path against a real-auth (Required) server"
      pattern: "run_inner_auth_client"
    - from: "crates/nosh-client/tests/common/mod.rs"
      to: "run_wt_accept_loop(.., authorized, host_signer, InnerAuthMode::Required)"
      via: "real-auth harness threads real authorized_keys + host signer + the Required auth mode so adversarial tests hit the real gate"
      pattern: "InnerAuthMode::Required"
---

<objective>
Prove the phase win condition with adversarial integration tests. Update the Phase-24 WT test harness for the new `run_wt_accept_loop` signature, add a real-inner-auth (`InnerAuthMode::Required`) server harness, and write end-to-end tests covering the happy path plus the four security-critical failures: tampered/relayed channel binding (WT-3), SessionOpen-before-auth (MH-1), unknown client key, and the TOFU no-TTY fail-closed (SEC-02). Keep the existing Phase-24 tests green.

Purpose: these tests are the executable proof that the highest-severity mitigations actually hold. The tampered-EKM test proves a terminating proxy cannot silently MITM the inner handshake; the SessionOpen-before-auth test proves the MH-1 state-machine gate. Because Plan 02 removed the compile-time bypass in favour of an explicit `InnerAuthMode` argument, these adversarial tests run against the REAL `run_inner_auth_server` gate (Required) even though nosh-server is built with the `test-support` feature — they are not hollow.
Output: updated `common/mod.rs` harness (new signature + a `spawn_wt_server_real_auth` variant that passes `InnerAuthMode::Required`); a new `inner_auth.rs` integration test file with the happy-path and four adversarial tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/25-inner-ssh-key-handshake-tofu-prompt/25-RESEARCH.md
@.planning/phases/25-inner-ssh-key-handshake-tofu-prompt/25-PATTERNS.md
@.planning/phases/25-inner-ssh-key-handshake-tofu-prompt/25-02-SUMMARY.md
@.planning/phases/25-inner-ssh-key-handshake-tofu-prompt/25-03-SUMMARY.md

<interfaces>
<!-- Harness (crates/nosh-client/tests/common/mod.rs:230-307): -->
<!--   struct WtTestServer { addr, cert_hash, registry, handle }; -->
<!--   spawn_wt_server(shell) calls run_wt_accept_loop(endpoint, registry, AuthLimits::default(), shell) — SIGNATURE NOW NEEDS authorized + host_signer + InnerAuthMode (Plan 02). -->
<!--   Identity::self_signed(["localhost","127.0.0.1"]); cert_hash via certificate_chain().first().hash(). -->
<!-- Existing Phase-24 test file: crates/nosh-client/tests/webtransport.rs (#![cfg(feature="webtransport")]) — uses spawn_wt_server + connect_wt + with_server_certificate_hashes. wt02/wt03 pass InnerAuthMode::TestBypass after Plan 02; wt01 may be real-auth (see 25-02-SUMMARY.md). -->
<!-- From Plan 02: -->
<!--   pub enum InnerAuthMode { Required, TestBypass } in nosh_server::wt_transport. -->
<!--   run_wt_accept_loop(endpoint, registry, AuthLimits, shell, authorized: Arc<Vec<NoshPublicKey>>, host_signer: Arc<dyn RawEd25519Signer>, auth_mode: InnerAuthMode). -->
<!--   With InnerAuthMode::Required (the default), handle_connection_wt runs the REAL run_inner_auth_server gate — there is NO compile-time skip. TestBypass is an explicit per-call opt-in for shell-pump tests only. -->
<!-- From Plan 03: nosh_client::inner_auth::run_inner_auth_client(&dyn NoshTransport, &Path known_hosts, &str host, Arc<dyn RawEd25519Signer>) -> (send, recv) (returns the authenticated control-stream pair; NOTE: supersedes PATTERNS.md's ()-returning sketch). -->
<!-- nosh-auth: InProcessEd25519Signer (build a host + client signer from generated Ed25519 keys); ed25519_spki_der; NoshPublicKey::from_raw/key32; record_known_host; load helpers. -->
<!-- nosh-proto: Message::SessionOpen/InnerAuth* ; write_message_ns/read_message_ns ; NoshTransport::export_keying_material. -->
<!-- Because Plan 02 removed the compile-time bypass, real inner auth runs whenever the harness passes InnerAuthMode::Required — even under the test-support feature. The adversarial tests use the Required harness; no separate test target / cfg gymnastics are needed. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update the WT test harness for the new accept-loop signature + add a real-inner-auth (Required) server</name>
  <files>crates/nosh-client/tests/common/mod.rs</files>
  <read_first>
    - crates/nosh-client/tests/common/mod.rs (the whole `WtTestServer` block lines 230-307; the existing native `spawn_server_*` helpers at 83-130 that already write host_key + authorized_keys files — reuse that key-fixture pattern)
    - crates/nosh-client/tests/webtransport.rs (how `spawn_wt_server` is called by wt01/wt02/wt03 so the update stays source-compatible; note which of them pass InnerAuthMode::TestBypass after Plan 02)
    - 25-02-SUMMARY.md (confirm the final `run_wt_accept_loop` signature and the `InnerAuthMode { Required, TestBypass }` enum; confirm whether wt01 was converted to real auth in Plan 02 or deferred here)
    - 25-PATTERNS.md section "crates/nosh-server/src/main.rs" (the host_signer = InProcessEd25519Signer::from_ssh_private pattern to mirror in the harness)
  </read_first>
  <action>
    Update `spawn_wt_server` so its `run_wt_accept_loop` call passes the new arguments. For the existing bypass-mode tests (wt02/wt03, and wt01 if not converted in Plan 02), supply an empty `authorized = Arc::new(vec![])`, a throwaway in-process host signer (ignored under bypass), and `InnerAuthMode::TestBypass` so those tests keep compiling and passing unchanged.
    Add a new helper `spawn_wt_server_real_auth(shell: Option<String>, authorized: Vec<NoshPublicKey>, host_signer: Arc<dyn RawEd25519Signer>) -> Option<WtTestServer>` that builds the endpoint the same way but passes the REAL `authorized` + `host_signer` AND `InnerAuthMode::Required`, so the production inner-auth gate (`run_inner_auth_server`) runs. Because Plan 02 removed the compile-time `cfg!(any(test, feature = "test-support"))` skip, passing `InnerAuthMode::Required` is sufficient to exercise the real gate even though nosh-server is compiled with the `test-support` feature — no separate test target, no `not(feature = "test-support")` guard, no env-var seam is required. Add a comment in this helper recording exactly that (it is the security-test-integrity contract).
    Expose a small fixture helper that generates an Ed25519 keypair, returns `(Arc<dyn RawEd25519Signer>, NoshPublicKey)`, and writes a `known_hosts` file pre-trusting a given server key (so happy-path tests skip the prompt), reusing the native helpers' tempdir pattern.
  </action>
  <verify>
    <automated>cargo test -p nosh-client --features webtransport --test webtransport --no-run 2>&1 | tail -8 && cargo build -p nosh-client --features webtransport --tests 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p nosh-client --features webtransport --tests` exits 0
    - The existing `webtransport.rs` (wt01/wt02/wt03) tests still compile against the updated `spawn_wt_server`
    - `grep -n 'spawn_wt_server_real_auth\|InnerAuthMode::Required' crates/nosh-client/tests/common/mod.rs` shows the real-auth harness exists and passes `InnerAuthMode::Required`
    - The real-auth harness threads non-empty `authorized` + a real host signer + `InnerAuthMode::Required` into `run_wt_accept_loop` (so `run_inner_auth_server` actually runs — not the synthetic-identity bypass)
    - No `not(feature = "test-support")` cfg gate or env-var seam is introduced — the real gate is reached purely via `InnerAuthMode::Required` (security-test-integrity contract from 25-02)
  </acceptance_criteria>
  <done>Harness compiles against the new signature; a real-inner-auth (Required) server variant + key/known_hosts fixtures exist; Phase-24 tests unaffected.</done>
</task>

<task type="auto">
  <name>Task 2: Adversarial + happy-path inner-auth integration tests (against the real Required gate)</name>
  <files>crates/nosh-client/tests/inner_auth.rs</files>
  <read_first>
    - crates/nosh-client/tests/webtransport.rs (the `connect_wt` + `with_server_certificate_hashes` dial pattern, the `tokio::time::timeout` discipline, the `have_sh()` skip guard, the live-shell assertion helper used by wt01)
    - crates/nosh-client/tests/common/mod.rs (the Task-1 real-auth harness + fixtures)
    - 25-03-SUMMARY.md (the final `run_inner_auth_client` signature + how the WT client path sends SessionOpen on the returned authenticated stream)
    - 25-02-SUMMARY.md (confirm the real gate runs purely via `InnerAuthMode::Required`)
    - 25-RESEARCH.md "Phase Requirements → Test Map" (the named test cases) and "Pattern 1: Transcript Layout" (to construct a tampered-EKM case)
  </read_first>
  <action>
    Create `crates/nosh-client/tests/inner_auth.rs` (`#![cfg(feature = "webtransport")]`, matching webtransport.rs) with `tokio::time::timeout` on every await. Every test uses the `spawn_wt_server_real_auth` harness (InnerAuthMode::Required) so it hits the real gate. Implement:
    (1) `inner_auth_happy_path`: real-auth server with the client key in authorized_keys + a known_hosts pre-trusting the server key; `connect_wt` then `run_inner_auth_client` returns Ok. ASSERT MUTUAL-AUTH COMPLETION INDEPENDENTLY OF have_sh(): `run_inner_auth_client` returning Ok proves the client verified the server's completion signature, AND the test must prove the server reached Authenticated — send a `SessionOpen` on the returned authenticated stream and assert the server ACCEPTS it (a session is dispatched / no InnerAuthFail / no protocol-close), which is impossible unless the server is in the Authenticated state (MH-1). This block runs with NO have_sh() guard — it is the WT-04 proof. THEN, as an ADDITIONAL bonus assertion guarded by `if have_sh()`, drive a live shell over that authenticated stream (reuse wt01's assertion) and assert it works. If sh is absent the bonus is skipped but the WT-04 mutual-auth proof above has already run.
    (2) `inner_auth_tampered_channel_binding_fails` (WT-3, the headline adversarial test): drive a client-side inner-auth exchange where the EKM the client signs over does NOT match the server's EKM — simulate the relayed-proxy condition by having the test client feed a deliberately altered EKM into the transcript (or use a custom in-test transport whose `export_keying_material` returns a different 32 bytes than the server's), then assert the server rejects with `InnerAuthFail` and no session is established. The test MUST fail if the EKM were dropped from the transcript (i.e. it genuinely exercises the binding). Runs against the Required gate.
    (3) `inner_auth_session_open_before_auth` (MH-1): against the `InnerAuthMode::Required` server, open the control stream and send `Message::SessionOpen{..}` as the FIRST frame (skipping the inner-auth exchange); assert the server closes the connection / never yields a session — `SessionOpen` is rejected in the Unauthenticated state. Because the server runs the real `run_inner_auth_server` gate (Required), the server is waiting for an `InnerAuthResponse`; a `SessionOpen` first frame is the wrong frame → fieldless `InnerAuthFail` + close. This test would FAIL (the SessionOpen would be accepted, or the await would hang until the test's `tokio::time::timeout` fired) if the gate were bypassed — assert against the timeout/close, NOT a session.
    (4) `inner_auth_unknown_client_key`: real-auth server whose authorized_keys does NOT contain the client key; assert `run_inner_auth_client` (or the exchange) ends with `InnerAuthFail` / Err and no session, with no distinguishing detail on the wire (the failure is the fieldless variant).
    (5) `tofu_no_tty_fails_closed` (SEC-02 / D-10): with an unknown server key and a non-TTY stdin in the test process, assert the client TOFU path fails closed (Err / refusal) and never records the key to known_hosts. If forcing non-TTY in-process is impractical, assert via the unit-level prompt helper that the no-TTY branch returns false AND add an integration assertion that an unknown key with no prior known_hosts entry does not produce a session.
    Each adversarial test must assert the SECURITY outcome (no session / fieldless failure), not merely that an error occurred.
  </action>
  <verify>
    <automated>cargo test -p nosh-client --features webtransport --test inner_auth 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p nosh-client --features webtransport --test inner_auth` passes all five tests
    - Every test constructs the server via `spawn_wt_server_real_auth` (InnerAuthMode::Required) — `grep -c 'spawn_wt_server_real_auth' crates/nosh-client/tests/inner_auth.rs` is ≥ 5; no test uses the TestBypass harness (so none can pass vacuously)
    - `inner_auth_happy_path` asserts MUTUAL-AUTH COMPLETION (run_inner_auth_client Ok + server accepts a subsequent SessionOpen) OUTSIDE any have_sh() guard — the WT-04 proof does not depend on a shell; the live-shell check is an additional have_sh()-guarded assertion
    - `inner_auth_tampered_channel_binding_fails` proves a mismatched EKM causes auth failure and would FAIL if the EKM were not part of the signed transcript (WT-3 mitigation proven)
    - `inner_auth_session_open_before_auth` proves a pre-auth `SessionOpen` is rejected against the Required gate, and would FAIL (SessionOpen accepted, or hang→timeout) if the bypass were active — i.e. it genuinely exercises `run_inner_auth_server`, not the synthetic-identity stub
    - `inner_auth_unknown_client_key` proves an unauthorized client gets the fieldless failure and no session
    - `tofu_no_tty_fails_closed` proves no-TTY unknown-key fails closed and does not record to known_hosts (SEC-02/D-10)
    - Every test uses `tokio::time::timeout` so a hang fails loudly
    - `cargo test --workspace` (default features) still passes — the new file does not break the default build
  </acceptance_criteria>
  <done>Five integration tests prove the happy path (shell-independent mutual-auth completion + a bonus live shell) and the four security-critical failures against the real Required gate; the WT-3 and MH-1 mitigations are executable evidence that cannot pass vacuously; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Adversarial test client → real-auth server | Tests stand in for a malicious/relaying peer to prove the gate holds |
| Test build → production gate | The test-support bypass must not let adversarial tests pass vacuously; tests use InnerAuthMode::Required so they hit the real gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-04-WT3 | Spoofing | tampered channel binding | mitigate | `inner_auth_tampered_channel_binding_fails` asserts a mismatched EKM → auth failure + no session; designed to fail if EKM were dropped from the transcript |
| T-25-04-MH1 | Elevation of privilege | pre-auth SessionOpen | mitigate | `inner_auth_session_open_before_auth` asserts the server rejects SessionOpen in the Unauthenticated state, against the real Required gate |
| T-25-04-ORACLE | Information disclosure | failure uniformity | mitigate | `inner_auth_unknown_client_key` asserts the fieldless failure + no session (no key-existence oracle) |
| T-25-04-NOTTY | Spoofing | TOFU no-TTY | mitigate | `tofu_no_tty_fails_closed` asserts fail-closed + no known_hosts record (SEC-02/D-10) |
| T-25-04-VACUOUS | Tampering | test-support bypass masking the gate | mitigate | All adversarial tests use `spawn_wt_server_real_auth` (InnerAuthMode::Required) so they exercise `run_inner_auth_server`, not the synthetic-identity TestBypass stub; acceptance criteria grep-confirm ≥5 Required-harness uses and zero bypass uses |
| T-25-04-SC | Tampering | cargo installs | accept | No new packages introduced by this plan (tests only) |
</threat_model>

<verification>
- `cargo test -p nosh-client --features webtransport --test inner_auth` green (5 tests).
- `cargo test -p nosh-client --features webtransport --test webtransport` still green (Phase-24 unaffected).
- `cargo test --workspace` green.
- The happy-path mutual-auth proof runs independently of have_sh(); the tampered-EKM and pre-auth-SessionOpen tests assert security outcomes against the real Required gate, not generic errors, and would fail if the bypass were active.
</verification>

<success_criteria>
- Happy path: mutual inner auth proven (run_inner_auth_client Ok + server accepts SessionOpen) shell-independently; live shell is a bonus when have_sh().
- WT-3 proven: tampered/relayed channel binding fails auth.
- MH-1 proven: pre-auth SessionOpen rejected against the real Required gate.
- SEC-02 proven: no-TTY unknown key fails closed; unauthorized client gets opaque failure.
</success_criteria>

<output>
Create `.planning/phases/25-inner-ssh-key-handshake-tofu-prompt/25-04-SUMMARY.md` when done.
</output>
