---
phase: 25-inner-ssh-key-handshake-tofu-prompt
plan: 04
type: execute
wave: 3
depends_on: ["25-02", "25-03"]
files_modified:
  - crates/nosh-client/tests/common/mod.rs
  - crates/nosh-client/tests/inner_auth.rs
autonomous: true
requirements: [WT-04, SEC-02]
must_haves:
  truths:
    - "A WebTransport client with a key in authorized_keys and a server key it already trusts completes mutual inner auth and runs a live shell session (WT-04 happy path)"
    - "An inner-auth exchange whose channel-binding EKM is tampered/relayed (different EKM on one side) FAILS auth — proving the RFC 9266 binding defeats a terminating-proxy MITM (WT-3, adversarial)"
    - "A SessionOpen frame sent before inner auth completes is rejected — the server never dispatches a session in the Unauthenticated state (MH-1, adversarial)"
    - "A client key absent from authorized_keys, and a server key the client declines at the TOFU prompt / no-TTY, both fail closed with no oracle and no session (WT-04, SEC-02)"
  artifacts:
    - path: "crates/nosh-client/tests/inner_auth.rs"
      provides: "End-to-end inner-auth + TOFU integration tests over a real-auth WT server"
      contains: "inner_auth_tampered_channel_binding_fails"
    - path: "crates/nosh-client/tests/common/mod.rs"
      provides: "Updated spawn_wt_server signature + a real-inner-auth WT server harness"
      contains: "run_wt_accept_loop"
  key_links:
    - from: "crates/nosh-client/tests/inner_auth.rs"
      to: "nosh_client::inner_auth::run_inner_auth_client"
      via: "tests drive the real client inner-auth path against a real-auth server"
      pattern: "run_inner_auth_client"
    - from: "crates/nosh-client/tests/common/mod.rs"
      to: "run_wt_accept_loop(.., authorized, host_signer)"
      via: "harness threads real authorized_keys + host signer for the adversarial tests"
      pattern: "host_signer"
---

<objective>
Prove the phase win condition with adversarial integration tests. Update the Phase-24 WT test harness for the new `run_wt_accept_loop` signature, add a real-inner-auth server harness, and write end-to-end tests covering the happy path plus the four security-critical failures: tampered/relayed channel binding (WT-3), SessionOpen-before-auth (MH-1), unknown client key, and the TOFU no-TTY fail-closed (SEC-02). Also keep the existing Phase-24 test-support-bypass tests green.

Purpose: these tests are the executable proof that the highest-severity mitigations actually hold. The tampered-EKM test is the one that proves a terminating proxy cannot silently MITM the inner handshake; the SessionOpen-before-auth test proves the MH-1 state-machine gate.
Output: updated `common/mod.rs` harness (new signature + a `spawn_wt_server_real_auth` variant); a new `inner_auth.rs` integration test file with the happy-path and four adversarial tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/25-inner-ssh-key-handshake-tofu-prompt/25-RESEARCH.md
@.planning/phases/25-inner-ssh-key-handshake-tofu-prompt/25-PATTERNS.md
@.planning/phases/25-inner-ssh-key-handshake-tofu-prompt/25-02-SUMMARY.md
@.planning/phases/25-inner-ssh-key-handshake-tofu-prompt/25-03-SUMMARY.md

<interfaces>
<!-- Harness (crates/nosh-client/tests/common/mod.rs:230-307): -->
<!--   struct WtTestServer { addr, cert_hash, registry, handle }; -->
<!--   spawn_wt_server(shell) calls run_wt_accept_loop(endpoint, registry, AuthLimits::default(), shell) — SIGNATURE NOW NEEDS authorized + host_signer. -->
<!--   Identity::self_signed(["localhost","127.0.0.1"]); cert_hash via certificate_chain().first().hash(). -->
<!-- Existing Phase-24 test file: crates/nosh-client/tests/webtransport.rs (#![cfg(feature="webtransport")]) — uses spawn_wt_server + connect_wt + with_server_certificate_hashes. -->
<!-- From Plan 02: run_wt_accept_loop(endpoint, registry, AuthLimits, shell, authorized: Arc<Vec<NoshPublicKey>>, host_signer: Arc<dyn RawEd25519Signer>). -->
<!--   handle_connection_wt under test/test-support bypasses inner auth (synthetic identity) — existing wt01/wt02/wt03 tests keep passing. -->
<!-- From Plan 03: nosh_client::inner_auth::run_inner_auth_client(&dyn NoshTransport, &Path known_hosts, &str host, Arc<dyn RawEd25519Signer>) -> (send, recv). -->
<!-- nosh-auth: InProcessEd25519Signer (build a host + client signer from generated Ed25519 keys); ed25519_spki_der; NoshPublicKey::from_raw/key32; record_known_host; load helpers. -->
<!-- nosh-proto: Message::SessionOpen/InnerAuth* ; write_message_ns/read_message_ns ; NoshTransport::export_keying_material. -->
<!-- To run REAL inner auth in tests, the server harness must build WITHOUT the test-support bypass for those cases — see action note on a dedicated test binary / cfg. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update the WT test harness for the new accept-loop signature + add a real-inner-auth server</name>
  <files>crates/nosh-client/tests/common/mod.rs</files>
  <read_first>
    - crates/nosh-client/tests/common/mod.rs (the whole `WtTestServer` block lines 230-307; the existing native `spawn_server_*` helpers at 83-130 that already write host_key + authorized_keys files — reuse that key-fixture pattern)
    - crates/nosh-client/tests/webtransport.rs (how `spawn_wt_server` is called by wt01/wt02/wt03 so the update stays source-compatible)
    - 25-02-SUMMARY.md (the exact final `run_wt_accept_loop` signature + whether the test-support bypass is keyed on `feature = "test-support"` vs `cfg(test)`)
    - 25-PATTERNS.md section "crates/nosh-server/src/main.rs" (the host_signer = InProcessEd25519Signer::from_ssh_private pattern to mirror in the harness)
  </read_first>
  <action>
    Update `spawn_wt_server` so its `run_wt_accept_loop` call passes the two new arguments. For the existing bypass-mode tests, supply an empty `authorized = Arc::new(vec![])` and a throwaway in-process host signer (the bypass path ignores them) so wt01/wt02/wt03 keep compiling and passing unchanged.
    Add a new helper `spawn_wt_server_real_auth(shell: Option<String>, authorized: Vec<NoshPublicKey>, host_signer: Arc<dyn RawEd25519Signer>) -> Option<WtTestServer>` that builds the endpoint the same way but threads the REAL `authorized` + `host_signer` so the production inner-auth path runs. Because `handle_connection_wt` bypasses inner auth whenever `cfg(any(test, feature = "test-support"))` is active, the real-auth tests cannot exercise the production gate from the default test build — resolve this by gating the adversarial tests behind a dedicated path: build the real-auth tests so the server runs the production gate. Confirm from 25-02-SUMMARY.md exactly how the bypass is keyed; if it is `feature = "test-support"`, run the real-auth integration tests WITHOUT that feature (a separate test target / `#![cfg(all(feature = "webtransport", not(feature = "test-support")))]` guard on the new file), and document the run command. If the bypass is `cfg(test)` (unconditionally on for any test), coordinate via the SUMMARY's documented seam (e.g. an env-var or a `#[cfg(test)]`-only override the harness can set to force real auth) and use that seam here. Pick the approach the SUMMARY supports; do not weaken the production gate.
    Expose a small fixture helper that generates an Ed25519 keypair, returns `(Arc<dyn RawEd25519Signer>, NoshPublicKey)`, and writes a `known_hosts` file pre-trusting a given server key (so happy-path tests skip the prompt), reusing the native helpers' tempdir pattern.
  </action>
  <verify>
    <automated>cargo test -p nosh-client --features webtransport --test webtransport --no-run 2>&1 | tail -8 && cargo build -p nosh-client --features webtransport --tests 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p nosh-client --features webtransport --tests` exits 0
    - The existing `webtransport.rs` (wt01/wt02/wt03) tests still compile against the updated `spawn_wt_server`
    - `grep -n 'host_signer\|spawn_wt_server_real_auth' crates/nosh-client/tests/common/mod.rs` shows the real-auth harness exists
    - The real-auth harness threads non-empty `authorized` + a real host signer into `run_wt_accept_loop`
    - The chosen real-auth test seam matches what 25-02-SUMMARY.md documents (no weakening of the release-build inner-auth gate)
  </acceptance_criteria>
  <done>Harness compiles against the new signature; a real-inner-auth server variant + key/known_hosts fixtures exist; Phase-24 tests unaffected.</done>
</task>

<task type="auto">
  <name>Task 2: Adversarial + happy-path inner-auth integration tests</name>
  <files>crates/nosh-client/tests/inner_auth.rs</files>
  <read_first>
    - crates/nosh-client/tests/webtransport.rs (the `connect_wt` + `with_server_certificate_hashes` dial pattern, the `tokio::time::timeout` discipline, the `have_sh()` skip guard, the live-shell assertion helper used by wt01)
    - crates/nosh-client/tests/common/mod.rs (the Task-1 real-auth harness + fixtures)
    - 25-03-SUMMARY.md (the final `run_inner_auth_client` signature + how the WT client path sends SessionOpen on the returned authenticated stream)
    - 25-RESEARCH.md "Phase Requirements → Test Map" (the named test cases) and "Pattern 1: Transcript Layout" (to construct a tampered-EKM case)
  </read_first>
  <action>
    Create `crates/nosh-client/tests/inner_auth.rs` (guarded with the same cfg the harness real-auth path requires, per Task 1) with `tokio::time::timeout` on every await and `have_sh()` skips where a shell is needed. Implement:
    (1) `inner_auth_happy_path`: real-auth server with the client key in authorized_keys + a known_hosts pre-trusting the server key; `connect_wt` then `run_inner_auth_client` returns Ok; send `SessionOpen` on the returned stream and assert a live shell works (reuse wt01's assertion). Proves WT-04 happy path.
    (2) `inner_auth_tampered_channel_binding_fails` (WT-3, the headline adversarial test): drive a client-side inner-auth exchange where the EKM the client signs over does NOT match the server's EKM — simulate the relayed-proxy condition by having the test client feed a deliberately altered EKM into the transcript (or use a custom in-test transport whose `export_keying_material` returns a different 32 bytes than the server's), then assert the server rejects with `InnerAuthFail` and no session is established. The test MUST fail if the EKM were dropped from the transcript (i.e. it genuinely exercises the binding).
    (3) `inner_auth_session_open_before_auth` (MH-1): open the control stream and send `Message::SessionOpen{..}` as the FIRST frame (skipping the inner-auth exchange); assert the server closes the connection / never yields a session — `SessionOpen` is rejected in the Unauthenticated state.
    (4) `inner_auth_unknown_client_key`: real-auth server whose authorized_keys does NOT contain the client key; assert `run_inner_auth_client` (or the exchange) ends with `InnerAuthFail` / Err and no session, with no distinguishing detail on the wire (the failure is the fieldless variant).
    (5) `tofu_no_tty_fails_closed` (SEC-02 / D-10): with an unknown server key and a non-TTY stdin in the test process, assert the client TOFU path fails closed (Err / refusal) and never records the key to known_hosts. If forcing non-TTY in-process is impractical, assert via the unit-level prompt helper that the no-TTY branch returns false AND add an integration assertion that an unknown key with no prior known_hosts entry does not produce a session.
    Each adversarial test must assert the SECURITY outcome (no session / fieldless failure), not merely that an error occurred.
  </action>
  <verify>
    <automated>cargo test -p nosh-client --features webtransport --test inner_auth 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p nosh-client --features webtransport --test inner_auth` passes all five tests (using the documented run command/feature combo from Task 1)
    - `inner_auth_tampered_channel_binding_fails` proves a mismatched EKM causes auth failure and would FAIL if the EKM were not part of the signed transcript (WT-3 mitigation proven)
    - `inner_auth_session_open_before_auth` proves a pre-auth `SessionOpen` is rejected (MH-1)
    - `inner_auth_unknown_client_key` proves an unauthorized client gets the fieldless failure and no session
    - `tofu_no_tty_fails_closed` proves no-TTY unknown-key fails closed and does not record to known_hosts (SEC-02/D-10)
    - Every test uses `tokio::time::timeout` so a hang fails loudly
    - `cargo test --workspace` (default features) still passes — the new file does not break the default build
  </acceptance_criteria>
  <done>Five integration tests prove the happy path and the four security-critical failures; the WT-3 and MH-1 mitigations are executable evidence; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Adversarial test client → real-auth server | Tests stand in for a malicious/relaying peer to prove the gate holds |
| Test build → production gate | The test-support bypass must not let adversarial tests pass vacuously against a bypassed gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-04-WT3 | Spoofing | tampered channel binding | mitigate | `inner_auth_tampered_channel_binding_fails` asserts a mismatched EKM → auth failure + no session; designed to fail if EKM were dropped from the transcript |
| T-25-04-MH1 | Elevation of privilege | pre-auth SessionOpen | mitigate | `inner_auth_session_open_before_auth` asserts the server rejects SessionOpen in the Unauthenticated state |
| T-25-04-ORACLE | Information disclosure | failure uniformity | mitigate | `inner_auth_unknown_client_key` asserts the fieldless failure + no session (no key-existence oracle) |
| T-25-04-NOTTY | Spoofing | TOFU no-TTY | mitigate | `tofu_no_tty_fails_closed` asserts fail-closed + no known_hosts record (SEC-02/D-10) |
| T-25-04-VACUOUS | Tampering | test-support bypass masking the gate | mitigate | Real-auth tests run via the seam documented in 25-02-SUMMARY (without the bypass) so they exercise the production gate, not the synthetic-identity stub |
| T-25-04-SC | Tampering | cargo installs | accept | No new packages introduced by this plan (tests only) |
</threat_model>

<verification>
- `cargo test -p nosh-client --features webtransport --test inner_auth` green (5 tests).
- `cargo test -p nosh-client --features webtransport --test webtransport` still green (Phase-24 unaffected).
- `cargo test --workspace` green.
- The tampered-EKM and pre-auth-SessionOpen tests assert security outcomes, not generic errors.
</verification>

<success_criteria>
- Happy path: mutual inner auth + live shell over WebTransport.
- WT-3 proven: tampered/relayed channel binding fails auth.
- MH-1 proven: pre-auth SessionOpen rejected.
- SEC-02 proven: no-TTY unknown key fails closed; unauthorized client gets opaque failure.
</success_criteria>

<output>
Create `.planning/phases/25-inner-ssh-key-handshake-tofu-prompt/25-04-SUMMARY.md` when done.
</output>
