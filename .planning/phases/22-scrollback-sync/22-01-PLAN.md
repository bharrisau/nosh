---
phase: 22-scrollback-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/nosh-proto/src/codec.rs
  - crates/nosh-proto/src/messages.rs
  - crates/nosh-server/src/terminal.rs
autonomous: true
requirements: [SCROLL-01, SCROLL-02, SCROLL-03, SCROLL-05]
must_haves:
  truths:
    - "message_discriminant_order_is_stable pins discriminants 15/16/17 for the three scrollback variants before any wire type is read by other code"
    - "ScrollbackRequest, ScrollbackPage (with epoch_at_snapshot), and ScrollbackCredit round-trip exactly through write_message/read_message"
    - "TerminalState exposes a read accessor that returns a contiguous page of scrollback lines plus the total available count, without exposing the VecDeque"
    - "Alt-screen scroll lines never enter TerminalState.scrollback (verified by unit test)"
  artifacts:
    - path: "crates/nosh-proto/src/messages.rs"
      provides: "ScrollbackRequest / ScrollbackPage / ScrollbackCredit Message variants + ScrollbackLine wire type"
      contains: "ScrollbackPage"
    - path: "crates/nosh-proto/src/codec.rs"
      provides: "Discriminant-stability + round-trip coverage for variants 15-17"
      contains: "ScrollbackRequest"
    - path: "crates/nosh-server/src/terminal.rs"
      provides: "scrollback_lines accessor + scrollback_excludes_alt_screen unit test"
      contains: "scrollback_lines"
  key_links:
    - from: "crates/nosh-proto/src/messages.rs"
      to: "crates/nosh-proto/src/codec.rs"
      via: "variant_name() match arms + discriminant test cases"
      pattern: "ScrollbackPage"
---

<objective>
Lay the Phase 22 wire-protocol foundation and the server-side scrollback read accessor, with the discriminant-stability test extended FIRST (verify-before-build) so the postcard wire format cannot be silently corrupted. Also add the SCROLL-03 alt-screen-exclusion verification unit test (the gate already exists in `scroll_up()`/`resize()` — this proves it, does not re-implement it).

Purpose: Every later plan (server sender task, client view) consumes these wire types and the accessor. Defining the contracts first means the executors of plans 02 and 03 receive the exact type shapes in their plan context — no codebase scavenger hunt.
Output: Three new append-only `Message` variants (discriminants 15/16/17), a `ScrollbackLine` payload type, a `TerminalState::scrollback_lines` accessor, extended codec tests, and the `scrollback_excludes_alt_screen` unit test.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-scrollback-sync/22-CONTEXT.md
@.planning/phases/22-scrollback-sync/22-RESEARCH.md
@.planning/phases/22-scrollback-sync/22-PATTERNS.md

<interfaces>
<!-- Current last-stable wire state (verified in codec.rs at planning time). -->
<!-- ChannelClose is discriminant 14 — the three new variants are 15, 16, 17. -->

Existing analog (Phase 21) in messages.rs:
  ChannelOpen { channel_id: u32, channel_type: ChannelType }
  ChannelAccept { channel_id: u32 }
  ChannelReject { channel_id: u32 }
  ChannelCredit { channel_id: u32, bytes: u64 }   // discriminant 13
  ChannelClose { channel_id: u32 }                // discriminant 14 (current last)

Existing TerminalState (terminal.rs):
  scrollback: VecDeque<Vec<Cell>>   // [0]=oldest, [len-1]=newest (just above viewport)
  const SCROLLBACK_LINE_CAP: usize = 10_000
  scroll_up() alt-screen gate at ~line 631 (DO NOT MODIFY)
  resize() alt-screen gate (D-19-09) at ~line 489 (DO NOT MODIFY)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend discriminant-stability + round-trip tests for variants 15-17 (FIRST COMMIT)</name>
  <files>crates/nosh-proto/src/codec.rs</files>
  <read_first>
    - crates/nosh-proto/src/codec.rs (the `message_discriminant_order_is_stable` test ~lines 276-306 and `mux_variants_round_trip` ~lines 311-343)
    - crates/nosh-proto/src/messages.rs (the Message enum + Phase 21 mux variant block ~lines 177-236, and `variant_name()` ~lines 311-315)
    - .planning/phases/22-scrollback-sync/22-PATTERNS.md (codec.rs section, lines 71-119)
  </read_first>
  <behavior>
    - message_discriminant_order_is_stable asserts Message::ScrollbackRequest encodes with leading byte 15
    - message_discriminant_order_is_stable asserts Message::ScrollbackPage encodes with leading byte 16
    - message_discriminant_order_is_stable asserts Message::ScrollbackCredit encodes with leading byte 17
    - mux_variants_round_trip includes the three new variants and asserts exact equality after write_message -> read_message
  </behavior>
  <action>
    Extend the `message_discriminant_order_is_stable` test in codec.rs: after the existing `(14, Message::ChannelClose { channel_id: 2 })` case, append three cases — `(15, Message::ScrollbackRequest { channel_id: 2, from_line: 0, count: 256 })`, `(16, Message::ScrollbackPage { channel_id: 2, from_line: 0, total_available: 0, epoch_at_snapshot: 0, lines: vec![] })`, `(17, Message::ScrollbackCredit { channel_id: 2, bytes: 0 })`. Add the same three variants to the `mux_variants_round_trip` `msgs` array. These reference the new variant names/fields that Task 2 will create, so this test will NOT COMPILE until Task 2 lands — that is intentional (verify-before-build: the test is the spec for the wire shape). Commit this test change as the first commit of the phase even though the crate does not yet build; the field layout written here (`channel_id`/`from_line`/`count` on Request; `channel_id`/`from_line`/`total_available`/`epoch_at_snapshot`/`lines` on Page; `channel_id`/`bytes` on Credit) is the binding contract Task 2 must match.
  </action>
  <verify>
    <automated>grep -q "ScrollbackPage { channel_id: 2, from_line: 0, total_available: 0, epoch_at_snapshot: 0, lines: vec!\[\] }" crates/nosh-proto/src/codec.rs && grep -q "(15, Message::ScrollbackRequest" crates/nosh-proto/src/codec.rs && echo OK</automated>
  </verify>
  <done>codec.rs contains discriminant cases 15/16/17 referencing the three scrollback variants and the round-trip array includes them. (Crate intentionally does not build yet — Task 2 supplies the variants.)</done>
  <acceptance_criteria>
    - codec.rs `message_discriminant_order_is_stable` contains exactly three new cases keyed 15, 16, 17 in that order, immediately after the discriminant-14 case
    - The three cases use the field names `from_line`, `count`, `total_available`, `epoch_at_snapshot`, `lines`, `bytes` as written above (this is the contract for Task 2)
    - `mux_variants_round_trip` `msgs` array includes ScrollbackRequest, ScrollbackPage, ScrollbackCredit
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Append ScrollbackRequest / ScrollbackPage / ScrollbackCredit + ScrollbackLine wire types</name>
  <files>crates/nosh-proto/src/messages.rs</files>
  <read_first>
    - crates/nosh-proto/src/messages.rs (Message enum, Phase 21 mux variant block ~177-236, `variant_name()` ~311-315, the existing Cell/style serialisation if Cell is referenced — search for how cells/rows are serialised in datagram.rs)
    - crates/nosh-proto/src/datagram.rs (StateDiff.epoch: u64 — the field type the client compares epoch_at_snapshot against; and any existing Cell wire representation)
    - .planning/phases/22-scrollback-sync/22-RESEARCH.md (Pattern 1, lines 184-240)
    - .planning/phases/22-scrollback-sync/22-PATTERNS.md (messages.rs section lines 26-67)
  </read_first>
  <behavior>
    - The three new variants are appended AFTER ChannelClose, becoming discriminants 15, 16, 17 (Task 1's test passes once this lands)
    - Each variant derives the same serde Serialize/Deserialize + PartialEq + Debug as existing Message variants
    - variant_name() returns "ScrollbackRequest" / "ScrollbackPage" / "ScrollbackCredit"
  </behavior>
  <action>
    In messages.rs, immediately after `ChannelClose { channel_id: u32 }` (the current last variant, discriminant 14), append three variants — APPEND-ONLY, never insert. Add a doc comment `// Phase 22: Scrollback Sync — append-only after ChannelClose (discriminant 14).`
    `ScrollbackRequest { channel_id: u32, from_line: u64, count: u32 }` — client to server, `from_line` indexed from newest line backwards (0 = line just above the live viewport).
    `ScrollbackPage { channel_id: u32, from_line: u64, total_available: u64, epoch_at_snapshot: u64, lines: Vec<ScrollbackLine> }` — server to client; `epoch_at_snapshot` (LOCKED, S-5) is the live-grid epoch captured atomically with the line snapshot; `total_available` lets the client detect top-of-history.
    `ScrollbackCredit { channel_id: u32, bytes: u64 }` — byte-granular credit grant, same shape as `ChannelCredit`.
    Define a `ScrollbackLine` type carrying per-line content with its original column width as per-line metadata (S-3 discretion default: send original width, no server-side reflow). Reuse the existing cell/row serde representation that `StateDiff` uses if one exists; otherwise define `ScrollbackLine { width: u16, cells: Vec<...> }` matching the codebase's existing cell wire encoding. Keep `lines: vec![]` valid (empty page allowed).
    Extend `variant_name()` with the three arms returning "ScrollbackRequest", "ScrollbackPage", "ScrollbackCredit". Do NOT add any field to or reorder any existing variant.
  </action>
  <verify>
    <automated>cargo test -p nosh-proto message_discriminant_order_is_stable && cargo test -p nosh-proto mux_variants_round_trip</automated>
  </verify>
  <done>nosh-proto compiles; both discriminant-stability and round-trip tests pass with the three new variants at discriminants 15/16/17.</done>
  <acceptance_criteria>
    - `cargo test -p nosh-proto message_discriminant_order_is_stable` exits 0
    - `cargo test -p nosh-proto mux_variants_round_trip` exits 0
    - messages.rs contains `ScrollbackPage` with field `epoch_at_snapshot: u64`
    - No existing Message variant's field list or order changed (discriminants 0-14 unchanged)
    - `ScrollbackLine` carries original per-line width metadata (S-3)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add TerminalState::scrollback_lines accessor + scrollback_excludes_alt_screen unit test</name>
  <files>crates/nosh-server/src/terminal.rs</files>
  <read_first>
    - crates/nosh-server/src/terminal.rs (scrollback field ~line 193, SCROLLBACK_LINE_CAP, scroll_up() alt-screen gate ~lines 626-640, resize() gate ~lines 481-496, TerminalState::new and advance/alt-screen activation path)
    - .planning/phases/22-scrollback-sync/22-PATTERNS.md (terminal.rs section, lines 123-201)
    - .planning/phases/22-scrollback-sync/22-RESEARCH.md (Pattern 2, lines 242-277; Code Examples scroll_up gate lines 472-491)
  </read_first>
  <behavior>
    - scrollback_lines(0, N) returns the N newest scrollback lines in display order (oldest-first within the page) plus total_available = scrollback.len()
    - scrollback_lines(from_line >= total, _) returns (vec![], total) — no panic
    - scrollback_lines(_, 0) returns (vec![], total) — no panic
    - from_line = u64::MAX returns (vec![], total) — no overflow, no panic (V5 input validation)
    - scrollback_excludes_alt_screen: primary lines scrolled before alt-screen appear; lines scrolled while alt_screen is active do NOT appear
  </behavior>
  <action>
    Add a public method `scrollback_lines(&self, from_line: u64, count: usize) -> (Vec<Vec<Cell>>, u64)` to `TerminalState` (do NOT expose the VecDeque). Indexing: `scrollback[0]`=oldest, `scrollback[len-1]`=newest; `from_line=0` is the newest line. Compute `total = self.scrollback.len() as u64`; if `from_line >= total || count == 0` return `(vec![], total)`. Else `newest_idx = (total - 1 - from_line) as usize`, `oldest_idx = newest_idx.saturating_sub(count - 1)`, collect `(oldest_idx..=newest_idx)` cloned rows in oldest-to-newest order, return `(lines, total)`. The accessor must NOT capture or return any epoch — `epoch_at_snapshot` is the caller's responsibility under the same lock (S-5).
    Add the `scrollback_excludes_alt_screen` unit test (ROADMAP criterion 3 verbatim): create `TerminalState::new(80, 24)`; advance enough primary-screen output to force several `scroll_up()` calls (lines scroll into scrollback); record `scrollback.len()`; activate alt screen (drive the `?1049h` / alt-screen-set path through `advance`); force several more scroll lines while alt_screen is active; deactivate alt screen (`?1049l`); assert `scrollback.len()` equals the recorded primary count (alt lines excluded) and that the retained lines are the primary ones. Also assert `scrollback_lines(u64::MAX, 256)` returns an empty page without panicking.
  </action>
  <verify>
    <automated>cargo test -p nosh-server scrollback_excludes_alt_screen && cargo test -p nosh-server scrollback_lines</automated>
  </verify>
  <done>scrollback_lines accessor compiles and is exercised; scrollback_excludes_alt_screen passes, proving the existing !alt_screen gate; bounds-check cases return empty pages without panic.</done>
  <acceptance_criteria>
    - `cargo test -p nosh-server scrollback_excludes_alt_screen` exits 0
    - terminal.rs contains `pub fn scrollback_lines` returning `(Vec<Vec<Cell>>, u64)`
    - The scroll_up() and resize() alt-screen gates are UNCHANGED (verify via grep that the `if !self.echo_state.alt_screen` guards still wrap the scrollback push in both)
    - A test asserts scrollback_lines with from_line = u64::MAX returns an empty page (no panic)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → server (channel data stream) | ScrollbackRequest fields (from_line, count) are untrusted input crossing into server index arithmetic |
| PTY/app output → TerminalState.scrollback | alt-screen apps (vim/htop) must not contaminate the primary scrollback buffer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering | wire format (postcard discriminants) | mitigate | Discriminant-stability test (Task 1) is the FIRST commit; new variants are append-only after ChannelClose=14; inserting/reordering fails the test (MUX-06) |
| T-22-02 | Tampering | `scrollback_lines` index arithmetic | mitigate | `from_line >= total` and `count == 0` early-return `(vec![], total)`; `saturating_sub` on oldest_idx; from_line=u64::MAX proven safe by test (S-4 / V5) |
| T-22-03 | Information disclosure | alt-screen contents leaking to scrollback | mitigate | `!alt_screen` gate on `scroll_up()`/`resize()` left unmodified; `scrollback_excludes_alt_screen` unit test is the regression guard (S-2) |
| T-22-04 | Tampering | accessor exposing VecDeque internals | mitigate | accessor returns owned `Vec<Vec<Cell>>` clone; the `VecDeque` and `SCROLLBACK_LINE_CAP` stay private to terminal.rs |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this phase (RESEARCH Package Legitimacy Audit: N/A); no install tasks |
</threat_model>

<verification>
- `cargo test -p nosh-proto` passes (discriminant + round-trip).
- `cargo test -p nosh-server scrollback_excludes_alt_screen scrollback_lines` passes.
- `grep -n "if !self.echo_state.alt_screen" crates/nosh-server/src/terminal.rs` still shows the gate in both scroll_up() and resize().
- No existing Message variant changed: `cargo test -p nosh-proto message_discriminant_order_is_stable` confirms discriminants 0-14 unchanged.
</verification>

<success_criteria>
- Three append-only wire variants exist at discriminants 15/16/17 and round-trip exactly.
- `ScrollbackPage` carries `epoch_at_snapshot: u64`.
- `TerminalState::scrollback_lines` returns a bounded page + total_available without exposing the VecDeque or panicking on adversarial inputs.
- SCROLL-03 alt-screen exclusion is proven by a passing unit test; the gate is verified-not-modified.
</success_criteria>

<output>
Create `.planning/phases/22-scrollback-sync/22-01-SUMMARY.md` when done.
</output>
