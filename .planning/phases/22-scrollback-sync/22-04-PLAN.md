---
phase: 22-scrollback-sync
plan: 04
type: execute
wave: 4
depends_on: ["22-01", "22-02", "22-03"]
files_modified:
  - crates/nosh-client/tests/channel_mux.rs
autonomous: true
requirements: [SCROLL-01, SCROLL-02, SCROLL-04, SCROLL-05]
must_haves:
  truths:
    - "A live client↔server integration test fetches scrollback over the Scrollback channel and asserts non-empty pages with a sane epoch_at_snapshot and total_available"
    - "Shift-PageUp enters Active scrollback mode and a subsequent non-paging keystroke both snaps the view back to Live AND is forwarded to the shell as PtyData (SCROLL-04)"
    - "PTY input latency and the 16 ms diff tick are unaffected during a scrollback transfer (M-6 isolation proof)"
    - "When the request queue / mpsc is saturated the pump drops oldest rather than stalling — no pump hang (S-4 / M-6)"
    - "Scrollback delivery is in-order with no gaps under simulated packet loss (S-1 reliable-only proof)"
    - "The scrollback↔live handoff at epoch_at_snapshot contains no duplicate or missing lines even when a diff tick is concurrent with the request — no torn epoch (SCROLL-05 / S-5)"
    - "The server confirmed_epoch advances DURING an active scrollback transfer — the client keeps acking epochs while in Active mode so the pump never stalls"
    - "Scrollback is fetchable immediately after a cold reattach on a channel id numerically distinct from the pre-orphan id (fresh re-open, not byte-replay)"
  artifacts:
    - path: "crates/nosh-client/tests/channel_mux.rs"
      provides: "scrollback integration tests: basic fetch, snap-back keybinding, PTY-latency isolation, drop-oldest, in-order under loss, epoch handoff (torn-epoch), post-reattach"
      contains: "scrollback_basic_fetch"
  key_links:
    - from: "crates/nosh-client/tests/channel_mux.rs"
      to: "server run_scrollback_sender_task + client open_channel"
      via: "live quinn loopback session, spawn_ctrl_drain, open_channel(ChannelType::Scrollback)"
      pattern: "ChannelType::Scrollback"
---

<objective>
Prove every SCROLL requirement and every locked security invariant as an automated integration test against a live client↔server loopback session, extending the existing `channel_mux.rs` suite (which currently only covers the echo channel). The Security note mandate is explicit: honour each constraint as a test, not just code.

Purpose: Wave 0 of this phase identified `channel_mux.rs` as a test gap — it only covers echo. This plan closes it with the SCROLL acceptance suite (incl. the SCROLL-04 snap-back keybinding) and the M-6 / S-1 / S-4 / S-5 regression guards.
Output: Seven scrollback integration test functions in `channel_mux.rs`, all green.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-scrollback-sync/22-CONTEXT.md
@.planning/phases/22-scrollback-sync/22-RESEARCH.md
@.planning/phases/22-scrollback-sync/22-PATTERNS.md
@.planning/phases/22-scrollback-sync/22-01-SUMMARY.md
@.planning/phases/22-scrollback-sync/22-02-SUMMARY.md
@.planning/phases/22-scrollback-sync/22-03-SUMMARY.md

<interfaces>
<!-- Test harness analogs already in channel_mux.rs (re-use, do NOT reinvent): -->
  spawn_ctrl_drain(ctrl_recv, frame_tx) -> JoinHandle    // ~102-118; MANDATORY to avoid control-stream deadlock
  await_channel_accept_from_drain(...)                   // ~134-154; get Accept from the drain mpsc
  recv_channel_reply(...)                                // ~74-95; holds ctrl_recv exclusively — do NOT use for scrollback tests
  // existing echo-channel test setup is the structural template for session bring-up

<!-- Wire types under test (plan 22-01): -->
  Message::ScrollbackRequest { channel_id, from_line, count }
  Message::ScrollbackPage { channel_id, from_line, total_available, epoch_at_snapshot, lines }
  Message::ScrollbackCredit { channel_id, bytes }

<!-- client open primitive (client.rs): open_channel(...) for ChannelType::Scrollback -->
<!-- StateDiff.epoch: u64 on datagrams — compared to epoch_at_snapshot for the handoff test -->
<!-- Shift-PageUp CSI = ESC[5;2~ = [0x1b,0x5b,0x35,0x3b,0x32,0x7e] (drive into the client stdin path) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: scrollback_basic_fetch + scrollback_epoch_handoff_no_gap + scrollback_post_reattach</name>
  <files>crates/nosh-client/tests/channel_mux.rs</files>
  <read_first>
    - crates/nosh-client/tests/channel_mux.rs (full file: module header deadlock rationale ~1-55, spawn_ctrl_drain ~102-118, await_channel_accept_from_drain ~134-154, recv_channel_reply ~74-95, the echo-channel test as the session-bring-up template)
    - crates/nosh-client/src/client.rs (open_channel ~760-783)
    - .planning/phases/22-scrollback-sync/22-RESEARCH.md (Phase Requirements -> Test Map lines 642-655; Pitfall 1 epoch handoff lines 386-394; Pitfall 5 reattach lines 434-442)
    - .planning/phases/22-scrollback-sync/22-PATTERNS.md (channel_mux.rs section lines 564-607)
    - .planning/phases/22-scrollback-sync/22-02-SUMMARY.md + 22-03-SUMMARY.md (final wire field names / channel-open signatures as built; confirmed_epoch / epoch ack accessor)
  </read_first>
  <action>
    Add three `#[tokio::test]` functions to channel_mux.rs, each spawning `spawn_ctrl_drain` and using `await_channel_accept_from_drain` (never `recv_channel_reply` — it would deadlock on the control stream).
    `scrollback_basic_fetch` (SCROLL-01): bring up a loopback session; feed enough PTY input that more than 24 rows scroll off the grid into server scrollback; open the Scrollback channel via `open_channel(..., ChannelType::Scrollback)`; write `Message::ScrollbackRequest { channel_id, from_line: 0, count: 256 }` to the channel `ch_send`; read frames from `ch_recv` and assert the first `ScrollbackPage` has `lines` non-empty, `total_available > 0`, and `epoch_at_snapshot` a plausible (>0 or >= some observed) epoch; grant credit via a `ScrollbackCredit` through the control path. ALSO assert that the server-side `confirmed_epoch` (observed via the existing epoch-ack accessor / a delivered datagram epoch) ADVANCES during this active scrollback transfer — i.e. while pages are flowing, keep feeding live PTY output and observe that the confirmed/applied epoch increases — proving the client keeps acking epochs while Active so the server pump never stalls (M-6 / SCROLL-05 ack-flow continuity). If a direct `confirmed_epoch` accessor is not exposed, assert that successive live datagrams continue to arrive with strictly increasing `.epoch` during the transfer.
    `scrollback_epoch_handoff_no_gap` (SCROLL-05 / S-5 torn-epoch proof): start a sustained sequence of live PTY output so diff ticks are firing; issue a `ScrollbackRequest` CONCURRENTLY with the diff ticks (do not quiesce the session first) so a diff tick races the snapshot; record the returned page's `epoch_at_snapshot` and the set of historical line contents; continue receiving live datagrams; assert (a) the page reports a SINGLE consistent `epoch_at_snapshot` and (b) the line content at the scrollback/live boundary appears exactly once across (history up to epoch_at_snapshot) ∪ (live grid at the first datagram with epoch >= epoch_at_snapshot) — no duplicate and no missing line at the seam, even under the concurrent tick (no torn epoch). Drive the assertion off line content equality, not timing. (This test is the falsifiable S-5 proof obligation referenced by plan 22-02 Task 1.)
    `scrollback_post_reattach` (SCROLL-05): bring up a session, produce scrollback, RECORD the channel id of the pre-orphan scrollback channel, then force a cold reattach (orphan + Reattach with the token, mirroring the existing reattach integration setup if one exists in the suite/harness); after `ReattachOutcome::Ok`, open a FRESH Scrollback channel and assert (a) its channel id is numerically DISTINCT from the recorded pre-orphan id (or equivalently that NO `ChannelReject` is received on the new channel — proving fresh-id re-open, not byte-replay), and (b) a `ScrollbackRequest` returns a non-empty `ScrollbackPage` — proving the server SessionSlot scrollback survived and the channel re-opens.
  </action>
  <verify>
    <automated>cargo nextest run --test channel_mux -p nosh-client scrollback_basic_fetch scrollback_epoch_handoff_no_gap scrollback_post_reattach</automated>
  </verify>
  <done>The three tests pass against a live loopback session: basic fetch with confirmed_epoch advancing during transfer, no-gap/no-dup epoch handoff under a concurrent diff tick (no torn epoch), and post-reattach fetchability on a fresh distinct channel id.</done>
  <acceptance_criteria>
    - `cargo nextest run --test channel_mux -p nosh-client scrollback_basic_fetch` exits 0 and asserts confirmed_epoch / live datagram epoch advances DURING the active transfer
    - `cargo nextest run --test channel_mux -p nosh-client scrollback_epoch_handoff_no_gap` exits 0, issues the request concurrently with diff ticks, and asserts a single consistent epoch_at_snapshot with no duplicate/missing line at the seam
    - `cargo nextest run --test channel_mux -p nosh-client scrollback_post_reattach` exits 0 and asserts the post-reattach channel id is distinct from the pre-orphan id (or no ChannelReject)
    - All three use `spawn_ctrl_drain` + `await_channel_accept_from_drain` (grep), not `recv_channel_reply`
    - The handoff test asserts on line-content equality at the seam (no duplicate / no missing), not on timing
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: scrollback_pty_latency_isolation + scrollback_backpressure_drop_oldest + scrollback_inorder_under_loss</name>
  <files>crates/nosh-client/tests/channel_mux.rs</files>
  <read_first>
    - crates/nosh-client/tests/channel_mux.rs (session bring-up + spawn_ctrl_drain helpers; any existing loss-injection / transport-config helper used by prior phases; how PTY input round-trip is measured if a helper exists)
    - .planning/phases/22-scrollback-sync/22-RESEARCH.md (Test Map lines 646-649 for the three test names; Pitfall 2 M-6 lines 398-407; Pitfall 3 S-1 lines 410-418; Security Domain threat table lines 678-685)
    - .planning/phases/22-scrollback-sync/22-02-SUMMARY.md (the bounded mpsc / MAX_PAGE_SIZE / drop-oldest behaviour as built; the credit window size)
  </read_first>
  <action>
    Add three `#[tokio::test]` functions, all using `spawn_ctrl_drain`.
    `scrollback_pty_latency_isolation` (SCROLL-02 / M-6): open the Scrollback channel and start a sustained scrollback transfer (request a large range, grant credit so pages flow); concurrently send PTY input bytes and measure the input→echo (or input→diff-tick) round-trip; assert the measured PTY latency stays under a generous bound (e.g. < 50 ms, well above the 16 ms tick but proving no multi-hundred-ms stall) and that diff ticks continue arriving during the transfer. The point is to prove the sender task does not block the pump — pick a bound that fails if the sender were inline in the select! loop.
    `scrollback_backpressure_drop_oldest` (SCROLL-02 / S-4): drive the scrollback path to its bounded limit (withhold credit / flood requests faster than the server processes) and assert the server pump does NOT hang — PTY input still round-trips and the session stays live — i.e. oldest queued items are dropped rather than the pump blocking. Assert the session does not deadlock within a timeout.
    `scrollback_inorder_under_loss` (SCROLL-02 / S-1): configure the loopback transport with simulated packet loss (reuse any existing loss-injection harness from prior phases; if none, drop a fraction of datagrams at the socket shim — scrollback rides a reliable stream so QUIC must retransmit) and assert that a fetched multi-page scrollback range arrives with lines in strict order and with no gaps (every requested line index present exactly once, contiguous). This is the reliable-only proof — a datagram-delivered scrollback would show gaps here.
  </action>
  <verify>
    <automated>cargo nextest run --test channel_mux -p nosh-client scrollback_pty_latency_isolation scrollback_backpressure_drop_oldest scrollback_inorder_under_loss</automated>
  </verify>
  <done>The three security/isolation tests pass: PTY latency unaffected during transfer, pump does not stall under back-pressure (drop-oldest), and scrollback is in-order/no-gap under loss.</done>
  <acceptance_criteria>
    - `cargo nextest run --test channel_mux -p nosh-client scrollback_pty_latency_isolation` exits 0 and asserts a concrete PTY-latency bound during an active transfer
    - `cargo nextest run --test channel_mux -p nosh-client scrollback_backpressure_drop_oldest` exits 0 and asserts the session does not hang under saturation (timeout-bounded)
    - `cargo nextest run --test channel_mux -p nosh-client scrollback_inorder_under_loss` exits 0 and asserts strict line ordering + no gaps under simulated loss
    - All three use `spawn_ctrl_drain`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: scrollback_keybinding_snap_back (SCROLL-04 integration)</name>
  <files>crates/nosh-client/tests/channel_mux.rs</files>
  <read_first>
    - crates/nosh-client/tests/channel_mux.rs (session bring-up template, spawn_ctrl_drain; how the client stdin path / run_pump is driven from a test if a harness exists, or how PTY input is injected client-side)
    - crates/nosh-client/src/main.rs (the ScrollbackView state machine + Shift-PageUp CSI interception + snap-back from plan 22-03 Tasks 2a/2b — the exact entry point a test drives bytes into)
    - .planning/phases/22-scrollback-sync/22-RESEARCH.md (Test Map SCROLL-04 line 652; Pattern 4 lines 328-359)
    - .planning/phases/22-scrollback-sync/22-03-SUMMARY.md (how run_pump consumes stdin in a testable way; the PtyData forward path on snap-back)
  </read_first>
  <action>
    Add a `#[tokio::test] scrollback_keybinding_snap_back` (SCROLL-04) over a channel/pty loopback session. Bring up a session and produce scrollback (so a fetch returns content). Drive the client input path with the Shift-PageUp CSI byte sequence `ESC[5;2~` = `[0x1b,0x5b,0x35,0x3b,0x32,0x7e]` to enter Active scrollback mode (assert the client issues a `ScrollbackRequest` and/or enters Active — observe via the resulting ScrollbackRequest on the channel or a state hook the 22-03 build exposes). Then send a single NON-paging keystroke (e.g. the byte `b'x'`). Assert BOTH:
    (a) the view snaps back to Live — observable as the client resuming normal datagram application / the Active state clearing (via whatever observable signal 22-03 exposes: a subsequent live render, or the client no longer suppressing datagram display);
    (b) the non-paging keystroke is FORWARDED to the shell as PtyData — assert the byte `x` reaches the server PTY and is echoed back in a live datagram / PTY output (proving the keystroke is delivered, not swallowed — the LOCKED SCROLL-04 behaviour).
    Use `spawn_ctrl_drain`; never `recv_channel_reply`. If driving raw stdin bytes into run_pump from an integration test requires a small test seam, prefer reusing whatever input-injection the existing client tests use (mirror it) over inventing a new path; if the only feasible observation point is the server-side PTY echo of `x` plus the ScrollbackRequest having been emitted, that pair is a sufficient SCROLL-04 assertion.
  </action>
  <verify>
    <automated>cargo nextest run --test channel_mux -p nosh-client scrollback_keybinding_snap_back</automated>
  </verify>
  <done>scrollback_keybinding_snap_back passes: Shift-PageUp enters Active (ScrollbackRequest emitted), and a following non-paging keystroke both snaps back to Live and is forwarded to the shell as PtyData (server echoes it).</done>
  <acceptance_criteria>
    - `cargo nextest run --test channel_mux -p nosh-client scrollback_keybinding_snap_back` exits 0
    - The test sends `ESC[5;2~` and asserts Active entry (ScrollbackRequest emitted or Active state observed)
    - The test sends a non-paging keystroke and asserts BOTH snap-back to Live AND the keystroke forwarded to the shell as PtyData (server echo / PTY receipt)
    - The test uses `spawn_ctrl_drain`, not `recv_channel_reply`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test client → live server | exercises the real ScrollbackRequest/Page/Credit path over quinn loopback |
| lossy transport shim → reliable scrollback stream | proves QUIC reliability holds for scrollback (no datagram fallback) |
| local stdin (test-driven CSI) → client run_pump | drives the SCROLL-04 snap-back keybinding behaviour |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-16 | Denial of service (regression guard) | sender blocking the pump (M-6) | mitigate | `scrollback_pty_latency_isolation` + `scrollback_backpressure_drop_oldest` assert the pump stays responsive and never hangs under transfer/saturation; `scrollback_basic_fetch` asserts confirmed_epoch advances during transfer |
| T-22-17 | Information disclosure / Tampering (regression guard) | scrollback over datagrams (S-1) | mitigate | `scrollback_inorder_under_loss` asserts no gaps under simulated loss — a datagram path would fail this |
| T-22-18 | Tampering (regression guard) | torn epoch/scrollback handoff (S-5) | mitigate | `scrollback_epoch_handoff_no_gap` issues the request concurrently with diff ticks and asserts a single consistent epoch_at_snapshot, no duplicate/missing line at the seam |
| T-22-19 | Denial of service (regression guard) | unbounded server buffer growth (S-4) | mitigate | `scrollback_backpressure_drop_oldest` proves drop-oldest bounded behaviour under flood |
| T-22-20 | Repudiation/UX (regression guard) | keystroke swallowed during snap-back (SCROLL-04) | mitigate | `scrollback_keybinding_snap_back` asserts the non-paging keystroke is forwarded to the shell as PtyData while the view snaps to Live |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | no new packages (RESEARCH audit N/A); no install tasks |
</threat_model>

<verification>
- `cargo nextest run --test channel_mux -p nosh-client` runs all seven new scrollback tests green.
- Each test uses `spawn_ctrl_drain` (control-stream deadlock avoidance) and does not use `recv_channel_reply`.
- The loss and isolation tests assert concrete, falsifiable bounds (ordering/no-gap; PTY latency; no-hang).
- The handoff test exercises a concurrent diff tick (torn-epoch proof) and the basic-fetch test asserts confirmed_epoch advances during transfer.
- The snap-back test asserts both the Live transition and PtyData forwarding (SCROLL-04).
</verification>

<success_criteria>
- SCROLL-01 fetch proven live; SCROLL-02 reliability + isolation + back-pressure proven; SCROLL-04 snap-back keybinding proven; SCROLL-05 epoch handoff (no torn epoch) + post-reattach (fresh distinct id) proven — all as automated tests.
- The M-6, S-1, S-4, S-5 invariants each have a falsifiable regression guard; confirmed_epoch advances during transfer.
</success_criteria>

<output>
Create `.planning/phases/22-scrollback-sync/22-04-SUMMARY.md` when done.
</output>
</output>
