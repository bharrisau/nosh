---
phase: 13-server-datagram-sender
plan: 03
type: execute
wave: 3
depends_on: ["13-02"]
files_modified:
  - crates/nosh-client/tests/sync.rs
autonomous: true
requirements: [SYNC-03]

must_haves:
  truths:
    - "An integration test connects a real client + in-process server, types characters, and asserts conn.read_datagram() yields a non-empty StateDiff datagram"
    - "A test sends an epoch-ack datagram and the server's subsequent diffs reflect the advanced baseline"
    - "A test exercises the ResumeComplete gate path (datagrams flow only after a fresh/resumed session is ready)"
  artifacts:
    - path: "crates/nosh-client/tests/sync.rs"
      provides: "SYNC-03 end-to-end integration tests for the server datagram sender"
      contains: "read_datagram"
  key_links:
    - from: "crates/nosh-client/tests/sync.rs"
      to: "decode_datagram + encode_epoch_ack (nosh-proto)"
      via: "decode received StateDiff datagram; encode + send epoch-ack"
      pattern: "decode_datagram"
    - from: "crates/nosh-client/tests/sync.rs"
      to: "in-process server diff_interval arm (Plan 02)"
      via: "spawn_server_with_shell + open_session + send_input, then read_datagram"
      pattern: "read_datagram"
---

<objective>
Add the SYNC-03 integration test file `crates/nosh-client/tests/sync.rs`: a real client + in-process server, drive shell output, and assert that StateDiff datagrams actually arrive at the client (ROADMAP Phase 13 criterion 2), that an epoch-ack advances the server baseline (D-13-01c full acked-epoch loop), and that the ResumeComplete gate behaves correctly (criterion 3).

Purpose: This is the end-to-end proof that the Plan 02 server sender works against a genuine quinn client over the real QUIC datagram channel — the success criterion the roadmap names explicitly. Per D-13-01c, Phase 13's test client is the only thing that emits epoch-acks this milestone (the real client does so in Phase 14).

Output: One new test file with focused `#[tokio::test]` cases, each guarded by the existing `have_sh()` skip so CI without `/bin/sh` is not a hard failure.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-server-datagram-sender/13-CONTEXT.md
@.planning/phases/13-server-datagram-sender/13-RESEARCH.md
@.planning/phases/13-server-datagram-sender/13-PATTERNS.md
@.planning/phases/13-server-datagram-sender/13-02-SUMMARY.md

<interfaces>
<!-- Extracted from the codebase. Use directly — no exploration needed. -->

From crates/nosh-proto (re-exported at crate root):
- `decode_datagram(bytes: &[u8]) -> Result<StateDiff, ProtoError>` — decodes a server→client StateDiff
- `encode_epoch_ack(epoch: u64) -> Bytes` (Plan 01) — builds a client→server epoch-ack datagram
- `StateDiff { epoch: u64, cols: u16, rows: u16, cursor: CursorPos, runs: Vec<DiffRun> }` — `runs` non-empty means changed cells present

From crates/nosh-client/src/client.rs:
- `pub async fn open_session(conn, term: String, cols, rows, env) -> anyhow::Result<(SendStream, RecvStream)>` (line 586) — opens the session bidi stream + sends SessionOpen; server replies SessionOpened on `recv` (read+discard it)
- `pub async fn send_input(send: &mut SendStream, bytes: &[u8]) -> anyhow::Result<()>` (line 608) — sends a PtyData frame (keystrokes)
- `pub async fn open_session_with_token(conn, term, cols, rows, env) -> Result<(SendStream, RecvStream, [u8;16])>` (line 489) — same but returns the reattach token (use if a test needs the token)
- reattach helpers: `send_reattach`, `await_reattach_reply`, `reattach_collect`, `ReattachOutcome` (lines 489-565)

From crates/nosh-client/tests/common/mod.rs (the test harness — `mod common;`):
- `pub const HOST: &str = "localhost";`
- `pub struct TestKey { pub public, ... }`; `TestKey::generate()`; `key.client_identity() -> ClientIdentity`
- `pub struct TestServer { pub addr: SocketAddr, pub registry: Arc<SessionRegistry>, ... }` (Drop aborts the task)
- `pub async fn spawn_server_with_shell(host_key, authorized, limits, shell_override: Option<String>) -> TestServer`
- `pub async fn spawn_server_with_registry(host_key, authorized, limits, shell_override, registry) -> TestServer`
- `pub fn client_endpoint(identity: ClientIdentity, known_hosts: PathBuf) -> anyhow::Result<quinn::Endpoint>`
- `pub fn have_sh() -> bool` (already exists — reuse, do not redefine)
- `pub async fn connect(...)` exists in client.rs as `client::connect`; reattach.rs builds `client_endpoint_for` then `client::connect`
- `nosh_server::server::AuthLimits` (`AuthLimits::default()`)

From crates/nosh-client/tests/transport.rs (datagram test analog):
- `conn.max_datagram_size().is_some()` confirms datagrams negotiated
- `client::datagram_roundtrip(conn, payload)` shows send_datagram/read_datagram usage

From crates/nosh-client/tests/reattach.rs (reattach + server-spawn analog):
- `server_with_key(registry, client_key)` helper shape (lines 30-43)
- `client_endpoint_for(key) -> (Endpoint, TempDir)` helper (lines 46-51)
- reattach drive pattern (drop conn → reconnect → send_reattach → await_reattach_reply)

quinn:
- `conn.read_datagram() -> impl Future<Output = Result<Bytes, ConnectionError>>` — wrap in `tokio::time::timeout`
- `conn.send_datagram(payload: Bytes) -> Result<(), SendDatagramError>` (synchronous)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Datagram-arrival + epoch-ack-loop integration tests</name>
  <files>crates/nosh-client/tests/sync.rs</files>
  <read_first>
    - crates/nosh-client/tests/transport.rs (datagram test patterns — `datagram_enabled`, `max_datagram_size`, the `connect` flow lines 27-62)
    - crates/nosh-client/tests/reattach.rs (server spawn + client endpoint + connect helpers lines 1-51; reattach drive pattern)
    - crates/nosh-client/tests/common/mod.rs (the full helper surface: HOST, TestKey, TestServer, spawn_server_with_shell, client_endpoint, have_sh, session_marker_usable lines 233-244 for the session-drive shape)
    - crates/nosh-client/src/client.rs (open_session line 586, send_input line 608, connect — to build a live session that STAYS open while datagrams flow)
    - .planning/phases/13-server-datagram-sender/13-PATTERNS.md (the tests/sync.rs section lines 221-344 — boilerplate, server spawn, datagram read pattern, epoch-ack emission pattern, have_sh skip pattern)
    - .planning/phases/13-server-datagram-sender/13-RESEARCH.md (Integration test shape lines 518-556)
  </read_first>
  <action>
    Create `crates/nosh-client/tests/sync.rs`. File header doc comment: "Phase 13 SYNC-03 integration tests — server datagram sender." Add `mod common;` and the imports from PATTERNS.md lines 244-264 (`use common::{TestKey, HOST};`, `use nosh_proto::datagram::{decode_datagram, encode_epoch_ack};`, `use nosh_server::registry::SessionRegistry`, `nosh_server::server::AuthLimits`, `std::time::Duration`). Add a small local helper `server_with_key(client_key)` mirroring reattach.rs (spawn_server_with_shell with a generated host key, the client key authorized, `AuthLimits::default()`, `Some("/bin/sh".into())`) and `client_endpoint_for(key)` (tempdir known_hosts + `common::client_endpoint`). Reuse `common::have_sh()` — do not redefine it.

    **Test 1 — `sync03_server_emits_datagram_after_pty_output`** (ROADMAP criterion 2): skip with an eprintln if `!have_sh()`. Generate a client key, spawn server, connect (`client::connect`), `open_session(conn, "xterm".into(), 80, 24, Vec::new())`, read+discard the `SessionOpened` frame from `recv`, then `send_input(&mut send, b"echo hello\n")`. Keep `send`/`recv`/`conn` alive (do NOT drain to SessionClose — the session must stay live for ticks to fire). Loop: `tokio::time::timeout(Duration::from_secs(5), conn.read_datagram())` until a datagram whose `decode_datagram(&bytes)` is `Ok(StateDiff)` with `!runs.is_empty()` arrives; assert it within the timeout. Assert `diff.epoch >= 1`. (A misrouted non-StateDiff datagram would `Err` in decode — loop past it; but the server only sends StateDiff this milestone.)

    **Test 2 — `sync03_acked_epoch_advances_baseline`** (D-13-01c full loop): skip if `!have_sh()`. Same setup; `send_input(b"echo A\n")`; read the first StateDiff (epoch E1, runs contain the 'A' output region). Send an epoch-ack: `conn.send_datagram(encode_epoch_ack(E1))`. Then `send_input(b"echo B\n")`, read the next StateDiff with `epoch > E1`. Assert the new diff's epoch advanced (`E2 > E1`) AND that the combined run content of E2 contains 'B' (the new output). NOTE per RESEARCH lines 542-543: the baseline-shrink assertion is approximate (the shell may emit prompt redraws); assert the WEAKER robust property — that after acking E1, a subsequent diff still arrives with a strictly greater epoch and includes the new 'B' content — rather than asserting 'A' is absent. Add a code comment citing RESEARCH Open Question 1 (acked-epoch is self-correcting; exact byte-minimality is not a stable test invariant).
  </action>
  <verify>
    <automated>cargo test -p nosh-client --test sync sync03_server_emits sync03_acked_epoch 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `crates/nosh-client/tests/sync.rs` exists and `grep -c "#\[tokio::test\]" crates/nosh-client/tests/sync.rs` returns at least 2.
    - `grep -n "read_datagram" crates/nosh-client/tests/sync.rs` matches (the datagram-arrival assertion).
    - `grep -n "encode_epoch_ack" crates/nosh-client/tests/sync.rs` matches (Test 2 sends an epoch-ack).
    - `grep -n "decode_datagram" crates/nosh-client/tests/sync.rs` matches (decodes the received StateDiff).
    - `grep -c "have_sh()" crates/nosh-client/tests/sync.rs` returns at least 2 (each test guarded).
    - `cargo test -p nosh-client --test sync sync03_server_emits sync03_acked_epoch` exits 0 (both pass, or skip cleanly if `/bin/sh` absent — on this Linux host `/bin/sh` exists so they run).
    - `cargo clippy -p nosh-client --tests -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>The integration tests prove a real client receives non-empty StateDiff datagrams after typing, and that the full acked-epoch loop works (client sends an epoch-ack, server emits a subsequent diff with a strictly greater epoch carrying the new output). Both tests pass on this host.</done>
</task>

<task type="auto">
  <name>Task 2: ResumeComplete-gate suppression test</name>
  <files>crates/nosh-client/tests/sync.rs</files>
  <read_first>
    - crates/nosh-client/tests/sync.rs (the file from Task 1 — add to it; reuse its `server_with_key`/`client_endpoint_for` helpers)
    - crates/nosh-client/tests/reattach.rs (the cold-reattach drive pattern: open session → get token → drop conn → reconnect → `send_reattach` + `await_reattach_reply` → replay → live; this is the exact sequence to reproduce the replay window)
    - crates/nosh-client/src/client.rs (open_session_with_token line 489, send_reattach line 514, await_reattach_reply line 535, reattach_collect line 560, ReattachOutcome line 465)
    - .planning/phases/13-server-datagram-sender/13-RESEARCH.md (sync03_datagrams_suppressed_during_replay shape lines 545-555 — note the "timing is tricky" caveat and the recommended observable-window approach)
  </read_first>
  <action>
    Add **Test 3 — `sync03_datagrams_flow_after_resume`** to `crates/nosh-client/tests/sync.rs`. Skip if `!have_sh()`.

    The RESEARCH file flags that observing the replay window precisely is timing-sensitive. Implement the ROBUST, deterministic property rather than a flaky mid-replay race: assert that a RESUMED (cold-reattach) session emits datagrams normally AFTER `ResumeComplete`, AND that the first post-resume datagram is a full-screen diff (D-13-01b empty-baseline reset). Concretely:
    1. Use `spawn_server_with_registry` with a registry that has a non-zero idle timeout so the session orphans rather than being reaped (mirror reattach.rs registry setup).
    2. Open a session, drive some output (`send_input(b"echo XYZ\n")`), capture the reattach token from `open_session_with_token`.
    3. Drop the connection (simulate transport loss → server orphans the session).
    4. Reconnect on a fresh client endpoint, `send_reattach(token, last_acked_seq)`, `await_reattach_reply` → assert `ReattachOutcome::Ok`. Read/consume the replayed `PtyData` frames on the reliable stream until replay drains (reuse the reattach.rs collect pattern; `reattach_collect` if it keeps the conn alive, otherwise inline the loop so the conn stays open).
    5. After replay, send fresh input (`send_input(b"echo NEW\n")`) and assert that `tokio::time::timeout(Duration::from_secs(5), conn.read_datagram())` yields a `Ok(StateDiff)` with non-empty runs — i.e. datagrams DO flow once the session is resumed (proving the gate opens after ResumeComplete, not stuck closed).
    6. Assert the first received post-resume StateDiff covers the full screen rather than a tiny delta — check `diff.runs` collectively span more than a single cell (e.g. total chars across runs > 1, since the empty-baseline reset produces a full repaint per D-13-01b). Add a code comment: "first post-resume diff = full screen via empty-baseline reset (D-13-01b); not a special keyframe path."

    If `reattach_collect` consumes/closes the connection (check its doc), use the lower-level `send_reattach` + `await_reattach_reply` + a manual `read_message` replay-drain loop so `conn` survives for the post-resume `read_datagram`. Keep the connection and streams in scope until all assertions complete (RAII drop at end aborts cleanly).
  </action>
  <verify>
    <automated>cargo test -p nosh-client --test sync sync03_datagrams_flow_after_resume 2>&1 | tail -25; cargo test -p nosh-client --test sync 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "sync03_datagrams_flow_after_resume" crates/nosh-client/tests/sync.rs` matches one `#[tokio::test]`.
    - `grep -n "send_reattach\|await_reattach_reply" crates/nosh-client/tests/sync.rs` matches (the cold-reattach drive).
    - `grep -n "ReattachOutcome::Ok" crates/nosh-client/tests/sync.rs` matches (the reattach success assertion).
    - The test asserts a post-resume `read_datagram` yields a non-empty StateDiff AND that the diff spans more than one cell (full-screen reset): verify by inspection that both assertions are present.
    - `cargo test -p nosh-client --test sync` exits 0 (all three tests pass on this host).
    - `cargo clippy -p nosh-client --tests -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>A resumed cold-reattach session is proven to emit datagrams after ResumeComplete (the gate opens post-replay, not stuck closed), with the first post-resume diff being a full-screen repaint via the empty-baseline reset (D-13-01b). All three sync.rs tests pass on this host.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test client → in-process server | The test drives the same client→server datagram path as a real client (epoch-acks) and server→client StateDiff path. No new boundary beyond Plan 02; this plan only exercises it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-08 | Tampering (test coverage) | acked-epoch loop end-to-end | mitigate | Test 2 exercises the full client-acks→server-advances loop over the real QUIC datagram channel, the only end-to-end coverage of D-13-01 this milestone (D-13-01c). |
| T-13-09 | Information Disclosure (test coverage) | ResumeComplete gate | mitigate | Test 3 verifies the gate opens correctly after a cold-reattach replay (datagrams flow post-resume, full-screen first diff), guarding against a regression that leaves the gate stuck closed or opens it during replay. |

No high-severity threats — this is a test-only plan adding no production code, no new auth/authz/crypto, and no package installs (no `T-13-SC` gate). The threats here are coverage assertions backing Plan 01/02's mitigations.
</threat_model>

<verification>
- `cargo test -p nosh-client --test sync` exits 0 (all three SYNC-03 tests pass on this `/bin/sh`-equipped Linux host).
- `cargo clippy -p nosh-client --tests -- -D warnings` exits 0.
- `cargo test --workspace` exits 0 (no regression introduced by the new test target).
- All grep gates in both tasks' acceptance_criteria pass.
</verification>

<success_criteria>
- A real quinn client receives non-empty StateDiff datagrams after typing into the remote shell (ROADMAP criterion 2).
- The full acked-epoch loop is exercised end-to-end: a client epoch-ack causes the server to emit a subsequent diff with a strictly greater epoch carrying new output (D-13-01c).
- The ResumeComplete gate is proven to open after a cold-reattach replay, with the first post-resume diff being a full-screen repaint (D-13-01b / ROADMAP criterion 3).
</success_criteria>

<output>
Create `.planning/phases/13-server-datagram-sender/13-03-SUMMARY.md` when done.
</output>
