# Phase 999.3: Client terminal-rendering correctness pack - Discussion Log

> **Audit trail only.** Decisions captured in CONTEXT.md.

**Date:** 2026-06-05
**Phase:** 999.3-client-terminal-rendering-correctness-pack-platform-agnostic
**Mode:** discuss (interactive, via /gsd:autonomous --interactive — backlog phase promoted)
**Areas discussed:** BUG-E prompt-boundary, Typematic threshold, BUG-H clear scope, Latency instrument scope

---

## Pre-discussion routing

Autonomous mode flagged Phase 18 + 999.1/999.2/999.3 as incomplete. STATE.md records Phase 18
deferred to a future milestone and 999.x as backlog. User chose: run a backlog phase → 999.3.

## BUG-E — backspace/left past prompt start

| Option | Description | Selected |
|--------|-------------|----------|
| Epoch-start col as floor | Record confirmed cursor col at epoch start; clamp there. Mosh-style. | ✓ |
| Suppress when ambiguous | Stop predicting backspace/left at epoch-start col; let server confirm. | |
| Leave col-0 clamp | Don't fix the floor. | |

→ D-01.

## Typematic / fast-typing glitch

| Option | Description | Selected |
|--------|-------------|----------|
| Predict per-char if all printable | Inspect batch; predict each printable char; suppress only escape/control/mixed. | ✓ |
| Raise byte threshold | Bump >4 to >16/>32. | |
| Time-based debounce | Inter-batch timing to separate key-repeat from paste. | |

→ D-02. Bracketed-paste suppression + CJK test must remain intact.

## BUG-H — clear scope

| Option | Description | Selected |
|--------|-------------|----------|
| Fix where the bug actually is | Client clear-on-connect + blank-cell painting; server ED fix only if a repro test proves it wrong. | ✓ |
| Client-only | Restrict to screen.rs. | |
| Both client + server proactively | Audit/harden terminal.rs regardless. | |

→ D-03. Evidence-driven; no speculative server-terminal edits.

## Latency instrument scope (D-17-02a)

| Option | Description | Selected |
|--------|-------------|----------|
| Defer as its own item | Keep 999.3 purely visible-rendering correctness. | |
| Include in this pack | Fold per-keystroke RTT instrumentation into 999.3. | ✓ |

→ D-04. Per-keystroke RTT replaces epoch-confirmation timing.

## Locked to recommended (not separately discussed)

- **BUG-F** (Enter after `read -s`): sync predicted caret from confirmed StateDiff cursor when
  the noecho epoch ends → D-05.
- **Test strategy**: deterministic Linux fail-before/pass-after reproduction test per bug,
  adversarial style → D-06.

## Claude's Discretion
- Clamp-floor representation; D-02 classifier location; physical-clear escape choice;
  per-keystroke RTT storage shape.

## Deferred Ideas
- None (latency item folded in rather than deferred).
