---
phase: 999.3-client-terminal-rendering-correctness-pack-platform-agnostic
verified: 2026-06-05T00:00:00Z
status: human_needed
score: 6/6 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: none
  previous_score: n/a
  note: "Initial verification. A prior code review (999.3-REVIEW.md) found 1 critical (CR-01) + 3 warnings + 1 info; all five have dedicated fix commits (173b6e6 CR-01, 71d071f WR-01, 124e126 WR-02, aefdd96 WR-03, 5207127 IN-01) and are confirmed present in the code."
human_verification:
  - test: "Mid-session Ctrl-L on a live Linux client against a real shell — type a few lines, then press Ctrl-L."
    expected: "The whole screen clears at once (cursor home, prior lines gone), not one line at a time. Verified mechanically end-to-end (server ED2 blanks grid → compute_diff_runs emits space runs → client emit_diff writes spaces), but the original symptom was reported from live use and the visual outcome is not captured by a unit test."
    why_human: "Visual terminal rendering across the real server→datagram→client pipeline; unit tests prove each seam but not the composed on-screen result."
  - test: "Connect to a Linux server in a terminal that already has prior content (e.g. previous `ls` output) and observe the first frame."
    expected: "Prior terminal content is cleared on connect (emit_connect_clear fires \\x1b[2J\\x1b[H once); no bleed-through under nosh's rendering."
    why_human: "Visual confirmation of the one-shot connect clear in a real terminal; the call site and method are verified in code but the on-screen effect is human-observable only."
  - test: "Fast-type / hold a key (typematic key-repeat) in vim over a live Linux session; also paste a multi-line block (bracketed paste) and enter a `read -s` password."
    expected: "Fast typing shows predictive echo per character (no bulk-suppress repaint glitch); bracketed paste is still suppressed (no corruption); the Enter after `read -s` advances to a fresh line; backspace at the new prompt cannot walk left of the prompt boundary."
    why_human: "These are interactive-timing / predictive-echo UX behaviours surfaced during live daily-driver use; the deterministic unit tests (D-06) cover the logic, but the felt behaviour over a real RTT is human-observable."
  - test: "Per-keystroke RTT instrumentation (D-04): run the client with `RUST_LOG=nosh::predict=debug` and type a few keystrokes."
    expected: "Debug logs show per-keystroke `confirm` events with `keystroke_id` + `latency_ms` (per-keystroke RTT), not per-epoch-confirmation latency. No character content appears in the logs (T-15-08)."
    why_human: "D-04 is instrumentation-only with no behavioural test; the log shape and content-redaction are best confirmed by inspecting real debug output."
---

# Phase 999.3: Client terminal-rendering correctness pack Verification Report

**Phase Goal:** Resolve the terminal-handling defects surfaced during Phase 17 live validation (BUG-H clear-on-connect / blank-cells / Ctrl-L; BUG-E backspace-past-prompt; BUG-F Enter-after-noecho; typematic fast-typing glitch; D-17-02a per-keystroke latency). All items reproduce on a Linux client — fix and test on Linux.
**Verified:** 2026-06-05
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

The phase has no REQ-IDs; success is defined by CONTEXT.md decisions D-01..D-06, each requiring an adversarial fail-before/pass-after test. All six are delivered and the tests are genuine (I mutated the fixes back to their pre-fix behaviour and confirmed the tests fail with the documented symptom — see Behavioral Spot-Checks).

### Observable Truths

| # | Truth (decision) | Status | Evidence |
|---|------------------|--------|----------|
| 1 | **D-01 (BUG-E):** backspace / cursor-left clamp at `epoch_start_col` (prompt boundary), not col 0 | ✓ VERIFIED | `predictor.rs:372,385` clamp `> self.epoch_start_col`; `epoch_start_col` set only on fresh epoch via `needs_epoch_start_sync` gate (`:278-280`). Tests `bug_e_backspace_clamps_at_epoch_start_col`, `bug_e_cursor_left_clamps_at_epoch_start_col` pass. |
| 2 | **D-05 (BUG-F):** after a noecho EpochReset the predicted caret is force-synced from the confirmed cursor so post-`read -s` Enter advances the line | ✓ VERIFIED | `reset_with_cursor` (`:608-622`) sets `predicted_cursor = confirmed`; called from `EpochReset` arm (`:422`). `BulkSuppressed` keeps plain `reset()`. Tests `bug_f_enter_after_noecho_syncs_caret_from_confirmed`, `bug_f_bulk_suppressed_does_not_sync_caret` pass. |
| 3 | **D-02 (typematic):** an all-printable-single-width stdin batch is predicted char-by-char (PredictBatch), not bulk-suppressed; escape/control/wide/mixed still BulkSuppressed; bracketed-paste + CJK preserved | ✓ VERIFIED | Content-inspection in `classify_input` (`:760-781`); `PredictBatch` arm in `on_input` (`:440-475`). Bracketed-paste matched first (`:741-745`). Tests `typematic_printable_batch_predicts_each_char`, `typematic_batch_advances_cursor`, `typematic_mixed_batch_with_escape_is_suppressed`, `classify_printable_batch_not_bulk_suppressed`, `classify_bulk_suppressed_with_escape_still_suppresses`, `classify_cjk_3byte_not_bulk`, `classify_bracketed_paste_*` all pass. |
| 4 | **D-03a (BUG-H blank cells):** a non-blank→blank cell is emitted as a space so the prior char is overwritten (no bleed-through / Ctrl-L works) | ✓ VERIFIED | `emit_diff` skip is `if want == have` (`screen.rs:449`) — a `' '` over `'X'` is unequal, so the space is written. Server side confirmed: ED2 (`terminal.rs:606-613`) blanks the grid; `compute_diff_runs` (`server.rs:230,252-258`) pushes the space into a run for blank-over-non-blank. Test `bug_h_blank_cell_after_clear_is_emitted` passes and fails when emit_diff is mutated to skip blanks (probe). |
| 5 | **D-03b (BUG-H connect-clear):** `emit_connect_clear` writes `\x1b[2J\x1b[H` then `reset_physical()`, wired once on connect in `run_pump` | ✓ VERIFIED | `screen.rs:529-532`; wired at `main.rs:796-812` after `ClientScreen::new`. Test `bug_h_connect_clear_emits_ed2_home_and_resets_physical` passes. |
| 6 | **D-04 (latency):** `run_pump` records per-keystroke RTT (keystroke_id map), not epoch-confirmation time; prune prevents unbounded growth; instrumentation-only, no behaviour change | ✓ VERIFIED | `keystroke_id` counter + `predict_enqueue_times.insert(keystroke_id, ...)` (`main.rs:1220-1221`); confirm-side drain + `retain` (`:1044-1067`); WR-02 prune on reset (`:1210-1213`). Send path unchanged (instrumentation-only). No behavioural test — flagged for human log inspection. |
| — | **D-06 (test strategy):** every bug has a deterministic Linux fail-before/pass-after test; suite compiles + passes on Linux | ✓ VERIFIED | 92 lib tests pass; integration suites pass. Three fixes mutated back to pre-fix behaviour → corresponding tests fail with documented symptom (genuine adversarial coverage). |

**Score:** 6/6 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `crates/nosh-client/src/predictor.rs` | epoch_start_col clamp + reset_with_cursor + PredictBatch + tests | ✓ VERIFIED | `epoch_start_col`, `needs_epoch_start_sync`, `reset_with_cursor`, `PredictBatch` all present and wired; clamps applied in 3 arms (incl. WR-01 PredictLineStart). |
| `crates/nosh-client/src/screen.rs` | blank-cell emission (already-correct, test added) + emit_connect_clear | ✓ VERIFIED | `emit_connect_clear` present; `emit_diff` correctly emits blank-over-nonblank; tests added. |
| `crates/nosh-client/src/main.rs` | emit_connect_clear wiring + per-keystroke RTT + WR-02/WR-03/IN-01 fixes | ✓ VERIFIED | All call sites + sanitisation + prune present. |
| `crates/nosh-server/src/terminal.rs` | NOT modified (per D-03 — server diff already correct) | ✓ VERIFIED | Unchanged; ED2 + compute_diff_runs confirmed correct by inspection. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| PredictBackspace/CursorLeft clamp | epoch_start_col | `> self.epoch_start_col` | ✓ WIRED | predictor.rs:372,385 |
| EpochReset arm | screen.confirmed_cursor() | reset_with_cursor | ✓ WIRED | predictor.rs:422 |
| classify_input batch | per-char PredictChar | PredictBatch arm in on_input | ✓ WIRED | predictor.rs:440-475 |
| emit_connect_clear | reset_physical() | method body | ✓ WIRED | screen.rs:531 |
| run_pump ClientScreen::new | emit_connect_clear | one-shot connect clear | ✓ WIRED | main.rs:798 |
| run_pump enqueue path | per-keystroke timestamp map | keystroke_id + Instant | ✓ WIRED | main.rs:1220-1221 |
| Ctrl-L (server) → client | space runs in StateDiff | ED2 → compute_diff_runs → emit_diff | ✓ WIRED | terminal.rs:606 → server.rs:252-258 → screen.rs:449 |

### Behavioral Spot-Checks (adversarial mutation probes — throwaway, reverted)

| Behavior | Probe | Result | Status |
|----------|-------|--------|--------|
| CR-01 fix is load-bearing | Mutated `reset_with_cursor` to old `epoch_start_col = confirmed.col; needs_epoch_start_sync = false` | `cr01_...` FAILED (clamp stuck at col 9, expected 6) | ✓ PASS (genuine fail-before) |
| D-02 distinguishes PredictBatch from BulkSuppressed | Mutated content-inspection back to flat `>4 → BulkSuppressed` | `typematic_printable_batch_predicts_each_char` + `typematic_batch_advances_cursor` FAILED; `typematic_mixed_batch_with_escape_is_suppressed` still passed | ✓ PASS (genuine fail-before; suppression intact) |
| BUG-H blank-cell emission guards bleed-through | Mutated `emit_diff` skip to also skip `want.ch == ' '` | `bug_h_blank_cell_after_clear_is_emitted` FAILED (only bare cursor MoveTo emitted) | ✓ PASS (genuine fail-before) |
| Full suite | `cargo test -p nosh-client` | 92 lib + all integration pass (1 slow idle test ignored) | ✓ PASS |
| Lint | `cargo clippy -p nosh-client --lib --bins` | clean, no warnings | ✓ PASS |

All probe mutations were reverted; no throwaway code left in the tree.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| screen.rs | 669 | `"XXXXX"` string | ℹ️ Info | Test fixture data (a string of literal X chars), not a debt marker. No action. |

No `TBD` / `FIXME` / unreferenced `TODO` / `HACK` / `PLACEHOLDER` markers in the modified files.

### Adversarial Focus Findings (per verification brief)

1. **BUG-H mid-session Ctrl-L — ACTUALLY fixed.** The executor's claim that D-03a was already correct is accurate: `emit_diff`'s `want == have` skip emits a space when a `'X'` becomes `' '` (they are unequal). The full path is real: Ctrl-L (0x0c) → server ED2 blanks grid → `compute_diff_runs` emits space runs for blank-over-non-blank cells (server.rs:252-258, verified by inspection — fn is private so no probe, but the branch logic is unambiguous) → client applies to confirmed → `emit_diff` writes the space. The `bug_h_blank_cell_after_clear_is_emitted` test reproduces exactly this non-blank→blank scenario and fails when emit_diff is mutated to skip blanks. D-03b (connect-clear) is a separate, additional fix for connect-time bleed-through, not the Ctrl-L mechanism. **Not a gap.**

2. **D-02 typematic — genuine fail-before/pass-after.** Despite the RED+impl landing in a single commit (676aa8d), the assertions genuinely distinguish PredictBatch from the old BulkSuppressed: reverting to the flat `>4 → BulkSuppressed` rule fails both `typematic_printable_batch_predicts_each_char` and `typematic_batch_advances_cursor`, while `typematic_mixed_batch_with_escape_is_suppressed` still passes (suppression contract intact). **Not a gap.**

3. **CR-01 fix integrity — confirmed real.** `reset_with_cursor` (predictor.rs:608-622) re-arms `needs_epoch_start_sync = true` and does NOT assign `epoch_start_col`. The test `cr01_reset_with_cursor_epoch_start_col_follows_new_prompt_not_pre_enter` fails (clamp stuck at the stale pre-Enter col 9) when the fix is reverted to the buggy assignment. WR-01/WR-02/WR-03/IN-01 also have dedicated fix commits and are present in the code. **Not a gap.**

### Gaps Summary

No blocking gaps. All six locked decisions (D-01..D-06) are delivered, wired, and proven by adversarial tests that I independently confirmed are genuine fail-before/pass-after via mutation probes. The prior code review's one critical (CR-01) and three warnings (WR-01/02/03) plus the info item (IN-01) all have dedicated fix commits and are confirmed present in the code; no separate REVIEW-FIX.md was written, but the commit history (173b6e6, 71d071f, 124e126, aefdd96, 5207127) and the code itself substantiate the fixes.

Status is `human_needed` (not `passed`) only because the original defects were live daily-driver UX symptoms (visual screen-clear, predictive-echo feel, per-keystroke latency logging) whose composed end-to-end on-screen / felt behaviour is inherently human-observable. The mechanical correctness is fully verified at the code and test level; the human items are confirmation of the live outcome, not unresolved engineering work.

---

_Verified: 2026-06-05_
_Verifier: Claude (gsd-verifier)_
