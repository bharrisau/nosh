---
phase: 999.3-client-terminal-rendering-correctness-pack-platform-agnostic
plan: 04
type: execute
wave: 2
depends_on:
  - 999.3-03
files_modified:
  - crates/nosh-client/src/main.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-04 (latency): run_pump records a timestamp per forwarded keystroke (per-keystroke RTT) and logs predicted->confirmed latency per keystroke, not per epoch-confirmation time (which included user think-time)"
    - "D-04 is instrumentation only: no user-visible behavior change; keystrokes still go on the reliable stream only; the predict_enqueue_times map still prunes to prevent unbounded growth"
    - "D-03b wiring: run_pump calls screen.emit_connect_clear once after constructing the ClientScreen so the terminal is cleared on connect"
  artifacts:
    - path: "crates/nosh-client/src/main.rs"
      provides: "per-keystroke RTT instrumentation in run_pump (D-04); emit_connect_clear call site on connect (D-03b wiring)"
      contains: "emit_connect_clear"
  key_links:
    - from: "run_pump stdin/enqueue path"
      to: "per-keystroke timestamp map"
      via: "Instant recorded per keystroke, matched on confirm with keystroke_id in the tracing log"
      pattern: "predict_enqueue_times"
    - from: "run_pump ClientScreen::new"
      to: "screen.emit_connect_clear"
      via: "one-shot connect clear written to stdout after screen construction"
      pattern: "emit_connect_clear"
---

<objective>
Two main.rs changes, both in `run_pump`:

- **D-04 (latency rework):** the existing D-17-02a hook records ONE timestamp per `prediction_epoch` and measures epoch-confirmation latency — which includes user think-time and is too coarse to back the "never worse than no prediction" claim. Rework it to measure **per-keystroke RTT** (predicted→confirmed for an individual keystroke). Instrumentation only — no user-visible behavior change.
- **D-03b wiring:** call `screen.emit_connect_clear(...)` once after constructing the `ClientScreen` so the terminal is cleared on connect (the method was added in plan 999.3-03).

Purpose: finer timing evidence (D-04) and completing the connect-clear path (D-03b).
Output: modified `main.rs`.

Depends on 999.3-03 because it calls `ClientScreen::emit_connect_clear` (added there). Both plans in wave 2 touch disjoint files (this = main.rs; plan 02 = predictor.rs), so they run in parallel.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.3-client-terminal-rendering-correctness-pack-platform-agnostic/999.3-CONTEXT.md
@.planning/phases/999.3-client-terminal-rendering-correctness-pack-platform-agnostic/999.3-PATTERNS.md
@.planning/phases/999.3-client-terminal-rendering-correctness-pack-platform-agnostic/999.3-03-SUMMARY.md

<interfaces>
<!-- Key contracts in main.rs run_pump + ClientScreen. -->

run_pump (main.rs line ~761):
- Screen construction: `let mut screen = nosh_client::screen::ClientScreen::new(cols, rows);` (line ~790).
- Latency map declaration: `let mut predict_enqueue_times: std::collections::HashMap<u64, Instant> = std::collections::HashMap::new();` (lines ~826-827).
- Enqueue side (stdin/keystroke arm): `let epoch_required = predictor.prediction_epoch();` then `predict_enqueue_times.entry(epoch_required).or_insert_with(Instant::now);` + `tracing::debug!(target: "nosh::predict", event = "predict", epoch_required, "keystroke predicted");` (lines ~1143-1153).
- Confirm side (datagram arm): `let epoch_before_cull = predictor.confirmed_epoch(); predictor.cull(&screen, diff.epoch, rtt_ms); ... let epoch_after_cull = predictor.confirmed_epoch(); if epoch_after_cull > epoch_before_cull { if let Some(enqueued_at) = predict_enqueue_times.remove(&epoch_after_cull) { let latency_ms = enqueued_at.elapsed().as_millis() as u64; tracing::debug!(target: "nosh::predict", event = "confirm", epoch = epoch_after_cull, latency_ms, "prediction confirmed"); } predict_enqueue_times.retain(|&k, _| k > epoch_after_cull); }` (lines ~991-1011).
- Stdout pattern: `stdout.write_all(&buf).await` + `stdout.flush().await` (e.g. ~1019-1023) with `screen.reset_physical()` on error.

ClientScreen: `pub fn emit_connect_clear<W: Write>(&mut self, out: &mut W) -> std::io::Result<()>` (added in plan 999.3-03 — writes \x1b[2J\x1b[H then reset_physical()).

INVARIANT: keystrokes go on the reliable stream only — never datagrams. D-04 must not change the send path, only add timing.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-03b connect-clear wiring in run_pump</name>
  <files>crates/nosh-client/src/main.rs</files>
  <read_first>
    - crates/nosh-client/src/main.rs (run_pump construction ~761-830, ClientScreen::new ~790, stdout write/flush pattern ~1019-1023)
    - .planning/phases/999.3-client-terminal-rendering-correctness-pack-platform-agnostic/999.3-PATTERNS.md (§ D-03b call site in run_pump)
    - .planning/phases/999.3-client-terminal-rendering-correctness-pack-platform-agnostic/999.3-03-SUMMARY.md (the emit_connect_clear signature as implemented)
  </read_first>
  <action>
    Immediately after `let mut screen = nosh_client::screen::ClientScreen::new(cols, rows);` (~line 790), add a one-shot connect clear: buffer into a `Vec<u8>`, call `screen.emit_connect_clear(&mut init_buf)?` (or match the exact signature from plan 03's SUMMARY — it returns `std::io::Result<()>`), then `stdout.write_all(&init_buf).await` and `stdout.flush().await`, following the established buffer→write_all→flush async pattern used elsewhere in run_pump. This fires once before the first datagram. Do NOT call render_to_stdout here (the confirmed grid is empty at connect; emit_connect_clear already resets physical so the first real render full-repaints). Add a short comment tagging D-03 / BUG-H.
  </action>
  <verify>
    <automated>cargo build -p nosh-client 2>&1 | grep -q "Compiling\|Finished" && grep -q "emit_connect_clear" crates/nosh-client/src/main.rs</automated>
  </verify>
  <acceptance_criteria>
    - `screen.emit_connect_clear(...)` is called once in run_pump right after screen construction, with the result written to stdout via the async write_all+flush pattern.
    - grep `emit_connect_clear` present in main.rs.
    - `cargo build -p nosh-client` succeeds; `cargo clippy -p nosh-client --bins` clean.
  </acceptance_criteria>
  <done>Terminal is cleared on connect via screen.emit_connect_clear; build clean.</done>
</task>

<task type="auto">
  <name>Task 2: D-04 per-keystroke RTT instrumentation rework</name>
  <files>crates/nosh-client/src/main.rs</files>
  <read_first>
    - crates/nosh-client/src/main.rs (map decl ~826-827, enqueue side ~1143-1153, confirm side ~991-1011)
    - .planning/phases/999.3-client-terminal-rendering-correctness-pack-platform-agnostic/999.3-PATTERNS.md (§ D-04 — fix shape, key type change, retain pruning preserved)
    - .planning/phases/999.3-client-terminal-rendering-correctness-pack-platform-agnostic/999.3-CONTEXT.md (§ Claude's Discretion — per-keystroke RTT storage shape)
  </read_first>
  <action>
    Rework the D-17-02a hook from epoch-grained to per-keystroke RTT. Change the `predict_enqueue_times` map key from `u64` (epoch) to a per-keystroke monotonic counter (Claude's discretion: a `u64` keystroke_id incremented per forwarded keystroke, OR a `(u64 epoch, u64 keystroke_id)` pair). Keep the same variable name and the same declaration location (~826-827); keep the value type `Instant`.

    On the enqueue side (~1143-1153): increment the keystroke counter for each forwarded keystroke and `insert` its `Instant::now()` keyed by the keystroke_id. Keep the `tracing::debug!` "keystroke predicted" record and add the `keystroke_id` field.

    On the confirm side (~991-1011): when a prediction confirms (epoch advanced via cull), match the confirmed keystroke(s) to their recorded timestamps and emit per-keystroke `latency_ms` logs mirroring the existing "prediction confirmed" format, adding a `keystroke_id` field. Preserve the `predict_enqueue_times.retain(...)` pruning so the map cannot grow unbounded (adjust the retain predicate to the new key type — prune entries whose keystroke_id is at/below the confirmed watermark).

    INVARIANT: do NOT change the keystroke send path — keystrokes still go on the reliable stream only; this is timing instrumentation only with no user-visible behavior change. Do NOT alter cull(), the render path, or datagram handling beyond the timing logs.

    Verification of "no behavior change": the existing client integration/unit tests must still pass unchanged. (There is no dedicated test for log output; correctness is the build + clippy + full suite passing + manual confirmation the send path is untouched. Note in the SUMMARY that D-04 is instrumentation-only and intentionally has no new behavioral test — its evidence is the tracing log shape.)
  </action>
  <verify>
    <automated>cargo test -p nosh-client 2>&1 | grep -q "test result: ok" && cargo clippy -p nosh-client --bins 2>&1 | grep -qv "warning: "</automated>
  </verify>
  <acceptance_criteria>
    - `predict_enqueue_times` key type changed from `u64` epoch to a per-keystroke counter (or (epoch, keystroke_id) pair); same variable name and decl location.
    - Enqueue side records a timestamp per keystroke and logs a `keystroke_id` field; confirm side logs per-keystroke `latency_ms` with `keystroke_id`.
    - `predict_enqueue_times.retain(...)` pruning preserved (no unbounded growth) — grep `predict_enqueue_times` and `retain` both present.
    - Keystroke send path unchanged (reliable stream only) — no edits to the send_input/write_message call sites.
    - `cargo build -p nosh-client` succeeds; `cargo test -p nosh-client` full suite passes; `cargo clippy -p nosh-client --bins` clean.
  </acceptance_criteria>
  <done>Latency hook measures per-keystroke RTT with keystroke_id logs and bounded map; no user-visible behavior change; suite passes.</done>
</task>

</tasks>

<verification>
- `cargo build -p nosh-client` and `cargo test -p nosh-client` pass on Linux.
- `cargo clippy -p nosh-client --bins` clean.
- emit_connect_clear is wired in run_pump; predict_enqueue_times is keyed per-keystroke with preserved pruning.
- Keystroke send path is unchanged (reliable stream only).
</verification>

<success_criteria>
- D-04: per-keystroke RTT instrumentation replaces epoch-confirmation timing; instrumentation only, no behavior change.
- D-03b: connect-clear is wired into run_pump.
</success_criteria>

<output>
Create `.planning/phases/999.3-client-terminal-rendering-correctness-pack-platform-agnostic/999.3-04-SUMMARY.md` when done. Record the chosen per-keystroke key shape and note D-04 is instrumentation-only.
</output>
