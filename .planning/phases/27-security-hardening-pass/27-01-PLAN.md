---
phase: 27-security-hardening-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - fuzz/fuzz_targets/osc_accumulation.rs
autonomous: true
requirements: [SEC-05]
must_haves:
  truths:
    - "oversized_multi_chunk_osc_is_bounded_then_resyncs passes on every push (CI required check)"
    - "The fuzz target re-runs at -max_len=2097152 without OOM/crash"
    - "Any future change to TerminalState::advance is gated by the OSC-bound regression check in CI"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "OSC-OOM regression gate job (SEC-05 required check)"
      contains: "oversized_multi_chunk_osc_is_bounded_then_resyncs"
    - path: "fuzz/fuzz_targets/osc_accumulation.rs"
      provides: "Documented correct cargo-fuzz invocation (-max_len, not env var)"
      contains: "max_len=2097152"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "crates/nosh-server/src/terminal.rs"
      via: "cargo test -p nosh-server oversized_multi_chunk_osc_is_bounded_then_resyncs"
      pattern: "oversized_multi_chunk_osc_is_bounded_then_resyncs"
---

<objective>
Adversarially re-verify the post-auth OSC-accumulation OOM bound (999.7 / SEC-05) and gate it so it can never silently regress. Per D-01 this is re-verification + regression-gating, NOT a net-new fix: the Phase-19 `osc_prefilter` in `TerminalState::advance` is already correct and category-agnostic (confirmed in 27-RESEARCH.md — it bounds ALL OSC numbers at the byte level before vte sees them).

Purpose: SEC-05 is a post-auth availability DoS that becomes far more dangerous once the server is internet-exposed (Phase 28 UAT validates it). The bound must hold and a required CI check must re-run the named bound test on any change.
Output: A CI required-check job running the bound test; a corrected fuzz invocation documented in the fuzz target.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-security-hardening-pass/27-RESEARCH.md
@.planning/phases/27-security-hardening-pass/27-PATTERNS.md
@docs/999.7-SECURITY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run the OSC-OOM bound test and fuzz target; correct the fuzz invocation doc</name>
  <files>fuzz/fuzz_targets/osc_accumulation.rs</files>
  <read_first>
    - crates/nosh-server/src/terminal.rs (TerminalState::advance, osc_prefilter, the oversized_multi_chunk_osc_is_bounded_then_resyncs test in the test module — READ to confirm the bound test name and that osc_prefilter is category-agnostic per 27-RESEARCH.md §"OSC category prefilter audit")
    - fuzz/fuzz_targets/osc_accumulation.rs (the deterministic 10 MiB multi-chunk path lines ~37-84 + any header comment describing how to run it)
    - 27-RESEARCH.md §"Fuzz invocation correction" and §"Pitfall 3" (the LIBFUZZER_MAX_LEN env var is SILENTLY IGNORED by cargo-fuzz)
  </read_first>
  <action>
    Re-run the named bound test as the first act: `cargo test -p nosh-server oversized_multi_chunk_osc_is_bounded_then_resyncs` — it MUST pass (research measured 1.41 s green). Re-run the fuzz target with the CORRECT invocation `cargo +nightly fuzz run osc_accumulation -- -max_len=2097152 -max_total_time=120` and confirm zero crash artifacts. Then update the header doc comment in fuzz/fuzz_targets/osc_accumulation.rs to record the correct invocation: state explicitly that the `LIBFUZZER_MAX_LEN` environment variable is silently ignored by cargo-fuzz and that `-max_len=2097152` must be passed after the `--` separator (cite 999.7 OSC_ACCUMULATION_MAX = 1 MiB as the bound being exercised). Do NOT delete or weaken the deterministic in-harness 10 MiB test. If re-verification surfaces a real bound gap (it should not, per research), STOP and flag for a tracked gap-closure plan rather than patching here.
  </action>
  <verify>
    <automated>cargo test -p nosh-server oversized_multi_chunk_osc_is_bounded_then_resyncs</automated>
    <automated>grep -q 'max_len=2097152' fuzz/fuzz_targets/osc_accumulation.rs && grep -qi 'LIBFUZZER_MAX_LEN' fuzz/fuzz_targets/osc_accumulation.rs</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p nosh-server oversized_multi_chunk_osc_is_bounded_then_resyncs` exits 0
    - `cargo +nightly fuzz run osc_accumulation -- -max_len=2097152 -max_total_time=120` produces no crash artifact
    - fuzz/fuzz_targets/osc_accumulation.rs header documents `-max_len=2097152` after `--` AND notes the env var is ignored
    - No change to osc_prefilter logic or the bound test (re-verification only)
  </acceptance_criteria>
  <done>The bound test and fuzz target are re-verified green; the correct fuzz invocation is documented in-file.</done>
</task>

<task type="auto">
  <name>Task 2: Add the SEC-05 OSC-OOM regression gate as a required CI check</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (lines 1-63 — the existing `linux`, `build-windows`, `audit` job structure; the v1.3 noecho required-check pattern is the analog)
    - 27-PATTERNS.md §"`.github/workflows/ci.yml`" (CI gate job pattern)
    - 27-RESEARCH.md §"Code Examples" → "CI gate pattern"
  </read_first>
  <action>
    Add a dedicated job `osc-bound-regression` to .github/workflows/ci.yml (after the existing `linux` job), runs-on ubuntu-latest, using `actions/checkout@v4` + `dtolnay/rust-toolchain@stable` + `Swatinem/rust-cache@v2`, whose single test step runs `cargo test --locked -p nosh-server oversized_multi_chunk_osc_is_bounded_then_resyncs`. Name the step "OSC-OOM regression gate (SEC-05)". Add a job-level comment stating this gate exists so that any future change to `TerminalState::advance` re-runs the bound test, and that the client has no vte parser today (so the OSC bound lives solely server-side) — if a client-side vte parser is ever added, an equivalent client gate must be added (forward-risk note per D-02 resolution in 27-02). Keep it a separate job (not folded into `linux`) so it shows as its own required check in branch protection. Do NOT remove or alter the existing `linux`/`build-windows`/`audit` jobs.
  </action>
  <verify>
    <automated>grep -q 'oversized_multi_chunk_osc_is_bounded_then_resyncs' .github/workflows/ci.yml</automated>
    <automated>python3 -c "import yaml,sys; d=yaml.safe_load(open('.github/workflows/ci.yml')); assert 'osc-bound-regression' in d['jobs'], 'job missing'; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` parses as valid YAML and contains a job keyed `osc-bound-regression`
    - That job runs `cargo test --locked -p nosh-server oversized_multi_chunk_osc_is_bounded_then_resyncs`
    - The existing `linux`, `build-windows`, `audit` jobs are unchanged
    - A comment documents the forward-risk that a future client-side vte parser would need its own gate
  </acceptance_criteria>
  <done>CI has a standalone OSC-OOM regression job that re-runs the bound test on every push/PR.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated server process → server TerminalState | PTY output bytes (post-auth, server-controlled) feed vte via TerminalState::advance |
| developer change → TerminalState::advance | A future code change could remove/weaken osc_prefilter, silently reintroducing the OOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Denial of Service | TerminalState::advance / osc_prefilter | mitigate | osc_prefilter caps OSC accumulation at OSC_ACCUMULATION_MAX (1 MiB) before parser.advance(); re-verified by the named bound test (Task 1) |
| T-27-02 | Tampering (regression) | TerminalState::advance over time | mitigate | Required CI check (Task 2) re-runs the bound test on every change; a PR that weakens the prefilter fails the `osc-bound-regression` job |
| T-27-03 | Denial of Service | osc_prefilter category coverage | accept | Research confirmed the prefilter is byte-level and category-agnostic — bounds OSC 0/2/52 AND 7/8/1337/all others; no per-category gap exists |
| T-27-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this plan (27-RESEARCH.md Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `cargo test -p nosh-server oversized_multi_chunk_osc_is_bounded_then_resyncs` passes
- Fuzz target re-run at `-max_len=2097152` produces no crash
- `.github/workflows/ci.yml` is valid YAML with the `osc-bound-regression` job
- `cargo test --workspace --locked` still green
</verification>

<success_criteria>
SEC-05 ROADMAP success criterion #1 satisfied: the bound test passes, the fuzz target re-runs at raised max_len without OOM, the prefilter is confirmed category-agnostic, and a CI gate prevents future regression from changes to `TerminalState::advance`.
</success_criteria>

<output>
Create `.planning/phases/27-security-hardening-pass/27-01-SUMMARY.md` when done
</output>
