---
phase: 27-security-hardening-pass
plan: 03
type: execute
wave: 2
depends_on: ["27-01", "27-02"]
files_modified:
  - docs/SECURITY.md
autonomous: true
requirements: [SEC-01]
must_haves:
  truths:
    - "D-08: docs/SECURITY.md exists and a security reviewer can understand nosh's internet-exposed trust model from it — it covers assets, trust boundaries, attacker capabilities, the proxy trust model, the Mode A vs Mode B distinction, the mandatory-inner-auth rationale, and residual risks"
    - "D-09: the doc documents the outer-CA-cert-but-inner-auth-is-authoritative composition (a terminating/compromised proxy cannot MITM auth) and references the RFC 9266 EKM channel binding"
    - "Every SEC-04 mitigation (D-02..D-07) and the SEC-05 OSC bound is referenced against the code that actually shipped in 27-01/27-02"
  artifacts:
    - path: "docs/SECURITY.md"
      provides: "STRIDE-structured threat model for operators + security reviewers"
      contains: "STRIDE"
      min_lines: 120
  key_links:
    - from: "docs/SECURITY.md"
      to: "crates/nosh-client/src/main.rs + crates/nosh-server/src/terminal.rs"
      via: "references shipped SEC-04/SEC-05 mitigations by D-ID and file"
      pattern: "D-0[2-7]"
---

<objective>
Author `docs/SECURITY.md` (SEC-01): the threat-model document that makes the server safe-to-expose claim auditable. Per D-08/D-09 it covers assets + trust boundaries, attacker capabilities in an internet-exposed deployment, the proxy trust model, the Mode A vs Mode B distinction, the mandatory-inner-auth rationale, and residual risks — and explicitly documents the outer-CA-cert-but-inner-auth-is-authoritative composition from Phase 24/25 (a terminating/compromised proxy is contained because the inner SSH-key handshake, RFC 9266 EKM-bound, is the real end-to-end trust anchor).

Structure (Claude's Discretion → recommended): STRIDE, audience = operators + security reviewers. This task is WAVED AFTER the SEC-04 code (27-02) and SEC-05 gate (27-01) so it documents what ACTUALLY SHIPPED, not aspirational mitigations — draft-early-finalise-late per D-08/D-09.

Purpose: SEC-01 is the written artifact a security reviewer reads before trusting an internet-exposed nosh server. Output: docs/SECURITY.md.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-security-hardening-pass/27-RESEARCH.md
@.planning/phases/27-security-hardening-pass/27-PATTERNS.md
@docs/999.1-SECURITY.md
@docs/999.7-SECURITY.md
@.planning/phases/27-security-hardening-pass/27-01-SUMMARY.md
@.planning/phases/27-security-hardening-pass/27-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/SECURITY.md as a STRIDE-structured threat model referencing the shipped mitigations</name>
  <files>docs/SECURITY.md</files>
  <read_first>
    - 27-RESEARCH.md §"SEC-01: Threat-Model Document Structure" (the full 8-section recommended outline) and §"Pitfall 5" (finalise after SEC-04 lands)
    - 27-01-SUMMARY.md and 27-02-SUMMARY.md (what ACTUALLY shipped — the real constant names, file locations, and test names to cite; do NOT invent — read these summaries for ground truth)
    - docs/999.1-SECURITY.md (pre-auth caps: AuthLimits max_concurrent=64, auth_timeout=5s, 1 MiB datagram buffers; residual RUSTSEC-2023-0071 rsa Marvin timing — accepted, parsing-only)
    - docs/999.7-SECURITY.md (OSC_ACCUMULATION_MAX = 1 MiB, osc_prefilter design, the named bound test)
    - 27-PATTERNS.md §"docs/SECURITY.md" + §"Structure pattern" (follow the existing 999.x doc voice)
    - .planning/STATE.md §"Decisions" (Phase 24 outer-CA-cert + Phase 25 RFC 9266 EKM binding / CSPRNG-nonce fallback; Mode A committed, Mode B Envoy stretch-only; InnerAuthFail fieldless)
    - CLAUDE.md security invariants (env sanitization LD_*/BASH_ENV; SSH_AUTH_SOCK never forwarded via env)
  </read_first>
  <action>
    Create docs/SECURITY.md following the STRIDE outline in 27-RESEARCH.md §"SEC-01 Threat-Model Document Structure". Use Australian English, plain professional voice. Required sections (D-08): (1) Scope and Topology — Mode A (nosh binds UDP/443 directly, the committed deliverable) vs Mode B (WebTransport behind an HTTP/3 proxy, stretch-only) with their trust-model differences; (2) Assets and Trust Boundaries — SSH identity (client private key, held in ssh-agent), session state (PTY, scrollback, reattach token), server host key (known_hosts/TOFU), and the boundaries client↔network, network↔server, proxy↔server; (3) The Proxy Trust Model (D-09) — what the outer WebTransport TLS provides (a Let's Encrypt cert from a sidecar: confidentiality + HTTPS/proxy-friendliness), what the inner SSH-key handshake provides (end-to-end identity + RFC 9266 EKM channel binding — note the CSPRNG-nonce fallback if EKM is unavailable, per Phase 25), and explicitly: a terminating/compromised proxy CANNOT MITM authentication because the inner handshake is the authoritative trust anchor (outer-CA-cert-but-inner-auth-is-authoritative); (4) Attacker Capabilities — passive network, active MITM pre-inner-auth, unauthenticated pre-auth DoS, authenticated malicious client, and compromised/malicious server sending terminal sequences; (5) STRIDE Threat Table — Spoofing (inner-auth EKM binding; InnerAuthFail fieldless = no key-existence oracle), Tampering (OSC/title/clipboard/hyperlink injection → mitigated by SEC-04 D-02..D-07, cite the real file+constant from 27-02-SUMMARY), Repudiation (out of scope — shell responsibility), Information Disclosure (OSC 52 read dropped; reattach token only after inner-auth completes), Denial of Service (OSC OOM → SEC-05 osc_prefilter 1 MiB + CI gate from 27-01; pre-auth flood → 64-slot cap + 5 s timeout; resize → MIN_RESIZE_INTERVAL_MS; PtyData → MAX_PTYDATA_FRAME_BYTES), Elevation of Privilege (env sanitization, SSH_AUTH_SOCK not forwarded, OSC 8 scheme whitelist); (6) Shipped Mitigations — reference each by file + D-ID using the ACTUAL names from the two SUMMARYs (do not paraphrase from memory; read them); (7) Residual Risks — RUSTSEC-2023-0071 (rsa 0.9.10 Marvin timing, accepted/parsing-only), ssh-agent compromise (out of scope), physical client access (out of scope); (8) Operator Checklist for Mode A deployment. Every mitigation claim MUST cite code that landed in 27-01/27-02 or a prior shipped phase — if a SUMMARY shows a mitigation was implemented differently than the research predicted, document what shipped, not the prediction. End the file with a short "Verify before relying on this" note (the named bound test, the CI gate job, `cargo test --workspace`) consistent with the 999.x doc convention. Add a one-line note that this doc is operator/reviewer guidance and should be reviewed by the responsible security owner before an external deployment relies on it.
  </action>
  <verify>
    <automated>test -f docs/SECURITY.md</automated>
    <automated>grep -qi 'STRIDE' docs/SECURITY.md && grep -qi 'inner' docs/SECURITY.md && grep -qi 'Mode A' docs/SECURITY.md && grep -q 'RFC 9266' docs/SECURITY.md</automated>
    <automated>grep -Eq 'D-0[2-7]' docs/SECURITY.md && grep -qi 'osc_prefilter' docs/SECURITY.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/SECURITY.md exists (≥ 120 lines) and contains all 8 sections (scope/topology, assets/boundaries, proxy trust model, attacker capabilities, STRIDE table, shipped mitigations, residual risks, operator checklist)
    - The outer-CA-cert-but-inner-auth-is-authoritative composition is stated explicitly (a compromised proxy cannot MITM auth) and references RFC 9266 EKM binding
    - Mode A (committed) vs Mode B (stretch) distinction is present
    - SEC-04 mitigations are cited by D-ID (D-02..D-07) and the SEC-05 osc_prefilter + CI gate are referenced
    - Residual RUSTSEC-2023-0071 is documented as accepted (parsing-only)
    - Mitigation claims match the shipped code in 27-01/27-02 SUMMARYs (no invented constants/file paths)
  </acceptance_criteria>
  <done>docs/SECURITY.md is a complete STRIDE threat model documenting the as-shipped internet-exposed trust model and mitigations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| document ↔ reader | A security reviewer / operator relies on this doc to assess internet exposure; an inaccurate doc creates false assurance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-11 | Repudiation / false-assurance | docs/SECURITY.md accuracy | mitigate | The doc is waved AFTER 27-01/27-02; it cites real shipped code from the SUMMARYs, not predictions — finalise-late per D-08/D-09 / Pitfall 5 |
| T-27-12 | Information Disclosure | doc content | accept | The doc describes mitigations, not secrets; no key material or token values are included |
| T-27-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this plan (docs only) |
</threat_model>

<verification>
- `docs/SECURITY.md` exists with all 8 sections
- STRIDE structure, Mode A/B, RFC 9266 inner-auth composition, and D-02..D-07 references all present
- Mitigation references match 27-01/27-02 SUMMARY ground truth
</verification>

<success_criteria>
SEC-01 ROADMAP success criterion #4 satisfied: docs/SECURITY.md exists and covers assets and trust boundaries, attacker capabilities in an internet-exposed deployment, the proxy trust model, the Mode A vs Mode B distinction, the mandatory-inner-auth rationale, and residual risks — documenting what actually shipped.
</success_criteria>

<output>
Create `.planning/phases/27-security-hardening-pass/27-03-SUMMARY.md` when done
</output>
