---
phase: 999.4-predictive-echo-repaint-pacing-live-fix-round-2
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/nosh-client/src/predictor.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-03 instrumentation: every on_input call emits one tracing::debug! event (target nosh::predict, event=on_input) recording action-kind tag (NO char content), pending_before, pending_after, predictions_dropped, predicted cursor — so the operator can reproduce mash/hold and see exactly when/why letters vanish"
    - "D-02: pressing Enter at an echoing prompt positively predicts the command-submitting line-advance (predicted cursor drops to col 0 of next row; scroll-at-bottom stays on row); Tab/ESC/other controls still EpochReset"
    - "D-02: Enter prediction does not mispredict during a noecho secret (tentative machinery suppresses it structurally); CR-01 / reset_with_cursor epoch-start-col behaviour and all 999.3 noecho tests preserved"
    - "D-03 hypothesis fix: a BulkSuppressed batch with pending>0 preserves already-shown predictions (become_tentative, not reset+clear) instead of erasing them until the next confirming datagram"
  artifacts:
    - path: "crates/nosh-client/src/predictor.rs"
      provides: "InputAction::PredictEnter variant + classify_input Enter→PredictEnter + on_input PredictEnter arm; action_kind_tag helper + on_input decision-trace tracing event; BulkSuppressed become_tentative-when-pending fix"
      contains: "PredictEnter"
  key_links:
    - from: "classify_input b\"\\r\"/b\"\\n\" arm (~792)"
      to: "InputAction::PredictEnter on_input arm (~415 region)"
      via: "new variant classification + handler"
      pattern: "PredictEnter"
    - from: "on_input (~326)"
      to: "tracing::debug! target nosh::predict event=on_input"
      via: "decision-trace after the match block, via action_kind_tag"
      pattern: "event = \"on_input\""
    - from: "BulkSuppressed arm (~424)"
      to: "become_tentative()"
      via: "branch on self.pending.is_empty()"
      pattern: "BulkSuppressed"
---

<objective>
Implement D-02 (predict the command-submitting Enter line-advance) and D-03 (instrument the
fast-typing "letters disappearing" glitch, then apply the hypothesis fix). Both live in
`predictor.rs`; they share the file, so they are sequenced in one plan to avoid file contention.

Purpose:
- D-03 instrumentation is the PRIMARY deliverable the operator explicitly asked for — a
  decision-trace that lets them reproduce (mash/hold) and confirm exactly when/why predictions
  are dropped. It lands first and is useful even if the root cause differs from the hypothesis.
- D-02 fixes the `read -s` newline: Enter at an echoing prompt should immediately drop the caret
  a row (Mosh newline_carriage_return style), reconciled with the noecho/tentative machinery so
  it does not mispredict during the hidden secret.
- D-03 hypothesis fix stops a mid-burst BulkSuppressed from erasing already-visible predictions.
Output: a `PredictEnter` input action with cursor line-advance, an `on_input` decision-trace
tracing event, and a BulkSuppressed-preserves-pending fix — each bundled with adversarial
fail-before/pass-after tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/999.4-predictive-echo-repaint-pacing-live-fix-round-2/999.4-CONTEXT.md
@.planning/phases/999.4-predictive-echo-repaint-pacing-live-fix-round-2/999.4-RESEARCH.md
@.planning/phases/999.4-predictive-echo-repaint-pacing-live-fix-round-2/999.4-PATTERNS.md

<interfaces>
<!-- Existing predictor.rs primitives the executor uses directly (do NOT reinvent). -->
- InputAction enum (~99-139): PredictChar{..}, PredictBatch(Vec<..>), PredictBackspace,
  PredictCursorLeft/Right, PredictLineStart/End, EpochReset, BulkSuppressed,
  BracketedPasteStart/End. Insert PredictEnter after EpochReset (~123).
- classify_input(bytes: &[u8]) -> InputAction (~738): [b'\r'] | [b'\n'] currently → EpochReset (~792).
  Tab (~795), ESC [0x1b,..] (~814), other <0x20 control (~817) stay EpochReset.
- on_input(&mut self, bytes: &[u8], screen: &ClientScreen) (~326): match action { ... }.
- become_tentative(&mut self) (~578-580): increments prediction_epoch ONLY.
- reset(&mut self) (~585-596): pending.clear() + become_tentative() + cursor_motion_pending=false
  + needs_epoch_start_sync=true.
- reset_with_cursor(&mut self, confirmed) (~608-622): reset() + predicted_cursor = confirmed.
- self.pending: VecDeque<PendingPrediction>; self.predicted_cursor: CursorPos { row, col };
  self.cursor_motion_pending: bool; self.needs_epoch_start_sync: bool; self.term_rows: u16 (~183).
- Test helpers: make_screen(cols, rows) (~860), PredictionOverlay::new(mode, cols, rows),
  PredictDisplayMode::Always, make_diff_with_char/make_diff_empty.
- Existing tracing analog (main.rs ~1054 and ~1223): target: "nosh::predict", event = "...",
  named fields. Mirror style; log NO character content (T-15-08).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-03 instrumentation — action_kind_tag helper + on_input decision-trace event</name>
  <files>crates/nosh-client/src/predictor.rs</files>
  <read_first>
    - crates/nosh-client/src/predictor.rs (the on_input preamble at ~326, the match action block, and the InputAction enum at ~99-139 to enumerate every variant for the tag helper)
    - crates/nosh-client/src/main.rs (existing tracing analogs at ~1054-1061 event="confirm" and ~1223-1229 event="predict" — field-first style to mirror; read only, do NOT modify main.rs)
    - .planning/phases/999.4-.../999.4-RESEARCH.md — Q3 instrumentation design (single event AFTER the match, fields: batch_len, action kind, pending_before/after, predictions_dropped, predicted cursor) and the T-15-08 no-char-content rule
    - .planning/phases/999.4-.../999.4-PATTERNS.md — D-03 surgery site 1 (on_input preamble capture pending_before), surgery site 2 (action_kind_tag helper, Option (b): tag-before-match to avoid touching match arms)
  </read_first>
  <action>
    Add a module-private `fn action_kind_tag(a: &InputAction) -> &'static str` near `classify_input`
    (~737) that maps every InputAction variant to a static string tag, with NO char/batch content
    (PredictBatch with len>1 → "PredictBatch", len<=1 → "PredictChar"). Include a "PredictEnter" arm
    now so it is ready for Task 2 (PredictEnter is added in Task 2 — if it does not yet compile,
    add the variant stub in Task 2 first; sequence so the tag helper enumerates all variants the
    enum will have). In `on_input` (~326): capture `let pending_before = self.pending.len();` before
    `classify_input`, and capture `let action_kind = action_kind_tag(&action);` BEFORE the `match action`
    block (Option (b) — avoids restructuring the existing match arms which move `action`). After the
    match block, emit ONE `tracing::debug!(target: "nosh::predict", event = "on_input", ...)` with
    fields: batch_len = bytes.len(), action = action_kind, pending_before, pending_after =
    self.pending.len(), predictions_dropped = pending_before.saturating_sub(self.pending.len()),
    pred_row = self.predicted_cursor.row, pred_col = self.predicted_cursor.col. Do NOT log any
    character content or raw bytes (T-15-08). Mirror the main.rs field-first style. This is the
    primary deliverable — make the event self-sufficient for an operator capture (mash/hold repro).
    No fenced code blocks in the implementation beyond the actual edits.
  </action>
  <acceptance_criteria>
    - `cargo build -p nosh-client` compiles clean.
    - `cargo clippy -p nosh-client --all-targets` clean.
    - `grep -v '^[[:space:]]*//' crates/nosh-client/src/predictor.rs | grep -c 'event = "on_input"'` == 1 (exactly one decision-trace event).
    - The tracing event includes `predictions_dropped` and `pending_before`/`pending_after` fields (grep present); action_kind_tag contains no `ch` or batch-content interpolation.
    - All existing predictor tests still pass: `cargo test -p nosh-client` green.
  </acceptance_criteria>
  <verify>
    <automated>cargo build -p nosh-client && cargo clippy -p nosh-client --all-targets 2>&1 | grep -c 'warning:' | grep -qx 0 && cargo test -p nosh-client</automated>
  </verify>
  <done>on_input emits one nosh::predict event=on_input decision-trace per call (action-kind tag, pending before/after, predictions_dropped, predicted cursor), no char content; action_kind_tag helper covers every variant; build/clippy/tests clean.</done>
</task>

<task type="auto">
  <name>Task 2: D-02 — PredictEnter variant, classify_input split, on_input line-advance arm + tests</name>
  <files>crates/nosh-client/src/predictor.rs</files>
  <read_first>
    - crates/nosh-client/src/predictor.rs (InputAction enum ~99-139; classify_input Enter arm ~792 and Tab/ESC/control arms ~795/814/817; the EpochReset on_input arm ~415-422; reset_with_cursor ~608-622 for field-mutation order; the existing classify_enter_epoch_reset test ~1067-1078; adversarial test analogs bug_e/bug_f/cr01 ~1929-2220)
    - .planning/phases/999.4-.../999.4-RESEARCH.md — Q2 (Mosh newline_carriage_return: col→0, advance row unless on last row where scroll is NOT predicted; noecho suppression handled by tentative machinery; Pitfall 3/4 noecho-cursor-visibility and the classify_enter test update; recommended tests enter_predicts_line_advance_cursor + enter_at_last_row_stays_on_row)
    - .planning/phases/999.4-.../999.4-PATTERNS.md — D-02 surgery sites 1-3 (enum insert, classify_input replace, on_input PredictEnter arm mirroring reset_with_cursor field order), the adversarial test doc-comment style, and the test list
  </read_first>
  <action>
    Add `InputAction::PredictEnter` to the enum after `EpochReset` (~123) with the doc-comment from
    PATTERNS D-02 surgery site 1. In `classify_input`, change the Enter arm (~792) from
    `[b'\r'] | [b'\n'] => InputAction::EpochReset` to `=> InputAction::PredictEnter`. Leave Tab
    (~795), ESC (~814), and other <0x20 control (~817) as EpochReset — only Enter is split off.
    Add an `on_input` match arm for `PredictEnter` (near the EpochReset arm ~415) mirroring
    reset_with_cursor's FIELD ORDER: `self.pending.clear()` → `self.become_tentative()` →
    `self.cursor_motion_pending = false` → `self.needs_epoch_start_sync = true`; then compute
    `let row = self.predicted_cursor.row;` and set predicted_cursor to `CursorPos { row: row+1, col: 0 }`
    if `row < self.term_rows.saturating_sub(1)` else `CursorPos { row, col: 0 }` (scroll-at-bottom:
    no row advance — matches Mosh, which does NOT predict scroll); finally
    `self.cursor_motion_pending = true`. Do NOT add an explicit noecho-mode flag — the tentative/epoch
    machinery suppresses the subsequent hidden secret structurally (RESEARCH anti-pattern). The
    EpochReset arm (with its D-05 reset_with_cursor BUG-F behaviour) stays intact for Tab/ESC/other.
    Ensure action_kind_tag (Task 1) has the PredictEnter arm.
    Update the existing `classify_enter_epoch_reset` test (~1067-1078) to assert `PredictEnter` for
    both b"\r" and b"\n", and rename it `classify_enter_predict_enter`.
    Add two adversarial tests in the PATTERNS doc-comment style (FAIL BEFORE / PASS AFTER):
      - `enter_predicts_line_advance_cursor`: press Enter at row 5 (not last row) on an 80x24 screen;
        assert predicted_cursor == CursorPos { row: 6, col: 0 } and cursor_motion_pending == true.
      - `enter_at_last_row_stays_on_row`: press Enter at row 23 (last row of 24); assert
        predicted_cursor == CursorPos { row: 23, col: 0 } (no advance) and cursor_motion_pending == true.
    Do not regress the 999.3 noecho tests (noecho_suppression, noecho_read_dash_s_zero_predicted_chars)
    or cr01/bug_e/bug_f. No fenced code blocks in the implementation beyond the actual edits/tests.
  </action>
  <acceptance_criteria>
    - `cargo test -p nosh-client classify_enter_predict_enter` exits 0 (asserts PredictEnter for CR and LF).
    - `cargo test -p nosh-client enter_predicts_line_advance_cursor` exits 0 (row 5 → predicted (6,0), cursor_motion_pending true).
    - `cargo test -p nosh-client enter_at_last_row_stays_on_row` exits 0 (row 23 of 24 → predicted (23,0), no advance).
    - `cargo test -p nosh-client` fully green — ALL existing 999.3 predictor tests pass (noecho_suppression, noecho_read_dash_s_zero_predicted_chars, cr01, bug_e, bug_f, typematic_*).
    - `cargo clippy -p nosh-client --all-targets` clean.
    - Tab and ESC still classify as EpochReset (grep: classify_input Tab/ESC arms unchanged → EpochReset present).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p nosh-client classify_enter_predict_enter && cargo test -p nosh-client enter_predicts_line_advance_cursor && cargo test -p nosh-client enter_at_last_row_stays_on_row && cargo test -p nosh-client && cargo clippy -p nosh-client --all-targets 2>&1 | grep -c 'warning:' | grep -qx 0</automated>
  </verify>
  <done>Enter classifies as PredictEnter and positively predicts the line-advance (scroll-at-bottom handled); Tab/ESC still EpochReset; updated + two new adversarial tests pass; no 999.3 regression; clippy clean.</done>
</task>

<task type="auto">
  <name>Task 3: D-03 hypothesis fix — BulkSuppressed preserves pending + fail-before/pass-after test</name>
  <files>crates/nosh-client/src/predictor.rs</files>
  <read_first>
    - crates/nosh-client/src/predictor.rs (the BulkSuppressed on_input arm ~424-430; become_tentative ~578-580; reset ~585-596; the BulkSuppressed classification path in classify_input ~759-780; adversarial test analogs ~1929-2220)
    - .planning/phases/999.4-.../999.4-RESEARCH.md — Q3 hypothesis fix (BulkSuppressed → become_tentative when pending>0, reset when empty; Pitfall 5 — at worst a stale prediction survives until next cull, the same RTT-bounded latency the tentative mechanism already accepts) and the bulk_suppressed_does_not_drop_existing_predictions test shape
    - .planning/phases/999.4-.../999.4-PATTERNS.md — D-03 surgery site 3 (the exact branch) and the adversarial test (setup: on_input b"a" → pending 1; on_input b"ab\x1bcd" → assert pending still 1)
  </read_first>
  <action>
    Change the BulkSuppressed on_input arm (~424-430) from unconditional `self.reset()` to: if
    `self.pending.is_empty()` call `self.reset()` (harmless, cleaner state); else call
    `self.become_tentative()` and set `self.needs_epoch_start_sync = true` — preserving already-shown
    predictions so a mash/key-repeat burst that classifies as BulkSuppressed does not erase visible
    letters until the next confirming datagram. cull() will reconcile them against the confirmed grid
    on the next datagram (~RTT) — the same trade-off Mosh accepts (RESEARCH Pitfall 5). Update the arm's
    doc-comment to reflect the new behaviour.
    Add an adversarial fail-before/pass-after test `bulk_suppressed_does_not_drop_existing_predictions`
    in the PATTERNS doc-comment style: make_screen(80,24) + PredictionOverlay::new(Always,80,24);
    `on_input(b"a", &screen)` then assert pending.len()==1 (setup); `on_input(b"ab\x1bcd", &screen)`
    (a mixed batch that classifies BulkSuppressed); assert pending.len()==1 AFTER, with the message
    documenting FAIL-BEFORE (reset cleared to 0) vs PASS-AFTER (become_tentative preserved 1). Verify
    b"ab\x1bcd" actually classifies as BulkSuppressed via the classify_input path — if not, choose a
    batch that does (e.g. one exceeding BULK_SUPPRESS_THRESHOLD with a control byte) and document it.
    Do not regress any existing test. No fenced code blocks in the implementation beyond the edit/test.
  </action>
  <acceptance_criteria>
    - `cargo test -p nosh-client bulk_suppressed_does_not_drop_existing_predictions` exits 0 (pending preserved at 1 after a BulkSuppressed batch with pending>0).
    - `cargo test -p nosh-client` fully green — all existing predictor/screen/server-adjacent client tests pass (cr01, bug_e, bug_f, typematic_*, noecho_*).
    - `cargo clippy -p nosh-client --all-targets` clean.
    - The BulkSuppressed arm branches on `self.pending.is_empty()` (grep: `is_empty` within the BulkSuppressed arm / become_tentative reachable from BulkSuppressed).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p nosh-client bulk_suppressed_does_not_drop_existing_predictions && cargo test -p nosh-client && cargo clippy -p nosh-client --all-targets 2>&1 | grep -c 'warning:' | grep -qx 0</automated>
  </verify>
  <done>BulkSuppressed preserves visible predictions when pending>0 (become_tentative, not reset+clear); fail-before/pass-after test passes; full client suite green; clippy clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stdin→predictor | local keystroke bytes enter the prediction engine; logged as decision-trace events |
| predictor→tracing log | decision-trace events emitted; must never leak typed characters |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.4-03 | Information Disclosure | D-03 on_input decision-trace tracing event | mitigate | Log only action-kind tag (action_kind_tag), counts, and predicted cursor — NEVER raw bytes or character content (T-15-08). PredictBatch logs as a tag, not its contents. |
| T-999.4-04 | Information Disclosure | D-02 PredictEnter cursor prediction during noecho secret | mitigate | No explicit noecho flag; the tentative/epoch machinery keeps the prediction hidden until confirmed. confirmed_epoch never advances under read -s, so the secret line-advance stays suppressed. Covered by preserved noecho_* tests. |
| T-999.4-05 | Tampering | D-03 BulkSuppressed become_tentative path | accept | At worst a stale prediction survives until the next cull (~RTT); cull reconciles against the confirmed grid and resets on mismatch. Same RTT-bounded latency the tentative mechanism already accepts (RESEARCH Pitfall 5). |
| T-999.4-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies — tracing/ed25519 etc already in tree. No package-legitimacy gate required. |
</threat_model>

<verification>
- `cargo build -p nosh-client`, `cargo clippy -p nosh-client --all-targets` clean.
- `cargo test -p nosh-client` fully green including the three new tests
  (classify_enter_predict_enter, enter_predicts_line_advance_cursor, enter_at_last_row_stays_on_row,
  bulk_suppressed_does_not_drop_existing_predictions) and ALL preserved 999.3 tests
  (cr01, bug_e, bug_f, typematic_*, noecho_*).
- Manual inspection: exactly one event="on_input" decision-trace; no char content in any trace;
  EpochReset arm intact for Tab/ESC; BulkSuppressed branches on pending emptiness.
</verification>

<success_criteria>
- D-02 satisfied: Enter positively predicts the command-submitting line-advance (scroll-at-bottom
  handled), reconciled with the noecho/tentative machinery; CR-01/reset_with_cursor and all 999.3
  noecho tests preserved.
- D-03 satisfied: instrumentation (primary deliverable) emits a per-on_input decision-trace with
  no char content; the hypothesis fix preserves visible predictions across a BulkSuppressed burst.
</success_criteria>

<output>
Create `.planning/phases/999.4-predictive-echo-repaint-pacing-live-fix-round-2/999.4-02-SUMMARY.md` when done.
Note any classify_input batch chosen for the BulkSuppressed test and confirm it classifies as BulkSuppressed.
</output>
