---
phase: 999.4-predictive-echo-repaint-pacing-live-fix-round-2
verified: 2026-06-05T00:00:00Z
status: passed
score: 5/5 shipped truths verified (gap closed post-verification)
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: "5/5 shipped truths verified (1 test-quality warning)"
  note: >
    The single gap — the non-adversarial WR-01 regression test — was closed after
    verification (commit strengthening wr01_mid_epoch_bulk_suppressed_does_not_tighten_epoch_start_col:
    removed the intervening col-2 sync so the flag-consuming sync happens at the mid-line
    col 5). Mutation-verified: re-inserting the WR-01 bug now FAILS the test; the fix passes
    it. Production code was already correct (per the verifier's faithful repro). All shipped
    truths (D-02 PredictEnter, D-03 instrumentation + BulkSuppressed fix, D-01 cleanly
    reverted) verified. Status raised to passed.
gaps:
  - truth: "WR-01 regression test genuinely guards against BUG-E reintroduction in the mid-epoch BulkSuppressed path"
    status: partial
    reason: >
      The PRODUCTION FIX is correct and proven (faithful repro: epoch_start_col tightens
      2→5 with the bug, stays at 2 without it). But the committed regression test
      `wr01_mid_epoch_bulk_suppressed_does_not_tighten_epoch_start_col` does NOT fail-before:
      re-inserting `self.needs_epoch_start_sync = true` into the BulkSuppressed else-branch
      (the exact WR-01 bug) leaves the test GREEN. The test's confirm diff uses cursor col 2
      (predictor.rs:2627, make_diff_with_char(2,0,2,'a')) instead of the mid-line col 5, so
      the spuriously-set flag is consumed harmlessly at a col-2 sync before the col-5 sync
      fires. The test is non-adversarial — it cannot detect the regression it claims to guard.
    artifacts:
      - path: "crates/nosh-client/src/predictor.rs"
        issue: >
          Test wr01_mid_epoch_bulk_suppressed_does_not_tighten_epoch_start_col (line 2586):
          step-4 confirm diff at line 2627 places the confirmed cursor at col 2, not the
          mid-line col 5. The needs_epoch_start_sync flag (if set by the bug) is consumed at
          the line-2630 sync with confirmed.col=2 (after cull empties pending), capturing the
          unchanged value 2 — so the line-2643 assertion passes regardless of the bug.
    missing:
      - >
        Rework the confirm step so the flag, if set by the bug, survives until a sync whose
        confirmed cursor is at the mid-line column (col 5). A working pattern (verified during
        this review): keep pending non-empty across the first confirming sync, then deliver a
        confirm datagram with runs covering "abc" AND cursor at col 5 so cull empties pending
        and the same sync fires the epoch_start_col capture at col 5. With the bug present this
        yields epoch_start_col=5 (test fails); with the fix it stays 2 (test passes).
---

# Phase 999.4: Predictive-echo & repaint-pacing live-fix round 2 — Verification Report

**Phase Goal (as shipped):** Deliver D-02 (predict the command-submitting Enter's line-advance so a `read -s` prompt drops the line) and D-03 (predictor decision-trace instrumentation + the BulkSuppressed-preserves-pending mash-glitch fix). D-01 (burst repaint pacing) was implemented then REVERTED and DEFERRED to phase 999.6 — verify it is genuinely gone.

**Verified:** 2026-06-05
**Status:** gaps_found (1 WARNING — production fixes all correct; one regression test is non-adversarial)
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | D-01 burst repaint pacing is genuinely reverted; server.rs back to pre-burst state | ✓ VERIFIED | `git diff 10710e6 HEAD -- crates/nosh-server/src/server.rs` is EMPTY (byte-identical to pre-burst). No `datagram_send_buffer_space`, no `'pump:`, no `MAX_DATAGRAMS_PER_TICK`, no burst loop. Diff send arm is single `if let Some(result)=build_state_diff(...)` → one `send_datagram` per tick (server.rs:676-712, 1168-1196). `build_state_diff` uses unconditional `fresh_runs` (server.rs:333). Burst test gone (`grep burst_drains` → none). Server suite 86/86 (pre-burst count). |
| 2 | D-01 infinite-spin & noecho-epoch regressions are absent | ✓ VERIFIED | `cargo test -p nosh-client --test auth` → 6 pass in **0.27s** (the spin bug had hung `mutual_auth_inprocess_happy_path`/`preauth_flood_bounded` >60s). `--test predict noecho_read_dash_s_zero_predicted_chars` → pass in 11.24s (no hang). |
| 3 | D-02 PredictEnter genuinely predicts the Enter line-advance | ✓ VERIFIED | classify_input: `\r`/`\n` → PredictEnter (predictor.rs:895); Tab→EpochReset (:898), ESC→EpochReset (:917), other controls→EpochReset (:920). on_input PredictEnter arm (:453-481): clear→become_tentative→needs_epoch_start_sync=true→epoch_start_col=0→cursor (row+1,0) if `row<term_rows-1` else (row,0)→cursor_motion_pending=true. **Mutation proof:** reverting Enter→EpochReset fails all 3 D-02 tests (classify_enter_predict_enter, enter_predicts_line_advance_cursor, enter_at_last_row_stays_on_row). Live `read -s` test exercises Enter+noecho together: secret chars structurally suppressed, confirmed_epoch never advances. |
| 4 | D-03 BulkSuppressed-preserves-pending fix is genuine + WR-01 fix correct (BUG-E not reintroduced) | ✓ VERIFIED (production) / ⚠️ test non-adversarial | BulkSuppressed arm (:482-503): `if pending.is_empty(){reset()} else {become_tentative()}` — no `needs_epoch_start_sync=true` (WR-01 fix applied). **Production fix proven correct:** faithful repro shows with the bug `epoch_start_col` tightens 2→5; without it stays 2. `bulk_suppressed_does_not_drop_existing_predictions` passes and is adversarial (reset() mutation would drop pending). **WARNING:** the `wr01_*` test itself is non-adversarial — see Gaps. |
| 5 | D-03 instrumentation complete: exactly one trace event on ALL exit paths, no char/byte content (T-15-08) | ✓ VERIFIED | `trace_exit!` macro (predictor.rs:340-354) called at all 5 exit points: 4 early returns (:359, :367, :524, :535) + final (:556). `grep -c 'event = "on_input"'` (non-comment) == 1. Fields are counts/tags only: `batch_len`, `action`=`action_kind_tag()` (static strings, :816-832), `pending_before/after`, `predictions_dropped`, `pred_row/col`. No `ch`/byte/`predicted_ch` content. |

**Score:** 5/5 shipped truths verified at the production-code level. One WARNING on regression-test quality (truth 4).

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `crates/nosh-server/src/server.rs` | NO D-01 burst code — pre-burst state | ✓ VERIFIED | Byte-identical to 10710e6; single send_datagram/tick |
| `crates/nosh-client/src/predictor.rs` | PredictEnter + trace + BulkSuppressed fix | ✓ VERIFIED | All three present, wired, substantive; 96 lib tests pass |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| classify_input | on_input PredictEnter arm | `\r`/`\n` → PredictEnter variant | ✓ WIRED | Classifier maps to variant; on_input arm executes line-advance |
| on_input | tracing subscriber | `trace_exit!` at all exits | ✓ WIRED | macro at 5 sites, single event id |
| BulkSuppressed (pending>0) | epoch_start_col preservation | omit needs_epoch_start_sync | ✓ WIRED | flag not set → sync_cursor_from_confirmed does not overwrite floor |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Auth tests do not hang (no spin) | `cargo test -p nosh-client --test auth` | 6 pass, 0.27s | ✓ PASS |
| read -s noecho suppression intact | `cargo test -p nosh-client --test predict noecho_read_dash_s_zero_predicted_chars` | 1 pass, 11.24s | ✓ PASS |
| Full client suite | `cargo test -p nosh-client` | 96 lib + all integration green | ✓ PASS |
| Full server suite | `cargo test -p nosh-server --lib` | 86/86 | ✓ PASS |
| Workspace build | `cargo build --workspace` | clean | ✓ PASS |

### Mutation / Adversarial Checks (verifier-run, throwaway — not committed)

| Mutation | Expected | Observed | Verdict |
|----------|----------|----------|---------|
| Enter `\r`/`\n` → EpochReset (revert D-02) | D-02 tests fail | classify_enter_predict_enter, enter_predicts_line_advance_cursor, enter_at_last_row_stays_on_row all FAILED | ✓ D-02 tests are genuine |
| Re-add `needs_epoch_start_sync=true` to BulkSuppressed else (reintroduce WR-01/BUG-E) | wr01 test should fail | wr01 test PASSED (96/96) | ✗ committed wr01 test is non-adversarial |
| Faithful WR-01 repro (confirm cursor at col 5) with bug | epoch_start_col → 5 | epoch_start_col = 5 | bug is real & detectable with a correct test |
| Faithful WR-01 repro without bug (production code) | epoch_start_col stays 2 | epoch_start_col = 2 | ✓ production WR-01 fix is correct |

All probe tests were removed; `git status crates/` is clean.

### Anti-Patterns Found

None blocking. No TBD/FIXME/XXX in the modified files. IN-01 dead write was removed (REVIEW-FIX confirmed and code at :464 documents the removal).

### Gaps Summary

The phase goal **as shipped is achieved**: D-01 is cleanly and completely reverted (server.rs byte-identical to pre-burst, regressions gone, fast tests), and D-02 + D-03 are genuinely delivered and proven by adversarial mutation at the production-code level. The live `read -s` test confirms the D-02 Enter prediction coexists with the noecho security invariant — the exact interaction the deferred D-01 broke.

The single gap is **test quality, not behaviour**: the committed regression test `wr01_mid_epoch_bulk_suppressed_does_not_tighten_epoch_start_col` does not fail when the WR-01 bug is reintroduced, because its confirm step places the confirmed cursor at col 2 rather than the mid-line col 5 the scenario requires. The underlying production fix is correct (independently proven by a faithful repro that does fail-before/pass-after), so this is a WARNING — there is no behavioural regression, but the protection against future reintroduction of BUG-E on the BulkSuppressed path is weaker than the test's name and comments claim. Recommend strengthening the test per the `missing` note before relying on it as a permanent BUG-E guard.

---

_Verified: 2026-06-05_
_Verifier: Claude (gsd-verifier)_
