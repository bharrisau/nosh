---
phase: 999.1-server-attack-surface-hardening-expose-to-internet-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - fuzz/Cargo.toml
  - fuzz/Cargo.lock
  - fuzz/.gitignore
  - fuzz/fuzz_targets/codec_decode.rs
  - fuzz/fuzz_targets/read_message.rs
  - fuzz/fuzz_targets/decode_datagram.rs
  - fuzz/fuzz_targets/decode_epoch_ack.rs
  - fuzz/fuzz_targets/osc_accumulation.rs
  - fuzz/fuzz_targets/quic_packet.rs
  - fuzz/corpus/codec_decode/.gitkeep
autonomous: true
requirements: []
must_haves:
  truths:
    - "The fuzz/ crate is excluded from the cargo workspace — cargo test --workspace and cargo build --workspace never touch the #[no_main] fuzz binaries"
    - "fuzz/Cargo.toml declares all six [[bin]] target entries up front (codec_decode, read_message, decode_datagram, decode_epoch_ack, osc_accumulation, quic_packet) AND all six source files exist (codec_decode real; the other five as minimal stubs) so Cargo can resolve every [[bin]] path at manifest-parse time; wave-2 plans OVERWRITE the stub source files without re-touching the manifest (D-01/D-02 scaffold)"
    - "A single decoder fuzz target (codec_decode, D-01) builds under cargo +nightly fuzz build codec_decode and runs without a crash"
    - "fuzz/ depends on nosh-proto/nosh-server only via path; no new runtime dependency is added to nosh-proto or nosh-server"
    - "corpus/ is committed (un-ignored) while artifacts/ stays gitignored"
  artifacts:
    - path: "Cargo.toml"
      provides: "Workspace exclusion of fuzz/ (D-01 scaffold)"
      contains: 'exclude = ["fuzz"]'
    - path: "fuzz/Cargo.toml"
      provides: "cargo-fuzz crate manifest (name = nosh-fuzz, self-contained workspace, dev-only fuzz deps, all 6 [[bin]] entries)"
      contains: "cargo-fuzz = true"
    - path: "fuzz/fuzz_targets/codec_decode.rs"
      provides: "Trivial harness-proving fuzz target over nosh_proto::codec::decode (D-01)"
      min_lines: 5
    - path: "fuzz/.gitignore"
      provides: "Un-ignore corpus/, keep artifacts/ ignored"
  key_links:
    - from: "fuzz/fuzz_targets/codec_decode.rs"
      to: "nosh_proto::codec::decode"
      via: "direct function call inside fuzz_target!"
      pattern: "codec::decode"
    - from: "Cargo.toml"
      to: "fuzz/"
      via: "workspace exclude"
      pattern: "exclude = \\[\"fuzz\"\\]"
---

<objective>
Scaffold the cargo-fuzz harness (D-01 foundation): create the `fuzz/` sub-crate, exclude it from the cargo workspace, declare all six fuzz-target `[[bin]]` entries in `fuzz/Cargo.toml`, and prove the harness builds and runs with ONE trivial decoder target (`codec_decode`). Declaring all six bins here means the wave-2 plans (03 decoder targets, 04 QUIC fuzzer) add only their target SOURCE files — they never touch `fuzz/Cargo.toml`, so they stay file-disjoint and parallel.

Purpose: Without workspace exclusion, `cargo test --workspace` (CI's `linux` job) breaks on the `#[no_main]` fuzz binaries. Proving one target builds confirms nightly + libFuzzer + path-dependency wiring before five more targets depend on it.
Output: `fuzz/` crate, `exclude = ["fuzz"]` in root `Cargo.toml`, six `[[bin]]` entries, one working `codec_decode` target with a seed corpus.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- The trivial target calls one decoder. Exact signature confirmed in PATTERNS.md lines 111-118. -->
From crates/nosh-proto/src/codec.rs:51 :
  pub fn decode(body: &[u8]) -> Result<Message, ProtoError>
  // Takes the postcard body WITHOUT the 4-byte length prefix.
  // MAX_FRAME_LEN = 16 * 1024 * 1024 (codec.rs:14)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the fuzz/ crate, exclude it from the workspace, declare all six [[bin]] entries + stub the five wave-2 target sources</name>
  <files>Cargo.toml, fuzz/Cargo.toml, fuzz/Cargo.lock, fuzz/.gitignore, fuzz/fuzz_targets/read_message.rs, fuzz/fuzz_targets/decode_datagram.rs, fuzz/fuzz_targets/decode_epoch_ack.rs, fuzz/fuzz_targets/osc_accumulation.rs, fuzz/fuzz_targets/quic_packet.rs</files>
  <read_first>
    - .planning/phases/999.1-.../999.1-RESEARCH.md — "Recommended fuzz/ Layout" (lines 98-165), Pitfall 1 (workspace breakage, lines 408-413), Pitfall 3 (corpus gitignore, lines 420-423)
    - .planning/phases/999.1-.../999.1-PATTERNS.md — "fuzz/Cargo.toml (config)" section (lines 28-103): exact workspace-exclude edit and the generated manifest shape with all six [[bin]] blocks (lines 66-101)
    - /home/bharris/github.com/bharrisau/nosh/Cargo.toml — root workspace manifest (members + workspace.dependencies already in context above)
  </read_first>
  <action>
    Run `cargo install cargo-fuzz` if not present, then `cargo fuzz init` at the repo root (creates fuzz/ skeleton). Immediately reconcile the manifests to the project's locked constraints:
    (1) Edit root `Cargo.toml`: add `exclude = ["fuzz"]` under `[workspace]` (keep the existing `members` list unchanged). If `cargo fuzz init` auto-added `fuzz` to `members`, remove it. This is the load-bearing edit — it keeps `cargo test --workspace` / CI from compiling the `#[no_main]` binaries (RESEARCH Pitfall 1).
    (2) Write `fuzz/Cargo.toml` to match PATTERNS lines 46-101: `name = "nosh-fuzz"`, `version = "0.0.0"`, `publish = false`, `edition = "2021"`, `[package.metadata] cargo-fuzz = true`, and its own self-contained `[workspace]` block. Dependencies: `libfuzzer-sys = "0.4"`, `nosh-proto = { path = "../crates/nosh-proto" }`, `nosh-server = { path = "../crates/nosh-server" }`, `arbitrary = { version = "1", features = ["derive"] }`, `quinn-proto = "0.11"`, `quinn = "0.11"`, `rustls = "0.23"`, `bytes = "1"`, `tokio = { version = "1", features = ["rt"] }`, `rcgen = "0.14"`. Declare ALL SIX `[[bin]]` entries now (`codec_decode`, `read_message`, `decode_datagram`, `decode_epoch_ack`, `osc_accumulation`, `quic_packet`; each `test = false`, `doc = false`).
    (3) **Create minimal STUB source files for the five wave-2 targets** so every declared `[[bin]]` path resolves at manifest-parse time (Cargo errors on a `[[bin]]` whose `path` file is absent — a per-target `cargo fuzz build codec_decode` still parses the whole manifest). Write each of `fuzz/fuzz_targets/read_message.rs`, `decode_datagram.rs`, `decode_epoch_ack.rs`, `osc_accumulation.rs`, `quic_packet.rs` as a no-op stub: `#![no_main]` + `use libfuzzer_sys::fuzz_target;` + `fuzz_target!(|_data: &[u8]| {});` with a `// STUB — overwritten by plan 999.1-0X` comment. Wave-2 plans 03/04 OVERWRITE these stubs with the real targets (they depend on this plan, so the overwrite is ordered; the manifest is never re-touched). NO new dependency is added to nosh-proto or nosh-server — fuzz deps live only here.
    (4) After writing `fuzz/Cargo.toml` by hand with the deps above, run `cargo generate-lockfile` inside `fuzz/` (or `cargo +nightly fuzz build codec_decode` once, which regenerates it) and COMMIT `fuzz/Cargo.lock` so the locked fuzz deps are reproducible — the hand-written manifest's deps differ from what `cargo fuzz init` generated.
    (5) Edit `fuzz/.gitignore` (cargo-fuzz generates one that ignores `corpus/` and `artifacts/`): un-ignore `corpus/` (so seeds are committed) but keep `artifacts/` ignored (crash artifacts are large/machine-specific) — RESEARCH Pitfall 3.
    Do NOT modify any decoder source code in this plan.
  </action>
  <verify>
    <automated>grep -q 'exclude = \["fuzz"\]' Cargo.toml && grep -q 'cargo-fuzz = true' fuzz/Cargo.toml && [ "$(grep -c 'fuzz_targets/' fuzz/Cargo.toml)" -eq 6 ] && grep -q '^quinn ' fuzz/Cargo.toml && grep -q '^rustls ' fuzz/Cargo.toml && grep -q '^rcgen ' fuzz/Cargo.toml && [ "$(ls fuzz/fuzz_targets/*.rs | wc -l)" -eq 6 ] && cargo build --workspace --locked 2>&1 | grep -vq 'no main function' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'exclude = ["fuzz"]' Cargo.toml` matches (workspace exclusion present).
    - `cargo build --workspace --locked` succeeds and does NOT attempt to build anything under `fuzz/fuzz_targets/` (no "no main function found" error).
    - `fuzz/Cargo.toml` contains `cargo-fuzz = true`, `publish = false`, path deps on `../crates/nosh-proto` and `../crates/nosh-server`, the `quinn`, `rustls`, and `rcgen` deps (needed by plan 04's quic_packet target), and exactly six `path = "fuzz_targets/<name>.rs"` lines (the six bins).
    - All six source files exist under `fuzz/fuzz_targets/` (`ls fuzz/fuzz_targets/*.rs | wc -l` == 6): `codec_decode.rs` real, the other five minimal stubs — so `cargo +nightly fuzz build codec_decode` can parse the manifest (every `[[bin]]` path resolves).
    - `fuzz/Cargo.lock` exists and is committed (reproducible fuzz deps).
    - `git check-ignore fuzz/corpus/codec_decode/.gitkeep` returns non-zero (corpus NOT ignored); `git check-ignore fuzz/artifacts` returns the artifacts path (artifacts ignored).
    - `git diff crates/nosh-proto/Cargo.toml crates/nosh-server/Cargo.toml` is empty (no new runtime deps added to those crates).
  </acceptance_criteria>
  <done>fuzz/ crate exists, workspace-excluded, all six [[bin]] declared; nosh-proto/nosh-server Cargo.toml unchanged; corpus committed, artifacts ignored.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Trivial codec_decode fuzz target + seed corpus (harness proof)</name>
  <files>fuzz/fuzz_targets/codec_decode.rs, fuzz/corpus/codec_decode/.gitkeep</files>
  <read_first>
    - .planning/phases/999.1-.../999.1-PATTERNS.md — "fuzz/fuzz_targets/codec_decode.rs" section (lines 107-141): exact target shape and seed-corpus construction
    - .planning/phases/999.1-.../999.1-RESEARCH.md — Pattern 1 (pure-function fuzz target, lines 167-182)
    - crates/nosh-proto/src/codec.rs — `decode` (line 51), `encode`, and the unit test at lines 84-94 (shows `let body = &frame[4..]`) for seed construction
  </read_first>
  <action>
    Write `fuzz/fuzz_targets/codec_decode.rs` per PATTERNS lines 123-132: `#![no_main]`, `use libfuzzer_sys::fuzz_target;`, body `fuzz_target!(|data: &[u8]| { let _ = nosh_proto::codec::decode(data); });`. Never `.unwrap()` in the body — only a panic fails the fuzzer (ProtoError return is fine). The `[[bin]]` entry already exists from Task 1.
    Seed the corpus `fuzz/corpus/codec_decode/`: generate at least the valid postcard BODIES (strip the first 4 bytes of `codec::encode()` output, per the unit test at codec.rs:91) for `SessionClose { exit_code: 0, reason: "ok" }`, `Resize { cols: 80, rows: 24 }`, `PtyData { data: vec![0x03] }`, plus an empty (0-byte) file to exercise the `DeserializeUnexpectedEnd` path. Generate seeds via a throwaway `cargo run` snippet or a one-shot test, then write the raw bytes into the corpus dir. Keep a `.gitkeep` so the dir is tracked even if a seed is later minimised away.
  </action>
  <verify>
    <automated>cargo +nightly fuzz build codec_decode && cargo +nightly fuzz run codec_decode -- -runs=100000 -max_total_time=60 && echo NOCRASH</automated>
  </verify>
  <acceptance_criteria>
    - `cargo +nightly fuzz build codec_decode` builds the target successfully (proves nightly + libFuzzer + path-dep wiring).
    - `cargo +nightly fuzz run codec_decode -- -runs=100000 -max_total_time=60` exits 0 with NO crash artifact written to `fuzz/artifacts/codec_decode/`.
    - `ls fuzz/corpus/codec_decode/` lists at least 4 seed files (3 valid bodies + 1 empty).
    - The target file contains `nosh_proto::codec::decode` and no `.unwrap()` on the decode result.
  </acceptance_criteria>
  <done>codec_decode builds under nightly, runs 100k iterations / 60s with no crash, seed corpus committed. Harness proven for Plans 03/04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted bytes → nosh-proto decoders | Arbitrary input from the public internet reaches `codec::decode`; the fuzz harness exercises this boundary |
| package install (cargo-fuzz, fuzz deps) | dev-tooling supply chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.1-01 | Denial of Service | `codec::decode` on hostile bytes | mitigate | codec_decode fuzz target proves Ok/Err-only, no panic/OOM; MAX_FRAME_LEN bound exercised by Plan 03 read_message |
| T-999.1-SC | Tampering | cargo-fuzz + fuzz crate installs (libfuzzer-sys, arbitrary, quinn-proto, quinn, rustls, rcgen) | mitigate | All packages [OK] in RESEARCH Package Legitimacy Audit (lines 81-92); quinn/rustls/rcgen already in workspace lockfile; no [ASSUMED]/[SUS]/[SLOP] — no blocking checkpoint required |
</threat_model>

<verification>
- `cargo build --workspace --locked` and `cargo test --workspace --locked` ignore fuzz/ (workspace exclusion holds).
- `fuzz/Cargo.toml` declares six `[[bin]]` targets.
- `cargo +nightly fuzz build codec_decode` succeeds.
- `cargo +nightly fuzz run codec_decode -- -runs=100000 -max_total_time=60` produces no crash.
</verification>

<success_criteria>
fuzz/ crate scaffolded and workspace-excluded; all six bins declared; codec_decode target builds and runs clean under nightly; seed corpus committed; nosh-proto/nosh-server runtime deps untouched. Wave-2 plans can add target source files without touching the manifest.
</success_criteria>

<output>
Create `.planning/phases/999.1-server-attack-surface-hardening-expose-to-internet-readiness/999.1-01-SUMMARY.md` when done.
</output>
