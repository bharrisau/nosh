---
phase: 999.1-server-attack-surface-hardening-expose-to-internet-readiness
plan: 05
status: complete
completed: 2026-06-07
tasks: 3/3
key_files:
  created:
    - deny.toml
    - docs/999.1-SECURITY.md
  modified: []
---

# Plan 999.1-05 Summary — security audit, deep-fuzz checkpoint, residual-risk doc

**D-03/D-04. All three tasks complete.**

## Task 1 — deny.toml + cargo-deny + A1/A2 (commit 085593f)
- Added `deny.toml` (advisories/bans/licenses). `cargo deny check`: advisories FAIL on **RUSTSEC-2023-0071** only (`rsa 0.9.10` via `ssh-key`, no upstream fix); bans OK (18 duplicate-version warnings); licenses OK after adding `0BSD` + `MPL-2.0` (permissive, with rationale); sources OK.
- **A1 resolved:** `migration(true)` does NOT affect pre-handshake anti-amplification — cited `quinn-proto-0.11.14/src/connection/paths.rs:153-155`, `connection/mod.rs:3782-3787`, `config/mod.rs:244`.
- **A2 resolved:** quinn issues no Retry by default; the RFC 9000 §8.1 3× limit is the sole pre-handshake mechanism — cited `endpoint.rs:500-514`, `endpoint.rs:721-768`, `token.rs:116-129`. Retry noted as optional hardening.

## Task 2 — operator deep-fuzz checkpoint (overnight campaign)
Ran the deep/long fuzz as an autonomous overnight campaign instead of a manual operator session. Driver evolved across the night: 18 → 9 workers (load management) → adaptive core-rebalancing (`fuzz/overnight/run-adaptive.sh`) that auto-retires saturated targets and shifts their cores to the slowest (`quic_packet`). A stale-log bug in the first adaptive version (read old run logs → would have mis-rebalanced) was caught and fixed mid-run; the fix was validated over multiple hourly cycles (it correctly auto-retired `osc_accumulation` at saturation and moved its workers to `quic_packet`).

**Result: ZERO crash artifacts** across all six targets. Peak executions: quic_packet ~875M, decode_epoch_ack ~2.1B, codec_decode ~838M, decode_datagram ~643M, read_message ~94M, osc_accumulation ~15M (slow per-exec; 143k-entry corpus). All reached coverage saturation. Stopped at convergence at operator request. Corpora committed under `fuzz/corpus/`.

## Task 3 — residual-risk security doc (this commit)
`docs/999.1-SECURITY.md` — 7 sections: pre-auth memory/connection caps, amplification posture (A1/A2 with source citations), pre-auth resource exhaustion (preauth_flood_bounded baseline), fuzz results table, cargo-audit summary, cargo-deny output, residual risks (RUSTSEC-2023-0071 accepted; quinn per-conn alloc unknown; optional Retry hardening; reverse-proxy/M7 out of scope). Verdict: pre-auth surface safe to expose raw on UDP/443, subject to documented residuals + verification steps.

## Outstanding (operator)
- Push to GitHub and confirm the `audit` CI job is green (not verifiable from the sandbox).
- Periodic re-run of `cargo audit` / `cargo deny` / the fuzz corpora on dependency or decoder changes.
