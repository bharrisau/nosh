---
phase: 999.1-server-attack-surface-hardening-expose-to-internet-readiness
plan: 05
type: execute
wave: 3
depends_on:
  - 999.1-02
  - 999.1-03
  - 999.1-04
files_modified:
  - docs/999.1-SECURITY.md
  - deny.toml
autonomous: false
requirements: []
must_haves:
  truths:
    - "A residual-risk security doc (docs/999.1-SECURITY.md, D-04) records the pre-auth audit: half-open memory caps, amplification posture, pre-auth resource exhaustion, fuzz results, cargo-audit + cargo-deny output, and documented residual risk"
    - "The doc resolves RESEARCH open questions A1 (does migration(true) weaken pre-handshake anti-amplification) and A2 (does EndpointConfig::default issue Retry) with a SOURCE citation from quinn-proto/src/endpoint.rs (D-04)"
    - "The doc states the worst-case half-open memory bound (AuthLimits semaphore=64, 5s timeout, ~128 MiB at 64x2 MiB datagram buffers) and cites server.rs / transport.rs (D-04)"
    - "The doc summarises each fuzz target's run (duration, corpus size, crash count) with ours-vs-quinn-proto attribution (D-04)"
    - "A minimal deny.toml exists (advisories/bans/licenses) and cargo deny check output is recorded in the doc (D-04)"
  artifacts:
    - path: "docs/999.1-SECURITY.md"
      provides: "Residual-risk pre-auth security audit (D-04)"
      contains: "amplification"
      min_lines: 60
    - path: "deny.toml"
      provides: "Minimal cargo-deny config (advisories/bans/licenses) (D-04)"
      contains: "[advisories]"
  key_links:
    - from: "docs/999.1-SECURITY.md"
      to: "quinn-proto/src/endpoint.rs"
      via: "A1/A2 anti-amplification source citation"
      pattern: "endpoint.rs"
    - from: "docs/999.1-SECURITY.md"
      to: "crates/nosh-server/src/server.rs"
      via: "AuthLimits semaphore (64, 5s) citation"
      pattern: "AuthLimits"
---

<objective>
Produce the residual-risk security document (D-04) that closes the phase: it reports what was tested, what holds, and the documented residual risk for exposing the server's UDP/443 QUIC ingress to the public internet. This plan runs LAST (wave 3) because it reports the fuzz-run results (Plans 03/04), the cargo-audit CI outcome (Plan 02), the cargo-deny scan, and the source-verified amplification posture. It also adds the minimal `deny.toml` and runs `cargo deny check` (cargo-deny stays a manual/documented step per D-03/Claude's Discretion — NOT in CI).

This plan has a checkpoint: the operator runs the deep/long local fuzz sessions (D-03: fuzzing is a manual activity) and confirms the run durations / crash counts the doc records. The wave-2 short time-boxed runs prove the harness; the doc records the operator's longer local sessions.

Purpose: This is the phase's deliverable artifact — the evidence base for the "safe to expose raw" decision.
Output: `docs/999.1-SECURITY.md` + `deny.toml`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md
@.planning/phases/999.1-server-attack-surface-hardening-expose-to-internet-readiness/999.1-02-SUMMARY.md
@.planning/phases/999.1-server-attack-surface-hardening-expose-to-internet-readiness/999.1-03-SUMMARY.md
@.planning/phases/999.1-server-attack-surface-hardening-expose-to-internet-readiness/999.1-04-SUMMARY.md

<interfaces>
<!-- Exact values + citations the doc must reason about. From RESEARCH Pre-Auth Resource Audit + PATTERNS. -->
crates/nosh-server/src/server.rs:35-51, 133-147 :
  AuthLimits { max_concurrent: 64, auth_timeout: Duration::from_secs(5) }
  // try_acquire_owned() -> incoming.refuse() at the QUIC layer before TLS state allocated.

crates/nosh-proto/src/transport.rs :
  datagram_receive_buffer_size(Some(1 << 20))  // 1 MiB
  datagram_send_buffer_size(1 << 20)           // 1 MiB
  max_idle_timeout(300s)                        // finite, never None
  // Worst case at 64 half-open: 64 x 2 MiB = 128 MiB datagram buffers + quinn per-conn structs.

crates/nosh-client/tests/auth.rs :
  preauth_flood_bounded            // empirical flood baseline
  mutual_auth_inprocess_happy_path

RESEARCH open questions to RESOLVE with a source citation (lines 534-549):
  A1: does migration(true) weaken the pre-handshake 3x anti-amplification limit?
  A2: does EndpointConfig::default() issue QUIC Retry packets, or rely on the 3x limit only?
  -> resolve by source-reading quinn-proto/src/endpoint.rs handle() (look for self.retry()/refuse()
     in the NewConnection arm) and ServerConfig::migration docs.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add deny.toml, run cargo deny, resolve A1/A2 from quinn-proto source</name>
  <files>deny.toml</files>
  <read_first>
    - .planning/phases/999.1-.../999.1-RESEARCH.md — "Minimal deny.toml" (lines 483-505), Anti-amplification posture (lines 396-402), Open Question 1 (A2 source-check recipe, lines 536-539), Assumptions A1/A2/A7 (lines 523-529)
    - .planning/phases/999.1-.../999.1-PATTERNS.md — "docs/999.1-SECURITY.md" content structure (lines 504-518)
    - crates/nosh-server/src/server.rs (AuthLimits, migration(true)) and crates/nosh-proto/src/transport.rs (buffer/idle values)
  </read_first>
  <action>
    Write `deny.toml` at the repo root per RESEARCH lines 485-503: `[advisories]` (`unmaintained = "workspace"`, `unsound = "all"`), `[bans]` (`multiple-versions = "warn"`), `[licenses]` with `allow = ["MIT", "Apache-2.0", "ISC", "BSD-2-Clause", "BSD-3-Clause", "Unicode-3.0", "OpenSSL"]`. Extend the allow-list ONLY if `cargo deny check` reports a license actually present in the tree that is acceptable — record any addition with the crate that required it.
    Install cargo-deny if absent (`cargo install cargo-deny`) and run `cargo deny check`; capture the advisories/bans/licenses output to `/tmp/999.1-deny.txt` for the doc (Task 2). Do NOT wire cargo-deny into CI (D-03 boundary: only cargo-audit is in CI).
    Resolve A1/A2 by SOURCE-reading quinn-proto 0.11.14: locate `endpoint.rs` in the cargo registry (`find ~/.cargo/registry/src -path '*quinn-proto-0.11.14/src/endpoint.rs'`), read the `handle()` method's `NewConnection` arm for `self.retry()` / `self.refuse()` / the 3x anti-amplification accounting, and read `ServerConfig::migration` docs/impl. Record the exact file + line/function citation and the resolved answers (A1: whether migration(true) touches the pre-handshake limit; A2: whether default issues Retry or relies on the 3x limit only) — these feed Task 2's doc.
  </action>
  <verify>
    <automated>test -f deny.toml && grep -q '\[advisories\]' deny.toml && grep -q '\[licenses\]' deny.toml && cargo deny check 2>&1 | tee /tmp/999.1-deny.txt | grep -Eiq 'advisories|licenses|error|ok' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `deny.toml` exists at repo root and contains `[advisories]`, `[bans]`, and `[licenses]` sections with the allow-list above.
    - `cargo deny check` runs to completion and its output is captured to `/tmp/999.1-deny.txt` for the doc.
    - The A1/A2 resolution names a concrete `quinn-proto-0.11.14/src/endpoint.rs` location (file + line/function) — recorded for Task 2.
    - `grep -c 'cargo deny\|cargo-deny' .github/workflows/ci.yml` returns 0 (D-03 boundary preserved).
  </acceptance_criteria>
  <done>deny.toml committed; cargo deny check output captured; A1/A2 resolved against quinn-proto source with a citation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator deep fuzz sessions + green-CI confirmation (D-03 manual run)</name>
  <action>Operator-driven: run extended local fuzz sessions for all six targets and confirm the green CI audit job, then hand the per-target durations/crash counts back so the NEXT task (Task 3) can write the fuzz-results table with real figures. This runs BEFORE the doc is written so the doc records genuine deep-run results, not just the wave-2 smoke runs. This step cannot be automated — D-03 makes deep fuzzing a manual activity and the green-CI confirmation is not observable from the sandbox.</action>
  <what-built>The fuzz harness (six targets) is complete and Task 1 has settled deny.toml + the A1/A2 source-read. The wave-2 plans proved each target builds and survives a short time-boxed run. D-03 specifies deep fuzzing is a MANUAL activity — the doc (Task 3) must record real operator-run sessions, not just the 60-120s smoke runs.</what-built>
  <how-to-verify>
    1. From the repo root, run a longer session per target (suggested >= 10 min each, or overnight for the postcard/QUIC targets):
       `cargo +nightly fuzz run codec_decode -- -max_total_time=600`
       (repeat for read_message, decode_datagram, decode_epoch_ack, osc_accumulation, quic_packet)
    2. Confirm NO crash artifact appears under `fuzz/artifacts/<target>/` for any target.
    3. If any crash is found: stop and report it — the executor will classify it ours-vs-quinn-proto, fix ours (decoder/terminal fix + regression test + corpus entry), and file quinn's upstream.
    4. Provide the per-target run duration and crash count so the executor can fill the fuzz-results table in `docs/999.1-SECURITY.md` (section 4) in Task 3.
    5. Confirm the GitHub Actions `audit` job (Plan 02) is green in the Actions tab after pushing (this cannot be verified from the sandbox).
  </how-to-verify>
  <resume-signal>Provide per-target run durations + crash counts (and the green-CI confirmation), or report any crash to be triaged.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write the residual-risk security doc (D-04)</name>
  <files>docs/999.1-SECURITY.md</files>
  <read_first>
    - .planning/phases/999.1-.../999.1-PATTERNS.md — "docs/999.1-SECURITY.md" required content structure (lines 504-518: seven sections) and placement decision (docs/, not phase dir)
    - .planning/phases/999.1-.../999.1-RESEARCH.md — "Pre-Auth Resource Audit" (lines 371-402), "Known Threat Patterns" (lines 617-628), Assumptions log A1-A8 (lines 521-530)
    - .planning/phases/999.1-.../999.1-CONTEXT.md — D-04 (lines 60-68): authoritative content spec
    - The three wave-2 SUMMARYs (in context above) for per-target fuzz run results + ours-vs-quinn attribution
    - crates/nosh-client/tests/auth.rs — preauth_flood_bounded (empirical baseline to cite)
  </read_first>
  <action>
    Write `docs/999.1-SECURITY.md` (placement per PATTERNS line 508 — Claude's Discretion settled on docs/, not the planning dir). Cover the seven required sections (PATTERNS lines 510-517):
    (1) Pre-auth memory caps — `AuthLimits` semaphore (64 concurrent, 5s timeout), cite server.rs:35-51 and 133-147; datagram buffers 1 MiB recv + 1 MiB send, cite transport.rs; finite 300s idle timeout; worst-case ~128 MiB at 64x2 MiB plus the un-quantified quinn per-conn struct (note A7 as a residual unknown).
    (2) Amplification posture — RFC 9000 §8.1 3x limit; STATE the A1/A2 resolution from Task 1 WITH the quinn-proto/src/endpoint.rs citation (does migration(true) affect the pre-handshake limit; does EndpointConfig::default issue Retry or rely on the 3x limit only). Document migration(true) scope (post-handshake only if that is what the source shows). If the source shows Retry is NOT issued by default, note "the 3x limit is the sole pre-handshake anti-amplification mechanism" and whether enabling Retry is a recommended hardening.
    (3) Pre-auth resource exhaustion — reference preauth_flood_bounded (auth.rs) as the empirical baseline; summarise its result (a legitimate client connects under a junk-sender flood).
    (4) Fuzz results — a per-target table (codec_decode, read_message, decode_datagram, decode_epoch_ack, osc_accumulation, quic_packet): run duration, corpus size, crash count. Use the operator's deep-run figures from the Task 2 checkpoint (which ran BEFORE this task) as the headline results, and the wave-2 short-run figures from the SUMMARYs as corroboration. State "no crashes" or document each finding with ours-vs-quinn-proto attribution.
    (5) cargo-audit — summarise the CI advisory-scan result (Plan 02 / a local `cargo audit` run); list any accepted RUSTSEC ids and why.
    (6) cargo-deny — paste/summarise the Task-1 `cargo deny check` advisories/licenses/bans output.
    (7) Residual risks — known-unknowns (e.g. quinn per-conn allocation size not publicly documented (A7); deferred items; the WebTransport/reverse-proxy caveat that a QUIC-terminating proxy breaks e2e mutual auth — M7, out of scope here).
    Australian English, plain professional voice. End with a short "To verify before relying on this" note (operator-run long fuzz sessions, the green CI audit job, periodic re-audit) — this is a security artifact, so flag what the operator must confirm.
  </action>
  <verify>
    <automated>test -f docs/999.1-SECURITY.md && for s in "memory" "amplification" "AuthLimits" "endpoint.rs" "cargo deny" "cargo audit" "Residual" "preauth_flood_bounded"; do grep -qi "$s" docs/999.1-SECURITY.md || { echo "MISSING: $s"; exit 1; }; done && [ "$(grep -vc '^#' docs/999.1-SECURITY.md)" -ge 60 ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/999.1-SECURITY.md` exists with all seven sections present.
    - `grep -i` finds: `memory`, `amplification`, `AuthLimits`, `endpoint.rs` (the A1/A2 citation), `cargo deny`, `cargo audit`, `Residual`, `preauth_flood_bounded`.
    - The amplification section states the A1 and A2 answers and cites `quinn-proto-0.11.14/src/endpoint.rs` (file + line/function), not a generic claim.
    - The fuzz-results section has a per-target row for all six targets with duration / corpus size / crash count.
    - The doc body (excluding `#` heading lines) is >= 60 lines.
  </acceptance_criteria>
  <done>docs/999.1-SECURITY.md covers all seven D-04 sections; A1/A2 resolved with a quinn-proto source citation; six-target fuzz table present; cargo-audit + cargo-deny output recorded; residual risks documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated/half-open connection state | memory + CPU consumed before auth completes (AuthLimits-bounded) |
| spoofed-source-address packets | QUIC anti-amplification (RFC 9000 §8.1 3x limit) |
| transitive dependency tree | CVEs/licenses/bans surfaced by cargo-audit + cargo-deny |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.1-11 | Denial of Service | half-open connection flood (memory exhaustion) | mitigate | AuthLimits semaphore=64 + 5s timeout + bounded 1 MiB buffers; doc records worst-case ~128 MiB; preauth_flood_bounded empirical baseline |
| T-999.1-12 | Denial of Service (amplification) | spoofed-IP packet triggering >3x response | mitigate | 3x limit source-verified (A1/A2) against quinn-proto endpoint.rs; doc records posture + any Retry-hardening recommendation |
| T-999.1-13 | Tampering / Information Disclosure | dependency CVE / unacceptable license / banned crate | mitigate | cargo-audit (CI, Plan 02) + cargo-deny (manual, this plan); findings recorded in doc with disposition |
| T-999.1-14 | Denial of Service | undocumented quinn per-conn allocation (A7) | accept | per-conn struct size not publicly documented; recorded as residual unknown; empirical flood test bounds observed behaviour |
</threat_model>

<verification>
- `deny.toml` present with advisories/bans/licenses; `cargo deny check` output captured.
- `docs/999.1-SECURITY.md` present with all seven D-04 sections; A1/A2 resolved with a quinn-proto/src/endpoint.rs citation; six-target fuzz table; cargo-audit + cargo-deny recorded; residual risks documented.
- Checkpoint: operator-run long fuzz sessions report no crashes (or any crash triaged); green CI audit confirmed.
</verification>

<success_criteria>
The phase closes with a written, evidence-backed residual-risk doc: pre-auth memory caps quantified and cited, amplification posture source-verified (A1/A2 resolved), pre-auth exhaustion baselined, all six fuzz targets run clean (operator long sessions recorded), cargo-audit/deny clean-or-dispositioned, residual risks named.
</success_criteria>

<output>
Create `.planning/phases/999.1-server-attack-surface-hardening-expose-to-internet-readiness/999.1-05-SUMMARY.md` when done.
</output>
