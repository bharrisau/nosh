# Phase 999.1: Server attack-surface hardening - Discussion Log

> **Audit trail only.** Decisions captured in CONTEXT.md.

**Date:** 2026-06-06
**Phase:** 999.1-server-attack-surface-hardening-expose-to-internet-readiness
**Mode:** discuss (interactive, via /gsd:autonomous --interactive — backlog phase promoted)
**Areas discussed:** fuzz toolchain, QUIC packet-layer depth, CI integration, scan + output

---

## Context
Operator wants to run the hardening run before testing exposure of the service through an HTTP/3
reverse proxy. Topology note surfaced: a real QUIC-terminating HTTP/3 proxy breaks e2e auth +
migration (→ M7 WebTransport-inner-auth, unbuilt); this phase hardens the pre-auth surface, the
right prerequisite either way. Operator chose 999.1 only (not 999.2).

## Fuzz toolchain
| Option | Selected |
|--------|----------|
| cargo-fuzz / libFuzzer (nightly) | ✓ |
| Stable arbitrary + proptest | |
| Both | |
→ D-01. fuzz/ sub-crate; targets = all nosh-proto decoders + OSC accumulation.

## QUIC packet-layer depth
| Option | Selected |
|--------|----------|
| Focus on our decoders; trust quinn | |
| ALSO deep-fuzz the raw QUIC packet path | ✓ |
→ D-02. Malformed/oversized/truncated UDP at the endpoint; accept that much exercises quinn-proto;
document ours vs quinn's findings.

## CI integration
| Option | Selected |
|--------|----------|
| Short fuzz smoke in CI + cargo-audit | |
| Local-only fuzzing, no CI fuzz job | ✓ |
→ D-03. No CI fuzz job; commit corpus + results; add `cargo audit` to ci.yml.

## Scan + output
| Option | Selected |
|--------|----------|
| Threat-surface audit doc + cargo-audit/deny + fuzz results | ✓ |
| Harnesses + brief note only | |
→ D-04. Residual-risk SECURITY doc: half-open memory caps, amplification, pre-auth exhaustion +
cargo-audit + cargo-deny + fuzz results.

## Claude's Discretion
- fuzz target layout / corpus seeding; QUIC-fuzzer mechanism (raw UDP vs quinn-proto feed); doc
  location; cargo-deny CI-vs-manual placement.

## Deferred Ideas
- M7 WebTransport/reverse-proxy topology; 999.2 client hardening; a CI fuzz-smoke job.
