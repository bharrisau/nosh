---
phase: 999.1-server-attack-surface-hardening-expose-to-internet-readiness
verified: 2026-06-07T08:00:00Z
status: passed
score: 4/4 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: none
  previous_score: n/a
---

# Phase 999.1: Server attack-surface hardening (expose-to-internet readiness) Verification Report

**Phase Goal:** Be confident the server's UDP/443 QUIC ingress is safe to expose to the internet — fuzz the nosh-proto decoders + the raw QUIC packet path, audit the pre-auth surface, produce a documented residual-risk report. No panics/OOM/unbounded growth on hostile PRE-AUTH input.
**Verified:** 2026-06-07
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | ----- | ------ | -------- |
| 1 | D-01: every untrusted nosh-proto decoder has a real cargo-fuzz target that calls the decoder over raw bytes, fuzzed clean | ✓ VERIFIED | 5 decoder targets present; each calls its decoder over raw `data` with no sanitisation (see below) |
| 2 | D-02: a raw QUIC-packet fuzzer feeds arbitrary bytes into quinn-proto `Endpoint::handle` with production-mirroring config, fuzzed clean | ✓ VERIFIED | `quic_packet.rs:132-137` builds `BytesMut::from(data)` and calls `ep.handle(...)`; mirrors `migration(true)` + `transport_config`; builds clean |
| 3 | D-03: cargo-audit in CI, NO CI fuzz job; fuzz crate workspace-excluded | ✓ VERIFIED | `audit` job in ci.yml; no `cargo fuzz` in CI; `cargo metadata` shows nosh-fuzz is NOT a workspace member |
| 4 | D-04: residual-risk security doc (7 sections, A1/A2 citations) + cargo-deny config | ✓ VERIFIED | `docs/999.1-SECURITY.md` §1-7 present incl. A1/A2 quinn-proto source citations; `deny.toml` parses under cargo-deny 0.19.8 |

**Score:** 4/4 truths verified

### Pre-auth scope honesty (the key adversarial question)

The "safe to expose raw on UDP/443" verdict is honestly scoped to the PRE-AUTH surface and does not overclaim:

- The pre-auth decoders (`codec::decode`, `codec::read_message`, `decode_datagram`, `decode_epoch_ack`) and the QUIC packet path are genuinely the pre-auth ingress and were fuzzed clean (0 crash artifacts across all 6 target dirs in `fuzz/artifacts/`).
- The one real OOM (CR-01 / OSC-OOM) is correctly identified as POST-auth — reachable only via PTY output after a session opens, not pre-auth ingress — documented in `docs/999.1-SECURITY.md` §4 (false-confidence caveat) and §7 (OSC-OOM residual), and tracked to phase 999.7 (present in ROADMAP.md:307-311 with corrected reasoning). It is NOT silently dropped.

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `fuzz/fuzz_targets/codec_decode.rs` | calls `codec::decode(data)` | ✓ VERIFIED | raw `data`, discards result, no `.unwrap()` |
| `fuzz/fuzz_targets/read_message.rs` | length-prefix framing over raw bytes | ✓ VERIFIED | OnceLock runtime, `read_message` on Cursor(data); WR-02 comment corrected (16 MiB bounded, strict-greater) |
| `fuzz/fuzz_targets/decode_datagram.rs` | calls `decode_datagram(data)` | ✓ VERIFIED | raw `data`, tag + MAX_RUNS guard exercised |
| `fuzz/fuzz_targets/decode_epoch_ack.rs` | calls `decode_epoch_ack(data)` | ✓ VERIFIED | raw `data`, T-13-01 0x01-tag-reject exercised |
| `fuzz/fuzz_targets/osc_accumulation.rs` | OSC caps over raw VT bytes | ✓ VERIFIED | `TerminalState::advance(data)` + non-tautological cap asserts |
| `fuzz/fuzz_targets/quic_packet.rs` | arbitrary bytes → `ep.handle` | ✓ VERIFIED | WR-01 fix applied (`ep.ignore(incoming)` on NewConnection); builds clean |
| `fuzz/Cargo.toml` | 6 `[[bin]]`, own `[workspace]` | ✓ VERIFIED | all 6 declared; isolated workspace |
| `deny.toml` | advisories/bans/licenses | ✓ VERIFIED | parses under cargo-deny 0.19.8; licenses ok |
| `.github/workflows/ci.yml` | cargo-audit job | ✓ VERIFIED | `audit` job with `actions-rust-lang/audit@v1` |
| `docs/999.1-SECURITY.md` | 7-section residual-risk doc | ✓ VERIFIED | §1-7 incl. A1/A2 citations, OSC-OOM residual |
| `crates/nosh-server/Cargo.toml` | vte comment corrected | ✓ VERIFIED | WR-02: wrong Phase-16 "pipe-buffer" claim corrected, tracked to 999.7 |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Fuzz targets enumerate | `cargo +nightly fuzz list` | 6 targets listed | ✓ PASS |
| QUIC target builds (WR-01 fix) | `cargo +nightly fuzz build quic_packet` | Finished release | ✓ PASS |
| Decoder target builds | `cargo +nightly fuzz build read_message` | Finished release | ✓ PASS |
| Fuzz crate excluded from workspace | `cargo metadata --no-deps` | nosh-fuzz absent (only 4 crates) | ✓ PASS |
| Workspace build ignores fuzz | `cargo build --workspace --locked` | Finished, no fuzz targets | ✓ PASS |
| deny.toml parses | `cargo deny check licenses` | licenses ok | ✓ PASS |
| No crash artifacts | `find fuzz/artifacts -type f` | none (clean) | ✓ PASS |
| Baseline tests present | grep auth.rs | `preauth_flood_bounded` + happy path exist | ✓ PASS |

### Code-Review Dispositions

| Finding | Disposition claimed | Verified |
| ------- | ------------------- | -------- |
| CR-01 OSC-OOM | documented + tracked (999.7), post-auth scope | ✓ doc §4/§7 + ROADMAP 999.7 + Cargo.toml comment |
| WR-01 quic ep.ignore | fixed | ✓ `quic_packet.rs:136` `ep.ignore(incoming)` |
| WR-02 read_message comment | corrected | ✓ Cargo.toml + read_message.rs comments corrected |
| IN-01 deny.toml vulnerability key | N/A for cargo-deny 0.19 | ✓ confirmed: 0.19.8 schema has no key; config parses |
| IN-02 fuzz Cargo.lock audit | accepted (dev-only deps) | ✓ reasonable; fuzz deps not shipped on server |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| (none) | — | No unreferenced TBD/FIXME/XXX in phase files; OSC-OOM debt references phase 999.7 | — | None |

### Human Verification Required

(none required for goal achievement)

Note: SUMMARY/doc flag one operator follow-up — push to GitHub and confirm the `audit` CI job goes green in the Actions tab (cannot be verified from the sandbox). This is an operational confirmation of an already-wired job, not a gap in the phase deliverables; the job is present and correctly structured in ci.yml. It does not block goal achievement.

### Gaps Summary

No gaps. All four decisions (D-01..D-04) are genuinely delivered. The six fuzz targets exist, build, and call their real decoders / the quinn-proto endpoint over raw arbitrary input with no sanitisation; the fuzz crate is workspace-excluded; cargo-audit is in CI with no CI fuzz job; deny.toml parses; the 7-section security doc carries the A1/A2 source citations and honestly scopes the verdict to the pre-auth surface. The one real OOM (OSC-OOM / CR-01) is correctly identified as post-auth (out of this phase's pre-auth scope), documented, and tracked to phase 999.7 — not silently dropped. All code-review dispositions check out against the codebase. Zero crash artifacts. The pre-auth attack surface is genuinely hardened and evidenced.

---

_Verified: 2026-06-07_
_Verifier: Claude (gsd-verifier)_
