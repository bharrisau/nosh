# Phase 999.1 — code-review fix summary

Review (`999.1-REVIEW.md`): 1 critical, 2 warnings, 2 info.

| Finding | Disposition |
|---------|-------------|
| **CR-01** vte `std` unbounded OSC accumulation → post-auth OOM (Phase-16 mitigation reasoning was wrong) | **Documented + tracked**, not fixed here. It is reachable only post-auth (PTY output), so 999.1's pre-auth goal is unaffected. Corrected `docs/999.1-SECURITY.md` §4 (osc fuzz false-confidence caveat) + §7 (OSC-OOM residual), corrected the wrong claim in `crates/nosh-server/Cargo.toml`, and filed **phase 999.7** for the fix (bound OSC before vte + multi-chunk adversarial test). Operator decision: document + dedicated follow-up phase. |
| **WR-01** quic_packet harness dropped `Incoming` (leaks endpoint slab/CID state) | **Fixed** (commit on main) — now `ep.ignore(incoming)` on `NewConnection`. `cargo +nightly fuzz build quic_packet` passes. |
| **WR-02** read_message comment falsely claimed the guard fires before allocation | **Fixed** — comment corrected: allocation is bounded AT MAX_FRAME_LEN (16 MiB); the guard is strict-greater so exactly-16MiB is accepted (bounded, not unbounded). |
| **IN-01** deny.toml missing explicit `vulnerability = "deny"` | **N/A for cargo-deny 0.19** — that schema has no `vulnerability` key (vulnerabilities denied by default; my attempt to add it failed deserialization). Reverted to the valid original and documented the rationale in deny.toml. |
| **IN-02** fuzz/Cargo.lock packages not covered by CI cargo-audit | **Accepted** — fuzz deps (libfuzzer-sys, arbitrary, …) are dev-only, not shipped on the server; the root Cargo.lock (the server's actual deps) is what CI audits. Noted, not actioned. |

**Net code change:** quic_packet.rs (ep.ignore), read_message.rs (comment), deny.toml (revert). Docs/comment/roadmap corrected for CR-01. No production server/decoder behaviour changed in 999.1.
