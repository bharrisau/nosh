---
phase: 999.1-server-attack-surface-hardening-expose-to-internet-readiness
plan: 03
type: execute
wave: 2
depends_on:
  - 999.1-01
files_modified:
  - fuzz/fuzz_targets/read_message.rs
  - fuzz/fuzz_targets/decode_datagram.rs
  - fuzz/fuzz_targets/decode_epoch_ack.rs
  - fuzz/fuzz_targets/osc_accumulation.rs
  - fuzz/corpus/read_message/.gitkeep
  - fuzz/corpus/decode_datagram/.gitkeep
  - fuzz/corpus/decode_epoch_ack/.gitkeep
  - fuzz/corpus/osc_accumulation/.gitkeep
autonomous: true
requirements: []
must_haves:
  truths:
    - "read_message (4-byte BE u32 length-prefix framing) refuses len > MAX_FRAME_LEN (16 MiB) before allocating — fuzzed, never OOMs (D-01)"
    - "decode_datagram (tag 0x01 StateDiff, MAX_RUNS=4096) returns Ok/Err on any bytes, never panics (D-01)"
    - "decode_epoch_ack (tag 0x02 ClientEpoch) returns Ok/Err on any bytes, never panics; a 0x01 StateDiff payload is rejected, not parsed as an epoch (D-01)"
    - "OSC accumulation via TerminalState::advance holds OSC_52_MAX_BYTES (65536) and MAX_TITLE_BYTES (1024) caps on hostile VT streams (D-01)"
    - "All four targets build under cargo +nightly fuzz build and run without a crash; seed corpus committed per target"
  artifacts:
    - path: "fuzz/fuzz_targets/read_message.rs"
      provides: "Length-prefix framing fuzz target (D-01) via OnceLock<Runtime> + in-memory AsyncRead"
      contains: "read_message"
    - path: "fuzz/fuzz_targets/decode_datagram.rs"
      provides: "StateDiff decode fuzz target (D-01)"
      contains: "decode_datagram"
    - path: "fuzz/fuzz_targets/decode_epoch_ack.rs"
      provides: "ClientEpoch decode fuzz target (D-01)"
      contains: "decode_epoch_ack"
    - path: "fuzz/fuzz_targets/osc_accumulation.rs"
      provides: "OSC cap fuzz target via TerminalState::advance (D-01)"
      contains: "TerminalState"
  key_links:
    - from: "fuzz/fuzz_targets/read_message.rs"
      to: "nosh_proto::codec::read_message"
      via: "OnceLock<Runtime>::block_on over in-memory cursor"
      pattern: "read_message"
    - from: "fuzz/fuzz_targets/decode_datagram.rs"
      to: "nosh_proto::datagram::decode_datagram"
      via: "direct call in fuzz_target!"
      pattern: "datagram::decode_datagram"
    - from: "fuzz/fuzz_targets/decode_epoch_ack.rs"
      to: "nosh_proto::datagram::decode_epoch_ack"
      via: "direct call in fuzz_target!"
      pattern: "datagram::decode_epoch_ack"
    - from: "fuzz/fuzz_targets/osc_accumulation.rs"
      to: "nosh_server::terminal::TerminalState::advance"
      via: "advance + osc52_pending/title cap assertions"
      pattern: "TerminalState::new"
---

<objective>
Cover the remaining D-01 untrusted decoders with coverage-guided fuzz targets: `read_message` (length-prefix framing), `decode_datagram` (StateDiff), `decode_epoch_ack` (ClientEpoch), and OSC accumulation (`TerminalState::advance` OSC 52 / title caps). `codec_decode` was already proven in Plan 01; this plan completes the decoder surface. The four `[[bin]]` entries already exist in `fuzz/Cargo.toml` (declared in Plan 01) — this plan adds ONLY the target SOURCE files + corpora, so it is file-disjoint from Plan 04 (quic_packet) and runs in parallel in wave 2. Each target asserts the no-panic / no-OOM / cap-holds contract on arbitrary bytes.

Purpose: These four functions are the postcard/framing/VT attack surface reachable on the public UDP/443 ingress before authentication. Fuzzing PROVES the no-panic contract the decoders already claim by design.
Output: four fuzz target source files + seed corpora (NO edit to fuzz/Cargo.toml).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md
@.planning/phases/999.1-server-attack-surface-hardening-expose-to-internet-readiness/999.1-01-SUMMARY.md

<interfaces>
<!-- Exact decoder signatures confirmed in PATTERNS.md. Use these directly — no codebase exploration needed. -->
From crates/nosh-proto/src/codec.rs:67 :
  pub async fn read_message<R: AsyncRead + Unpin>(r: &mut R) -> Result<Message, ProtoError>
  // 4-byte BE u32 length prefix; guard `if len > MAX_FRAME_LEN { return Err(FrameTooLarge) }`
  // fires BEFORE `vec![0u8; len]`. MAX_FRAME_LEN = 16 MiB.

From crates/nosh-proto/src/datagram.rs:430 :
  pub fn decode_datagram(bytes: &[u8]) -> Result<StateDiff, ProtoError>
  // byte0 must be TAG_STATE_DIFF = 0x01; runs.len() > MAX_RUNS (4096) → Err.

From crates/nosh-proto/src/datagram.rs:183 :
  pub fn decode_epoch_ack(bytes: &[u8]) -> Result<u64, ProtoError>
  // byte0 must be TAG_CLIENT_EPOCH = 0x02; TAG_STATE_DIFF=0x01 explicitly rejected (T-13-01).

From crates/nosh-server/src/terminal.rs :
  pub fn new(cols: u16, rows: u16) -> Self            // :192
  pub fn advance(&mut self, bytes: &[u8])             // :222 (confirmed pub)
  pub fn osc52_pending(&self) -> Option<(&[u8], &[u8])> // :374
  pub fn title(&self) -> Option<&str>                 // :~390
  pub const OSC_52_MAX_BYTES: usize = 65_536;         // :56
  pub const MAX_TITLE_BYTES: usize = 1_024;           // :60
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: read_message + decode_datagram + decode_epoch_ack fuzz targets (D-01)</name>
  <files>fuzz/fuzz_targets/read_message.rs, fuzz/fuzz_targets/decode_datagram.rs, fuzz/fuzz_targets/decode_epoch_ack.rs, fuzz/corpus/read_message/.gitkeep, fuzz/corpus/decode_datagram/.gitkeep, fuzz/corpus/decode_epoch_ack/.gitkeep</files>
  <read_first>
    - .planning/phases/999.1-.../999.1-PATTERNS.md — "read_message.rs" (lines 143-186), "decode_datagram.rs" (lines 190-219), "decode_epoch_ack.rs" (lines 223-251): exact target shapes, tag protocol, and seed-corpus byte sequences
    - .planning/phases/999.1-.../999.1-RESEARCH.md — Pattern 2 (async read_message + OnceCell<Runtime>, lines 184-207), Pattern 3 (decode_datagram raw bytes, lines 209-243), Pitfall 4 (runtime fd exhaustion, lines 425-428)
    - crates/nosh-proto/src/codec.rs — `read_message` (lines 67-77), MAX_FRAME_LEN (line 14), async unit test (lines 107-133) for seed construction
    - crates/nosh-proto/src/datagram.rs — `decode_datagram` (lines 430-447), `decode_epoch_ack` (lines 183-192), MAX_RUNS, negative tests (lines 636-679, 957-994) for seed bytes
  </read_first>
  <action>
    The three `[[bin]]` entries (`read_message`, `decode_datagram`, `decode_epoch_ack`) already exist in `fuzz/Cargo.toml` from Plan 01 — do NOT edit that manifest. Create only the source files below.
    `read_message.rs` (PATTERNS lines 163-183): `#![no_main]`; use a module-level `static RT: OnceLock<tokio::runtime::Runtime>` built `new_current_thread()` (NOT per-iteration — RESEARCH Pitfall 4); in the body `rt.block_on(async { let mut cursor = tokio::io::BufReader::new(std::io::Cursor::new(data)); let _ = nosh_proto::codec::read_message(&mut cursor).await; })`. The invariant under test: the MAX_FRAME_LEN guard fires before allocation — a `0xFF_FF_FF_FF` prefix must yield `Err(FrameTooLarge)`, never OOM.
    `decode_datagram.rs` (PATTERNS lines 208-216): `fuzz_target!(|data: &[u8]| { let _ = nosh_proto::datagram::decode_datagram(data); });`.
    `decode_epoch_ack.rs` (PATTERNS lines 240-248): `fuzz_target!(|data: &[u8]| { let _ = nosh_proto::datagram::decode_epoch_ack(data); });`.
    Never `.unwrap()` the decoder result in any body — only a panic fails the fuzzer.
    Seed corpora (raw bytes written into each `corpus/<target>/` dir, per PATTERNS):
    - read_message: valid round-trip frame for SessionClose; `00 00 00 00` (zero len); `FF FF FF FF` + 0 body (FrameTooLarge without alloc); `00 00 00 05` + 3-byte truncated body (Io/UnexpectedEof).
    - decode_datagram: `[0x01]` + valid postcard StateDiff body (empty runs); `[0x02, ...]` wrong tag; `[]`; `[0x01, 0xFF]` corrupt body; a diff at exactly MAX_RUNS (4096) for the boundary.
    - decode_epoch_ack: `[0x02, 0x01]`; `[0x02, 0x00]`; the u64::MAX varint `[0x02, 0xFF×8, 0x01]`; `[0x01, ...]` wrong tag; `[]`.
    Generate the valid-encoding seeds with a throwaway `cargo run`/test snippet using `codec::encode` / the datagram `tag_encode` helpers, then write raw bytes. Keep a `.gitkeep` per dir.
  </action>
  <verify>
    <automated>cargo +nightly fuzz build read_message && cargo +nightly fuzz build decode_datagram && cargo +nightly fuzz build decode_epoch_ack && cargo +nightly fuzz run read_message -- -runs=200000 -max_total_time=90 && cargo +nightly fuzz run decode_datagram -- -runs=200000 -max_total_time=90 && cargo +nightly fuzz run decode_epoch_ack -- -runs=200000 -max_total_time=90 && echo NOCRASH</automated>
  </verify>
  <acceptance_criteria>
    - Each of the three targets builds under `cargo +nightly fuzz build <name>`.
    - `cargo +nightly fuzz run <name> -- -runs=200000 -max_total_time=90` exits 0 for each with no crash artifact in `fuzz/artifacts/<name>/`.
    - `read_message.rs` uses a `static OnceLock<Runtime>` (grep `OnceLock`) — NOT a per-iteration `Runtime::new`.
    - `ls fuzz/corpus/read_message`, `.../decode_datagram`, `.../decode_epoch_ack` each list the seed files described above (≥4, ≥5, ≥5 respectively incl. .gitkeep).
    - No `.unwrap()` on the decoder result in any of the three bodies.
    - `git diff fuzz/Cargo.toml` is empty (manifest untouched — bins pre-declared in Plan 01).
    - If a run crashes: the crashing input + the decoder fix + a regression unit test in the owning nosh-proto module land together, and the crash input is added to the corpus. Note any such fix in the SUMMARY.
  </acceptance_criteria>
  <done>read_message, decode_datagram, decode_epoch_ack build and fuzz clean; corpora committed; fuzz/Cargo.toml untouched. Any crash → fix + regression test + corpus entry.</done>
</task>

<task type="auto">
  <name>Task 2: osc_accumulation fuzz target — OSC 52 / title caps (D-01)</name>
  <files>fuzz/fuzz_targets/osc_accumulation.rs, fuzz/corpus/osc_accumulation/.gitkeep</files>
  <read_first>
    - .planning/phases/999.1-.../999.1-PATTERNS.md — "osc_accumulation.rs" (lines 255-311): confirmed `advance(&mut self, &[u8])` is pub, cap constants, read API, exact target shape, and seed VT byte sequences
    - .planning/phases/999.1-.../999.1-RESEARCH.md — Pattern 4 (OSC accumulation, lines 245-270, assumption A4)
    - crates/nosh-server/src/terminal.rs — `advance` (line 222), `osc_dispatch` cap logic (lines 682-729), `OSC_52_MAX_BYTES`/`MAX_TITLE_BYTES` (lines 56-63), `osc52_pending`/`title` (lines 374-401), `new` (lines 192-206)
  </read_first>
  <action>
    The `osc_accumulation` `[[bin]]` already exists in `fuzz/Cargo.toml` from Plan 01 — do NOT edit that manifest. Write `fuzz/fuzz_targets/osc_accumulation.rs` per PATTERNS lines 286-308: `#![no_main]`; `use nosh_server::terminal::{TerminalState, OSC_52_MAX_BYTES, MAX_TITLE_BYTES};`; body constructs `let mut state = TerminalState::new(80, 24); state.advance(data);` then ASSERTS the caps actually hold — `if let Some((_sel, payload)) = state.osc52_pending() { assert!(payload.len() <= OSC_52_MAX_BYTES) }` and `if let Some(title) = state.title() { assert!(title.len() <= MAX_TITLE_BYTES) }`. These asserts are intentional: a cap-violation panic IS the fuzzer signal (distinct from the other targets which only guard against incidental panics). Verify the exact `advance`/`osc52_pending`/`title` signatures in terminal.rs before writing (RESEARCH A4) and adjust the binding patterns to match.
    Seed corpus `fuzz/corpus/osc_accumulation/` (raw VT bytes, PATTERNS line 311): `\x1b]2;My Title\x07`; `\x1b]52;c;SGVsbG8=\x07`; `\x1b]2;` + ~2000-byte title + `\x07` (over MAX_TITLE_BYTES — must be discarded, not stored); `\x1b]52;c;` + ~70000-byte payload + `\x07` (over OSC_52_MAX_BYTES — must be truncated); `\x1b]0;` (incomplete OSC); a mixed stream of newlines + CSI + OSC. Keep a `.gitkeep`.
  </action>
  <verify>
    <automated>cargo +nightly fuzz build osc_accumulation && cargo +nightly fuzz run osc_accumulation -- -runs=200000 -max_total_time=90 && echo NOCRASH</automated>
  </verify>
  <acceptance_criteria>
    - `cargo +nightly fuzz build osc_accumulation` builds.
    - `cargo +nightly fuzz run osc_accumulation -- -runs=200000 -max_total_time=90` exits 0 with no crash artifact (the cap asserts hold on all explored VT input).
    - The target contains `TerminalState::new` and BOTH cap assertions referencing `OSC_52_MAX_BYTES` and `MAX_TITLE_BYTES`.
    - `ls fuzz/corpus/osc_accumulation` lists the seeds above incl. the two over-cap seeds.
    - `git diff fuzz/Cargo.toml` is empty (manifest untouched).
    - If a cap-assert fires: the terminal.rs cap fix + a regression test land with the corpus entry; noted in SUMMARY.
  </acceptance_criteria>
  <done>osc_accumulation builds and fuzzes clean; OSC 52 + title caps proven on hostile VT bytes; corpus committed; fuzz/Cargo.toml untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted UDP/443 bytes → nosh-proto decoders | reliable-stream framing + datagram tag/body reach `read_message`/`decode_datagram`/`decode_epoch_ack` pre/at auth |
| server-rendered terminal stream → TerminalState | VT byte stream (incl. OSC 52 / title) accumulated server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.1-04 | Denial of Service | `read_message` length-prefix abuse (huge/zero/truncated len) | mitigate | fuzz target proves MAX_FRAME_LEN (16 MiB) guard fires before `vec![0u8; len]` — no OOM |
| T-999.1-05 | Denial of Service | `decode_datagram` malformed postcard / runs flood | mitigate | fuzz proves MAX_RUNS (4096) guard + tag check; Ok/Err only |
| T-999.1-06 | Tampering | `decode_epoch_ack` tag confusion (0x01 read as epoch) | mitigate | fuzz exercises tag rejection (T-13-01 invariant: 0x01 StateDiff not parsed as epoch) |
| T-999.1-07 | Denial of Service | OSC 52 / title large-payload OOM | mitigate | osc_accumulation asserts OSC_52_MAX_BYTES + MAX_TITLE_BYTES caps hold on hostile VT input |
</threat_model>

<verification>
- All four targets build under `cargo +nightly fuzz build <name>`.
- Each runs ≥90s / ≥200k iterations with no crash.
- Seed corpora committed per target (`fuzz/corpus/<target>/`).
- `fuzz/Cargo.toml` untouched (bins pre-declared in Plan 01).
- Any crash found → decoder/terminal fix + regression test + corpus entry land together (in-scope per CLAUDE.md).
</verification>

<success_criteria>
The full D-01 decoder surface (codec_decode from Plan 01 + the four here) is fuzzed clean with committed corpora; the no-panic/no-OOM/cap-holds contract is empirically proven on arbitrary bytes.
</success_criteria>

<output>
Create `.planning/phases/999.1-server-attack-surface-hardening-expose-to-internet-readiness/999.1-03-SUMMARY.md` when done. Record per-target run duration, corpus size, and crash count (with ours-vs-upstream attribution if any crash).
</output>
