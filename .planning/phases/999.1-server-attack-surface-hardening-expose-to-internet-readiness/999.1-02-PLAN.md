---
phase: 999.1-server-attack-surface-hardening-expose-to-internet-readiness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements: []
must_haves:
  truths:
    - "CI runs cargo-audit on every push/PR as a peer job (D-03), gating against RustSec advisories in Cargo.lock"
    - "NO CI fuzz job is added (D-03 — fuzzing stays local-only)"
    - "The audit job uses the prebuilt actions-rust-lang/audit@v1 action (no slow cargo install)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "cargo-audit CI job (D-03)"
      contains: "actions-rust-lang/audit@v1"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "RustSec advisory DB"
      via: "actions-rust-lang/audit@v1 action"
      pattern: "actions-rust-lang/audit@v1"
---

<objective>
Add a `cargo audit` job to CI (D-03): a cheap dependency-CVE gate that scans `Cargo.lock` against the RustSec advisory DB on every push/PR. Deliberately NO CI fuzz job (D-03 — fuzzing is local-only). This plan touches only `ci.yml`, so it is disjoint from the fuzz crate and runs in wave 1 in parallel with Plan 01.

Purpose: Expose-to-internet readiness needs a continuous CVE tripwire on transitive deps (quinn, rustls, postcard, etc.) without the flake/maintenance cost of a CI fuzz smoke.
Output: a new `audit` peer job in `.github/workflows/ci.yml`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the cargo-audit peer job to ci.yml (D-03)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/999.1-.../999.1-PATTERNS.md — ".github/workflows/ci.yml — add audit job" section (lines 483-500) and "CI Job Structure Pattern" (lines 455-477): the audit job is a self-contained peer of `linux`/`build-windows` and needs neither rust-toolchain nor rust-cache
    - .planning/phases/999.1-.../999.1-RESEARCH.md — "CI step for cargo audit" (lines 467-481) and Pitfall 6 (cargo install flake → use the prebuilt action, lines 435-438)
    - .github/workflows/ci.yml — existing `linux` and `build-windows` jobs (already in context above)
  </read_first>
  <action>
    Append a new `audit` job to `.github/workflows/ci.yml` as a peer of the existing `linux` and `build-windows` jobs (add it after the `build-windows` block, at the same indentation as the other entries under `jobs:`). Job shape per PATTERNS lines 488-498:
    - `name: cargo audit`, `runs-on: ubuntu-latest`
    - steps: `uses: actions/checkout@v4`, then `uses: actions-rust-lang/audit@v1` (named "Audit Rust dependencies") with `ignore: ""` (a placeholder for accepting documented RUSTSEC-YYYY-NNNN advisories later).
    Do NOT add `dtolnay/rust-toolchain` or `Swatinem/rust-cache` to this job — `actions-rust-lang/audit@v1` ships a prebuilt binary and fetches the advisory DB itself (RESEARCH Pitfall 6). Do NOT add any `cargo fuzz run` step and do NOT add a `cargo deny` step to CI (D-03: fuzzing local-only; D-04: cargo-deny is a manual/documented step, placed in Plan 05).
    If `actions-rust-lang/audit@v1` surfaces an advisory in the current `Cargo.lock`, do not silently broaden `ignore` — record the RUSTSEC id and surface it; the security doc (Plan 05) decides disposition.
  </action>
  <verify>
    <automated>grep -q 'actions-rust-lang/audit@v1' .github/workflows/ci.yml && ! grep -q 'cargo fuzz' .github/workflows/ci.yml && ! grep -q 'cargo deny\|cargo-deny' .github/workflows/ci.yml && python3 -c "import yaml,sys; d=yaml.safe_load(open('.github/workflows/ci.yml')); assert 'audit' in d['jobs']; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'actions-rust-lang/audit@v1' .github/workflows/ci.yml` matches.
    - `.github/workflows/ci.yml` parses as valid YAML and `jobs.audit` exists as a peer of `jobs.linux` and `jobs.build-windows`.
    - No `cargo fuzz` and no `cargo deny`/`cargo-deny` string appears anywhere in `ci.yml` (D-03 / D-04 boundary).
    - The `audit` job has exactly two steps (checkout + the audit action) and does NOT include `dtolnay/rust-toolchain` or `Swatinem/rust-cache`.
  </acceptance_criteria>
  <done>ci.yml has an `audit` peer job using `actions-rust-lang/audit@v1`; no fuzz/deny in CI; YAML valid.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| transitive dependency tree | CVEs in third-party crates (quinn, rustls, postcard, ring, etc.) reach the exposed server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.1-02 | Tampering / Information Disclosure | transitive dependency CVE | mitigate | cargo-audit CI gate scans Cargo.lock against RustSec DB on every push/PR (D-03) |
| T-999.1-03 | Denial of Service | CI flake from in-CI fuzz/install | accept | D-03 deliberately keeps fuzzing local-only and uses the prebuilt audit action (no cargo install) to avoid CI flake |
</threat_model>

<verification>
- `ci.yml` is valid YAML and contains the `audit` job using `actions-rust-lang/audit@v1`.
- No fuzz job, no cargo-deny step in CI.
- Human-verify (out of band, per D-16-04b precedent): the operator confirms the `audit` job goes green in the GitHub Actions tab after push. Note this in the SUMMARY as a pending human check; it cannot be confirmed from the sandbox.
</verification>

<success_criteria>
A `cargo audit` peer job is wired into CI, gating dependency CVEs; CI contains no fuzz or cargo-deny step.
</success_criteria>

<output>
Create `.planning/phases/999.1-server-attack-surface-hardening-expose-to-internet-readiness/999.1-02-SUMMARY.md` when done.
</output>
