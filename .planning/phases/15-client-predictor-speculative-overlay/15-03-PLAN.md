---
phase: 15-client-predictor-speculative-overlay
plan: 03
type: execute
wave: 3
depends_on: [15-01, 15-02]
files_modified:
  - crates/nosh-client/tests/predict.rs
autonomous: true
requirements: [PREDICT-02, PREDICT-03, PREDICT-04, PREDICT-05, PREDICT-06]
must_haves:
  truths:
    - "A vim insert (iHello<Esc>) produces zero corrupt cells and no speculative display after ESC"
    - "A read -s noecho prompt produces zero predicted characters on the client screen"
    - "Typing 你好 (CJK) advances the predicted cursor by the correct column count and renders the wide char with a blank continuation cell"
    - "less/htop cursor-addressing (CSI) input resets the epoch and disables prediction with no corruption"
    - "Bracketed paste produces no prediction during the paste; Ctrl-C mid-line cleanly resets the epoch"
    - "Over a simulated-loss link, predictions confirm/cull correctly (>= epoch check) and never render worse than no prediction"
    - "Home/End (and Ctrl-A/Ctrl-E) land the cursor on the correct confirmed column after a server update"
  artifacts:
    - path: "crates/nosh-client/tests/predict.rs"
      provides: "D-15-04 adversarial validation matrix — unit-level + live-server integration cases"
      min_lines: 250
      contains: "read -s"
  key_links:
    - from: "tests/predict.rs PredictionHarness"
      to: "nosh_client::predictor::PredictionOverlay"
      via: "drives on_input + cull, asserts cell_at / confirmed_epoch"
      pattern: "PredictionOverlay::new"
    - from: "tests/predict.rs noecho integration"
      to: "nosh_server live PTY (read -s)"
      via: "spawn_server_with_registry + client_endpoint_for"
      pattern: "spawn_server_with_registry"
---

<objective>
Implement the D-15-04 adversarial validation matrix as `crates/nosh-client/tests/predict.rs` — the hard gate that marks Phase 15 done. Cover every case: vim insert, `read -s` noecho (security), CJK `你好`, less/htop cursor-addressing, bracketed paste, Ctrl-C mid-line, simulated datagram loss, and Home/End motion. Use unit-level harness cases (construct `ClientScreen` + `PredictionOverlay` directly, feed fake `StateDiff`s) for deterministic state-machine assertions, and live-server integration cases (real `nosh-server` PTY via `spawn_server_with_registry`) for the security-critical noecho and end-to-end paths.

Purpose: Prove "never render worse than no prediction" and the noecho security requirement adversarially, not by assumption (STATE.md blocker; D-15-01c).

Output: `tests/predict.rs` passing the full D-15-04 matrix; phase gate satisfied.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-client-predictor-speculative-overlay/15-CONTEXT.md
@.planning/phases/15-client-predictor-speculative-overlay/15-RESEARCH.md
@.planning/phases/15-client-predictor-speculative-overlay/15-PATTERNS.md
@.planning/phases/15-client-predictor-speculative-overlay/15-01-SUMMARY.md
@.planning/phases/15-client-predictor-speculative-overlay/15-02-SUMMARY.md

<interfaces>
<!-- Test surface from Plan 01/02. -->
```rust
// nosh_client::predictor
pub enum PredictDisplayMode { Always, Adaptive, Never }
impl PredictionOverlay {
    pub fn new(mode: PredictDisplayMode, cols: u16, rows: u16) -> Self;
    pub fn on_input(&mut self, bytes: &[u8], screen: &ClientScreen);
    pub fn cull(&mut self, screen: &ClientScreen, new_epoch: u64, rtt_ms: u64);
    pub fn cell_at(&self, row: u16, col: u16) -> Option<Cell>;   // via Overlay trait
    pub fn predicted_cursor(&self) -> Option<CursorPos>;
    pub fn confirmed_epoch(&self) -> u64;
    pub fn prediction_epoch(&self) -> u64;
}
// nosh_client::screen: ClientScreen::new(cols, rows), apply(&StateDiff), confirmed_cell(r,c) -> &Cell, last_applied_epoch()
// nosh_proto::datagram: StateDiff, DiffRun, CellStyle, CursorPos, encode_datagram(...), decode_datagram(&[u8])
```

<!-- Test harness analogs (crates/nosh-client/tests/render.rs + tests/common/mod.rs) — copy verbatim per 15-PATTERNS.md "predict.rs" section: -->
<!--   server_with_key(registry, client_key) -> common::TestServer  (spawn_server_with_registry, AuthLimits::default(), Some(SH)) -->
<!--   client_endpoint_for(key) -> (quinn::Endpoint, TempDir)  (common::client_endpoint + known_hosts tempdir) -->
<!--   const SH: &str = "/bin/sh"; have_sh() guard; mod common; use common::{spawn_server_with_registry, TestKey, HOST}; -->
<!--   make_diff(epoch, chars, cursor) helper to construct a StateDiff for unit cases (model on render.rs make_diff). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test harness + unit-level matrix (vim, CJK, less, paste, Ctrl-C, loss, Home/End)</name>
  <files>crates/nosh-client/tests/predict.rs</files>
  <read_first>
    - crates/nosh-client/tests/render.rs (entire file — header, server_with_key, client_endpoint_for, make_diff, unit + integration patterns)
    - crates/nosh-client/tests/common/mod.rs (spawn_server_with_registry, TestKey, client_endpoint, HOST, client_identity)
    - crates/nosh-client/src/predictor.rs (the engine API + its inline tests for patterns to reuse)
    - .planning/phases/15-client-predictor-speculative-overlay/15-RESEARCH.md "Adversarial Test Strategy" (PredictionHarness shape, the D-15-04 matrix table with per-case Setup/Assert, simulated-loss epoch N then N+2)
    - .planning/phases/15-client-predictor-speculative-overlay/15-CONTEXT.md D-15-04 (the full matrix incl. Home/End extras)
    - .planning/phases/15-client-predictor-speculative-overlay/15-PATTERNS.md "predict.rs" section (test header, pure-unit-helper pattern, make_diff)
  </read_first>
  <action>
    Create `crates/nosh-client/tests/predict.rs` with the header doc per 15-PATTERNS.md (Phase 15 prediction tests; D-15-04 matrix). Add a `make_diff(epoch: u64, chars: &str, cursor: CursorPos) -> StateDiff` helper modeled on render.rs.

    Implement the unit-level D-15-04 cases (construct `ClientScreen::new(80,24)` + `PredictionOverlay::new(...)` directly, no QUIC), each `#[test]` with a descriptive assert message:
    - vim_insert_zero_corrupt_cells: feed 'i' → assert epoch reset (no visible prediction); feed 'H','e','l','l','o' with a confirming `make_diff` (matching chars at advancing cursor) between each so confirmed_epoch advances and cells show; feed `\x1b` (ESC) → after ESC assert `predicted_cursor()` is None and every `cell_at(row, c)` over the typed region returns None OR equals the confirmed char (zero corrupt cells — no predicted char that disagrees with confirmed).
    - cjk_wide_char_column_advance: feed the 3-byte UTF-8 for '你' (use `"你".as_bytes()`); assert predicted_cursor col advanced by 2; with an Always-mode predictor and confirming diff, `cell_at(0,0)` shows '你' and `cell_at(0,1)` is blank/None (continuation). Repeat for '好'. (PREDICT-06)
    - less_cursor_addressing_disables_prediction: feed `\x1b[H` (CSI cursor home) → assert pending empty / `predicted_cursor()` None immediately (epoch reset). (PREDICT-03)
    - bracketed_paste_no_prediction: feed `\x1b[200~`, then "abc", then `\x1b[201~`; assert `cell_at` returns None for the pasted chars throughout (Always mode), and the epoch was reset.
    - ctrl_c_midline_clean_reset: feed "ab" (with confirms), then `\x03`; assert pending empty / all tentative after Ctrl-C, no stale predicted cells.
    - simulated_loss_ge_epoch_confirm: feed 5 keystrokes producing predictions with epoch_required 1..=5; cull with epochs 1, 3, 5 only (skip 2,4), each with a confirming `make_diff`; assert all predictions for epochs 1,3,5 AND the skipped 2,4 are confirmed by the `>=` check (pending empty of those), and no stale predictions remain after epoch 5; assert no full reset occurred (no spurious clearing). (PREDICT-02, Pitfall 4)
    - home_end_motion_lands_correct_column: pre-load a confirmed row with content "hello" via `screen.apply(make_diff(...))`; feed `\x1b[F` (End) → assert predicted_cursor col == 5 (end of content); feed `\x1b[H` (Home) → col == 0; also test Ctrl-A (0x01)→0 and Ctrl-E (0x05)→5. After a follow-up confirming diff, assert the cursor lands on the correct confirmed column. (D-15-01 Home/End extra)
    - rtt_adaptive_loopback_invisible: with `PredictDisplayMode::Adaptive`, feed a keystroke and `cull` with `rtt_ms = 5` (loopback) → assert `cell_at` returns None and `predicted_cursor()` is None (criterion #4 / PREDICT-05); then `cull` with `rtt_ms = 100` → predictions become visible and underlined. (PREDICT-05)
  </action>
  <verify>
    <automated>cargo test -p nosh-client --test predict 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p nosh-client --test predict` passes all unit-level cases.
    - Each named D-15-04 case is present: `grep -cE "vim_insert|cjk_wide|less_cursor|bracketed_paste|ctrl_c_midline|simulated_loss|home_end|rtt_adaptive" crates/nosh-client/tests/predict.rs` >= 8.
    - The simulated-loss case skips epochs (asserts the `>=` tolerance, not `==`): the test culls with non-consecutive epochs (e.g. 1,3,5) and asserts skipped-epoch predictions are still confirmed.
    - The CJK case uses real UTF-8 bytes (`"你".as_bytes()`), not a synthetic width.
    - The adaptive-loopback case asserts invisibility at low RTT and visibility at high RTT.
  </acceptance_criteria>
  <done>tests/predict.rs covers vim, CJK, less, paste, Ctrl-C, simulated-loss, Home/End, and adaptive-RTT cases as passing unit-level tests against the real engine.</done>
</task>

<task type="auto">
  <name>Task 2: Live-server integration cases — read -s noecho (security) + end-to-end echo</name>
  <files>crates/nosh-client/tests/predict.rs</files>
  <read_first>
    - crates/nosh-client/tests/predict.rs (the file from Task 1 — extend it)
    - crates/nosh-client/tests/render.rs lines 35-56 (server_with_key, client_endpoint_for) and lines 324-455 (async integration pattern: connect, drive session, read_datagram loop, timeout/assert)
    - crates/nosh-client/tests/common/mod.rs (spawn_server_with_registry signature, AuthLimits, TestKey, HOST)
    - crates/nosh-client/src/client.rs (connect, send_input — to drive keystrokes into the live PTY)
    - .planning/phases/15-client-predictor-speculative-overlay/15-RESEARCH.md "Noecho Suppression" (the structural mechanism + the security validation requirement) and "Adversarial Test Strategy" (read -s case Setup/Assert)
    - .planning/phases/15-client-predictor-speculative-overlay/15-CONTEXT.md D-15-01c (noecho is a hard security requirement, validated adversarially not assumed)
  </read_first>
  <action>
    Copy the `server_with_key` and `client_endpoint_for` helpers verbatim from render.rs (per 15-PATTERNS.md), plus the `have_sh()` guard and `const SH`. Add `#[tokio::test]` integration cases that spawn a real server PTY:

    - noecho_read_dash_s_zero_predicted_chars (SECURITY — D-15-01c / PREDICT-04): connect a live client to a `/bin/sh` session; drive a `read -s X` (or `stty -echo`) prompt into the PTY via `client::send_input`; wait for the server StateDiff(s) establishing the prompt; then send typed password characters via `send_input` AND feed the same bytes to a local `PredictionOverlay::new(PredictDisplayMode::Always, ...)` (worst case — even in Always mode the engine must suppress); apply each incoming StateDiff to a `ClientScreen` and call `predictor.cull(&screen, diff.epoch, 100)`. ASSERT: after the server has processed the input, `predictor.cell_at(cursor_row, c)` returns None for every typed password char position, `predictor.confirmed_epoch() < predictor.prediction_epoch()` throughout the noecho window, and the confirmed screen contains none of the typed password characters. Use the render.rs timeout/deadline loop pattern; skip cleanly via `have_sh()` if `/bin/sh` is unavailable.
    - end_to_end_printable_echo_confirms (PREDICT-02 e2e): in a normal echoing `/bin/sh`, send a printable char, predict it locally (Always mode), and assert that an incoming StateDiff with the echoed char advances `predictor.confirmed_epoch()` and the prediction is culled (confirmed), demonstrating the live confirm path end-to-end.

    For both, drive the session using the exact connect + send_input + read_datagram loop from render.rs's integration test; assert with descriptive panic messages on timeout. Do NOT assert on raw terminal escape bytes — assert on `ClientScreen`/`PredictionOverlay` state (the research-recommended assertion surface).
  </action>
  <verify>
    <automated>cargo test -p nosh-client --test predict -- --include-ignored 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p nosh-client --test predict` runs the integration cases (or skips cleanly when `/bin/sh` is unavailable via `have_sh()`); no test fails.
    - The noecho case actually runs a `read -s` / `stty -echo` against a LIVE server PTY (not a fabricated diff): `grep -cE "read -s|stty -echo|read -" crates/nosh-client/tests/predict.rs` >= 1, AND `grep -c "spawn_server_with_registry\|server_with_key" crates/nosh-client/tests/predict.rs` >= 1.
    - The noecho case asserts `cell_at(...) == None` for password chars AND `confirmed_epoch() < prediction_epoch()` (security invariant): both assertions present in that test fn.
    - The noecho predictor is constructed in `Always` mode (worst case) so the test proves structural suppression, not RTT gating: `grep -c "PredictDisplayMode::Always" crates/nosh-client/tests/predict.rs` >= 1 in the noecho fn region.
    - Assertions are on ClientScreen/PredictionOverlay state, not raw ANSI bytes.
  </acceptance_criteria>
  <done>Live-server integration cases prove the noecho security requirement adversarially against a real read -s PTY (zero predicted chars, confirmed_epoch frozen below prediction_epoch) and demonstrate the end-to-end confirm path; the full D-15-04 gate passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live server PTY → client predictor (test) | The integration test exercises the real server-echo authority boundary: noecho on the server PTY must keep predictions hidden on the client. |
| test keystroke → send_input → server | The test drives real keystrokes; assertions confirm prediction never leaks unechoed chars. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-10 | Information disclosure | unverified noecho suppression (the core security gate) | mitigate | An adversarial live-PTY `read -s` test (Task 2) asserts zero predicted password chars and `confirmed_epoch < prediction_epoch`; constructed in Always mode so it cannot pass merely because RTT gating hid output. Closes the STATE.md blocker requiring adversarial (not assumed) validation. |
| T-15-11 | Spoofing | a test that "passes" without exercising the real path | mitigate | Acceptance greps require a LIVE `read -s`/`stty -echo` against `spawn_server_with_registry` (not a fabricated diff) and assertions on PredictionOverlay state, preventing a vacuous pass. |
| T-15-12 | Tampering | flaky timing causing false confirmation | accept | Uses the established render.rs deadline/timeout loop with explicit panic-on-timeout; same flakiness envelope as the existing Phase 14 integration tests. `have_sh()` guard avoids false failures where `/bin/sh` is absent. |
</threat_model>

<verification>
- `cargo test -p nosh-client --test predict` passes the full D-15-04 matrix (unit + integration), or skips integration cleanly when `/bin/sh` is absent.
- The noecho case runs a real `read -s`/`stty -echo` against a live server PTY and asserts zero predicted chars + frozen confirmed_epoch in Always mode.
- All eight D-15-04 named cases present and passing; CJK uses real UTF-8 bytes; simulated-loss asserts the `>=` epoch tolerance.
- `cargo clippy -p nosh-client --tests -- -D warnings` — no new warnings.
</verification>

<success_criteria>
The D-15-04 validation matrix passes end-to-end: vim insert produces zero corrupt cells, `read -s` against a live PTY produces zero predicted characters (security gate proven adversarially), CJK advances columns correctly, cursor-addressing/paste/Ctrl-C reset cleanly, simulated datagram loss confirms via the `>=` epoch check without rendering worse than no prediction, and Home/End land on the correct confirmed column — satisfying PREDICT-02 through PREDICT-06 as a hard phase gate.
</success_criteria>

<output>
Create `.planning/phases/15-client-predictor-speculative-overlay/15-03-SUMMARY.md` when done.
</output>
